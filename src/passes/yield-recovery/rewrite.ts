// docs/specs/passes/25-yield-async-recovery.md -- spec 25 section 3.3: the group collapses to one `function*`
// statement at the stub's position.
export { rewriteGeneratorGroup as rewrite } from "../tree.ts";
