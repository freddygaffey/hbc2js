# Known bugs ledger

Every bug found that is not yet fixed-with-a-regression-test lives here until it is. Rows are removed only when a test exists.

| date | source | component | description | why no test yet | owner |
|---|---|---|---|---|---|
| 2026-08-31 | npm-test-gate-speed task (gate wall-time investigation); root cause NOT the same as the (now-fixed) label-clean/expr-rebuild hang — that was an unmemoized exponential recursion (performance), this is a wrong *value*, so they are unlikely to share a cause; still unconfirmed which pass is responsible | `src/passes/**` | `01-if-else-chain.min` at v84/v94 only (v96/98/99, and the un-minified `01-if-else-chain` at every version, all correct) decompiles to code with wrong runtime behaviour: the Hermes VM's own execution of the original bytecode prints `-5 -> negative`, the decompiled candidate prints `-5 -> undefined` — the negative-number branch of a basic if/else chain is mishandled. Verified against the real trace oracle (`src/harness/ladder.ts`'s `runOracleLadder`, D14 Hermes-VM cross-check), reproducible across repeats (not a flake). Isolated with `decompile(bytes, {..., passes: {none: true}})`: passes and matches expected output with every M5 pass disabled, so this is also a pass-pipeline regression, not `src/cfg`/`src/structure`/`src/emit`. | excluded (not fixed) via `src/harness/tiers.ts`'s new `KNOWN_WRONG_OUTPUT` table so `runTier`'s callers don't fail on it; a real regression test needs the actual `src/passes` bug identified and fixed. | M5 passes lane (unassigned) |
| 2026-08-30 | review M4-M8 | `src/deps/dscan.ts` | calls `readLiterals(mod.literalValueBuffer, …)` without a version, so at v≥97 (every `literalValueBuffer` module) a tag-6 run is read as a 1-byte ByteString instead of payload-less `Undefined` and the rest of the run desynchronises | **fixed in code** (deps review, 2026-08-30: the version is now passed); no gate fixture is a v≥97 Metro bundle, so the regression check is the sweep-tier react-navigation-example (hbc98) inventory test until one exists | deps lane |
| 2026-08-30 | deps --confirm sweep verification | `src/deps/confirm.ts` (`resolveCandidateVersion`/`nearestVersionByDate`) | "nearest npm release by date" can resolve a fast-moving package (`react-native-gesture-handler`, `react-native-screens`, `react-native-safe-area-context`, `react-native-pager-view` — all publish nightlies continuously) to an unbundleable nightly build, so `tests/sweep/deps/confirm-react-navigation.test.ts`'s own `>= 5 confirmed` floor sometimes isn't met on a live network run (2/5 confirmed one run, 5/5 another, same code) — precision stayed 100% every time observed, only recall is network-luck-dependent | sweep-tier, real-network, nondeterministic by nature (npm's nightly cadence); would need version-selection logic (e.g. preferring a non-prerelease nearest release) to fix, which is a design change out of the verification task's scope | deps lane |
| 2026-08-31 | adversarial fixture generation: 28-this-binding-extracted, 29-var-hoisting-redeclaration | `tests/fixtures/adversarial/README.md`'s committed `expected.txt` generation recipe (`node -e "...; require('./source.js')"`) | this repo's own `package.json` has `"type": "module"`, so Node's file-based module resolution force-parses every fixture `source.js` as an ES module when generated with that recipe — for **28** that makes a bare function call's `this` stay `undefined` (always-strict ESM) instead of substituting `globalThis` (sloppy-mode spec behaviour), turning `this.value` into a caught TypeError; for **29** a top-level `function myVar(){}` + `var myVar` collide under ES-module top-level scoping rules, throwing a `SyntaxError` before the program ever runs at all. **Neither is a real Hermes-vs-Node/spec divergence**: forcing CommonJS explicitly (`node --input-type=commonjs -e '<source>'`, and separately renaming to `.cjs` and `require()`-ing it) reproduces the fixtures' actual, correct sloppy/CJS behaviour on Node — `extracted: undefined-value` for 28, `hoisting trace: function\|function\|string\|string-value\|is-string` for 29 — which matches the Hermes VM (v94/v96/v99, both fixtures) **and** the real decompiler's output exactly. The harness's own sandbox (`src/harness/*` trace runner used by `runOracleLadder`) already sidesteps this — `runTier({tier:"adversarial",...})` reports **PASS** for both — so no fixture reclassification or `reference-policy.ts` change was needed; only the two committed `expected.txt` files (human-facing documentation, not consumed by the harness) are wrong and should be regenerated with an explicit CJS invocation (or the generation recipe in the README fixed to force `--input-type=commonjs`) so they stop misleading a reader who compares by eye. | **fixed (CONSOLIDATION 24)**: 28/29 (and the ESM-loader-frame-tainted 36/41) regenerated with the README's explicit `--input-type=commonjs` recipe; regression test `tests/gate/harness/adversarial-expected.test.ts` re-derives every adversarial `expected.txt` in script mode on every gate run (exact for exit-0 programs, stdout-prefix for deliberately-crashing ones) and rejects the ESM force-parse signature outright | fixture lane |
| 2026-08-31 | `src/harness/ladder.ts`'s D14 Hermes-VM cross-check (adversarial/36-optional-chaining-sideeffect, all versions) | harness comparison method | the cross-check compares `printLines(candidateTrace)` (`src/harness/trace.ts`'s `printLines`, which by design only keeps `"out"` records, never `"err"`) against the Hermes VM's *raw combined stdout+stderr* text (`hermes-vm.ts`'s `runHermes`, which naturally includes Hermes's own crash line when the program throws uncaught). For any fixture whose top-level program deliberately/legitimately throws an uncaught error — 36 is exactly this, `null.method?.()` throwing `TypeError` by spec — the candidate side's projection never has a crash line while the VM side's raw text always does, so `candidatePrint !== hermesPrint` fires and the ladder reports DIVERGENT even when the two sides agree on everything: same print sequence, same crash site, same error *type*, differing only in each engine's own native message wording (Hermes: "Cannot read property 'method' of null"; V8/Node: "Cannot read properties of null (reading 'method')") — exactly the class of thing the existing `--relax error-messages` flag already exists to acknowledge is unsound to compare. Verified directly for fixture 36 at v94/v96/v99: VM and decompiled-under-Node agree on `r1`/`r2`/`r3`/`trace` and the crash line/location, both engines. Fix belongs in `ladder.ts`'s cross-check, not the decompiler: either drop the VM's trailing error line before the print-projection compare, or extend the projection on both sides to include a normalised (type-only) terminal-error record. Left unfixed here — `ladder.ts` is shared core harness code (gate + hardened + sweep + this new adversarial tier all call it), out of this task's "minimal runner hook" scope. | **fixed (CONSOLIDATION 25)**: both sides now project the same way — `printProjection` (trace.ts: print lines + `uncaught <Name>` for a main-phase throw) vs `hermesPrintProjection` (hermes-vm.ts: stdout lines + `uncaught <Name>` parsed from the VM's stderr crash report), name only, never the engine-specific message. Regression test `tests/gate/harness/ladder-uncaught.test.ts`: a deliberately-throwing program is PASS when the candidate throws the same type (even with V8's different wording), DIVERGENT when it does not throw / throws another type / throws where the bytecode does not; and fixture 36 through the real decompiler is PASS at v94. Adversarial tier: 36 PASS at v94/v96/v99 | M5/harness lane |
| 2026-08-31 | adversarial fixture metadata: 02-proxy-trap-counting, 06-closure-loop-var-vs-let | `tests/fixtures/adversarial/<name>/versions.txt` | both fixtures' committed `versions.txt` blanket-claims `v94: FAILS`, `v96: FAILS`, `v99: FAILS` ("check source.js for details"), but all three `.hbc` files are present, compile, and run fine under every Hermes VM (v94/v96/v99) — the file is simply stale/wrong, most likely leftover boilerplate never cleaned up after the fixtures were actually compiled successfully. `src/harness/tiers.ts`'s new `discoverAdversarialInputs` deliberately does not consult this file at all (trusts `.hbc` file presence instead) for exactly this reason — see that function's own comment. | cosmetic/metadata only, no functional impact on the new adversarial tier; low priority, delete or correct the two `versions.txt` files whenever convenient | fixture lane |
| 2026-08-31 | CONSOLIDATION 26 triage of adversarial/21-class-private-fields (v99.hbc) — first surfaced by wiring the adversarial sweep tier (`tests/sweep/adversarial/report.test.ts`), not part of the 6-fixture triage | `src/emit/lower.ts` (`PutOwnGetterSetterByVal` / `DefineOwnGetterSetterByVal` case, ~line 451) — **emit stage** | **(a) real decompiler bug, confirmed.** Static Hermes (v98/v99) lowers a class getter/setter *pair* as two instructions, each with the other half `undefined` — `21/v99.hbc` fn#0: `0032 LoadConstUndefined r1`, `0039 DefineOwnGetterSetterByVal r8, r7, r9, r1, 0` (getter `f3 "get value"`), then `0044 DefineOwnGetterSetterByVal r8, r7, r1, r6, 0` (setter `f4 "set value"`). The VM (`Interpreter::caseDefineOwnGetterSetterByVal`, `lib/VM/Interpreter-slowpaths.cpp` @ `913d31a`) sets `dpFlags.setGetter` / `setSetter` **only for the non-undefined operand** and leaves the other half of an existing accessor alone; the emit unconditionally writes `Object.defineProperty(r8, r7, {get: _fn3, set: r1})` then `Object.defineProperty(r8, r7, {get: r1, set: _fn4})` — the second call replaces the getter with `undefined`, so every later `c.value` read is `undefined` (VM `initial: 0` / `after set: 100`, candidate `undefined` / `undefined`; `inc()` reads the private symbol directly, which is why `after inc: 1 2` / `final: 101` still match). Not a private-field bug at all — `#value` storage and reads are right. Not a toolchain artefact either: the VM's own `hermesc` (same commit as the VM) emits the identical two-instruction shape and the VM prints the correct values. Object-literal accessor pairs are unaffected at every version (hermesc merges them into ONE `PutOwn`/`DefineOwnGetterSetterByVal`, even with other keys between the two halves — probed at v94 and v99), which is why `36-class-getters-setters`' object half passes; its `Rectangle` class half only passes because nothing reads `r.width` after the setter is defined. Minimal reproducer: `class Box { get v(){…} set v(x){…} }` then read `b.v` → construct fixture `tests/fixtures/constructs/58-class-accessor-pair-split` (v98/v99 only; VM `get after pair: 1` / `get after set: 7`, candidate `undefined` / `undefined` at both versions). Fix (not in this task): omit the `get:`/`set:` key whose operand register is a literal `undefined` — `Object.defineProperty` with only the other key leaves an existing accessor half untouched, exactly the VM's semantics. | **FIXED (consolidation item 3, 2026-08-31)**: `src/emit/lower.ts` now omits the `get:`/`set:` descriptor key whose operand register statically holds a literal `undefined` (`isLiteralUndefinedReg`: nearest preceding write in the block is `LoadConstUndefined`, or every write in the function is); v99 trace-EQUIVALENT (`get after pair: 1` / `get after set: 7`). Regression test: `58-class-accessor-pair-split@98/@99` removed from `KNOWN_WRONG_OUTPUT`, so the gate runs it as an ordinary fixture. | fixed |
| 2026-08-31 | CONSOLIDATION 26 triage of adversarial/20-symbol-keyed-properties (v99.hbc) — first surfaced by wiring the adversarial sweep tier, not part of the 6-fixture triage | toolchain + harness — **(b) oracle/harness artefact**, not a decompiler bug against the available ground truth; it does expose a real gap in `src/tables/generated/builtins-hbc99-*.ts` (parse/disasm-table stage), see below | The fixture's `v99.hbc` was compiled by `tools/hermesc/v99/hermesc` (`hermes-compiler@260318099`, ≈2026-03-18) but the reference VM `tools/hermes-vm/v99` is built from Hermes `913d31a` (2026-03-05). Between those revisions Hermes added `HermesBuiltin.setFunctionName`, emitted for every **computed-key method** (`{ [Symbol.iterator]() {} }`, `{ [k]() {} }`, class computed/private static members): `20/v99.hbc` fn#0 `0079 ToPropertyKey r2, r1` … `0089 CallBuiltin r4, b55, 4` … `008d DefineOwnByVal r5, r1, r2, 1`, where `hermesc -dump-bytecode` names b55 `HermesBuiltin.setFunctionName` but index 55 means `functionPrototypeApply` in the VM's table and in the decompiler's `HBC99_MAR2026` (generated from the *same* `913d31a`). So the VM itself mis-executes this bytecode (`Uncaught TypeError: Can't apply() to non-callable` at `source.js:18:18` — the method definition, not the `for-of`), and the decompiler faithfully emits the same wrong builtin (`r1.apply(r2, 0)` → V8 `TypeError: CreateListFromArrayLike called on non-object`): candidate and VM die at the same site with the same error *name*, only the native message differs. Proof it is the toolchain, not the code: the VM's own `hermesc` (same commit) compiles `source.js` with no `CallBuiltin` there at all (`ToPropertyKey` / `CreateClosure` / `DefineOwnByVal` only — 964-byte vs 928-byte .hbc), and both `hermes source.js` and `hermes -b <that .hbc>` print `custom iterator: 1,2,3`. v94/v96 PASS because their compilers predate the builtin. Why the harness reports DIVERGENT rather than PASS-vs-VM: `src/harness/ladder.ts`'s D14 cross-check only ever *adds* a divergence when the candidate disagrees with the VM; when the candidate **agrees with the VM but not with `source.js` under Node** (here: both project `uncaught TypeError`, Node prints `1,2,3`) the Node-vs-Node `traces diverge at record 3` verdict is kept — the VM overrules Node only for fixtures in `KNOWN_DIVERGENT_FIXTURES`, contrary to that function's own D14 comment. **Wider consequence (real, table stage):** `setFunctionName` was inserted at index 55, shifting every private builtin after it by one under the `260318099` compiler — probed with the npm compiler: `HermesBuiltin.spawnAsync` = b58 (our table: `makeAsyncIterator`), `makeAsyncIterator` = b59 (ours: `awaitAsyncGenerator`); indices ≤54 (`getTemplateObject` 41, `copyDataProperties` 46, `arraySpread` 48, `apply` 49) are unchanged. This — not a "broken VM build" — is the root cause of every entry in `reference-policy.ts`'s `VM_LIMITATIONS` (07-for-of-iterable has a custom `[Symbol.iterator]()`; 27/28/29/31/54 are `async`): a plain `async function` compiled by the npm v99 compiler dies in the VM with `undefined is not a function at _makeAsyncIterator`. The decompiler survives the async shift today only because async lowering keys off function flags, not the builtin name (`27-async-await-basic@99` decompiles correctly), but any production RN "1000.x" bundle containing a computed-key method decompiles to a bogus `.apply(...)` call that throws. "Format version 99" does not pin the builtin table (cf. docs/TOOLCHAIN.md on the two v99 opcode tables). | no decompiler code change in this task. Three follow-ups, none done here: (1) a `builtins-hbc99-<post-0318>` table with `setFunctionName` at 55 (+1 shift after it), selected by probing (the decompiler already probes between two v99 opcode tables), plus an emit for `setFunctionName` (a no-op in source form — a method definition already names its function); (2) rebuild `tools/hermes-vm/v99` from a commit at or after the one adding `setFunctionName` so D14 has a matching VM, then retire `VM_LIMITATIONS`; (3) `ladder.ts`: let VM==candidate override a Node-vs-candidate divergence (PASS-with-caveat), per its own D14 comment. Adversarial 20 stays DIVERGENT at v99 until (1)+(2) or (3). No construct fixture: one already exists for the shape (`07-for-of-iterable`, `VM_LIMITATIONS`-excused at v99) and the crash is in the oracle, not the decompiler. **fix: not in this task** | tables/toolchain lane + harness lane (unassigned) |
| 2026-08-31 | adversarial/06-closure-loop-var-vs-let (v94.hbc) | decompiler closure capture | for-loop `let` bindings should create fresh variables per iteration, allowing closures to capture different values (0,1,2), but decompiled code treats it like `var`, causing all closures to capture the final value (3,3,3) | regression test in place (adversarial fixture 06) | M5 |
| 2026-08-31 | adversarial/02-proxy-trap-counting (v94.hbc) | decompiler Proxy handler | `'x' in proxy` should invoke the `has` trap (count=1), but decompiled code doesn't call it (count=0) | **no longer reproduces (CONSOLIDATION 26, 2026-08-31)**: `runTier({tier:"adversarial", only:["02-proxy-trap-counting"]})` is PASS at v94/v96/v99, and the decompiled v94 candidate's output is byte-identical to `tools/hermes-vm/v94`'s own run (`has traps: 1`). Fixed as a side effect of an intervening commit, not bisected; adversarial fixture 02 stays as the regression test | M5 |
| 2026-08-31 | adversarial/28-this-binding-extracted (v94.hbc) | decompiler this binding | method extracted and called without context should error or get undefined-this behavior; decompiled code shows undefined-value instead of expected error | regression test in place (adversarial fixture 28) | M5 |
| 2026-08-31 | adversarial/29-var-hoisting-redeclaration (v94.hbc) | decompiler hoisting | var redeclaration after function with same name — decompiled output diverges from expected | regression test in place (adversarial fixture 29) | M5 |
| 2026-08-31 | adversarial/30-tdz-shadowing (v94.hbc) | decompiler TDZ | shadowing with TDZ (accessing `inner` before declaration in a scope where `inner` exists in outer scope) should throw ReferenceError, decompiled code diverges | regression test in place (adversarial fixture 30) | M5 |
| 2026-08-31 | adversarial/36-optional-chaining-sideeffect (v94.hbc) | decompiler optional chaining | optional chaining with side effects throws during decompiled execution; check source for error details | **not a bug** — false positive of the ladder cross-check artifact above (row 2026-08-31 `src/harness/ladder.ts`), fixed under CONSOLIDATION 25; fixture 36 is PASS-vs-VM at v94/v96/v99 | M5 |
| 2026-08-30 | T14 adversarial pass-ladder testing | `src/passes/global-access/{match,check}.ts` | `isProvenGlobal` proves a register global by scanning every write across the *whole function* and requiring exactly one write literally valued `globalThis`, chronologically first — it never checks whether a *later* write in the same statement list (after the guarded read) can re-execute before that read on a repeat visit. When the guard+read+a later clobbering write all live in one loop body, the fold is accepted anyway: `check()` re-derives the identical unsound proof, so it is no independent guard either. Confirmed by directly executing both the pre- and post-rewrite forms under Node (no D14 ambiguity — no `let`-in-loop, no TDZ, no `arguments`, plain property reads): the un-rewritten program correctly prints `GLOBAL, OTHER` over two iterations; the rewritten one wrongly prints `GLOBAL, GLOBAL`, reading the real global on every iteration instead of the clobbered local from the 2nd iteration on. Not VM-dependent — reproducible with plain `node -e`. | regression test in place, pinned to the current (wrong) behaviour: `tests/gate/passes/adversarial-ladder.test.ts` ("global-access BUG (pinned)"); flip the assertions to the correct refusal in the same commit that fixes `isProvenGlobal`/`classifySite` to account for repeat-visit (loop-body) re-entry | M5 pass-ladder lane |
| 2026-08-30 | T14 adversarial pass-ladder testing | `src/structure/structure.ts` (`ramsey`'s `maxDepth` recursion guard, default 1500, `src/structure/index.ts`) | The guard exists so a pathologically deep function refuses cleanly (`E_TOO_COMPLEX`) instead of crashing, but on a cold process (the realistic case — every `hbc2js` CLI invocation starts fresh) a flat chain of basic blocks with no actual nesting (e.g. a very large switch, or thousands of sequential jump-separated statements) overflows Node's real V8 call stack with an uncaught `RangeError: Maximum call stack size exceeded` well before the counter reaches 1500: measured boundary ≈1030 blocks still structure cleanly, ≈1075+ crash — over 400 short of the documented limit. The guard's per-recursion-unit stack cost was evidently sized without a cold-process, un-JIT-warmed measurement: the same chain lengths structure fine *later in the same process* (after other recursions have run), which is why this must be measured in a fresh child process, not merely in-process. Not VM-dependent, not D14-related — a pure engineering/robustness fact about `ramsey`'s recursion. **Threshold is platform-dependent** — measured ~1030 OK / ~1075 crash on a Linux x86_64 container, but a macOS Node 24 runner overflows at 1000, which is why the first version of the pinning test failed CI on macOS for the wrong reason. The portable claim is the failure *mode*: below the 1500 guard a flat chain dies with a raw `RangeError` instead of a clean `E_TOO_COMPLEX` refusal. | regression test in place, pinned to the current (wrong) behaviour: `tests/gate/passes/adversarial-ladder.test.ts` ("structure limits BUG (pinned)"), using an isolated child-process probe (`tests/gate/passes/fixtures/chain-structure-probe.mts`) so the measurement is not warmup-dependent; flip the assertion once `maxDepth`'s default (or the guard's stack-cost-per-depth-unit) is recalibrated for a cold process. Related but distinct from the already-documented `lowerTree` stack overflow on a real Shopify bundle noted in `docs/STATUS.md` (worked around there with `--stack-size=6000`) — same class of problem, different function, not yet cross-checked whether one fix covers both. | no portable regression test is possible: the threshold is a property of the host's usable V8 stack, and two attempts to pin it (a fixed 1000-block chain, then a runtime-measured ladder) each failed CI on a different macOS/Node combination for the platform rather than the defect. A test becomes possible when the guard is made stack-aware; the fix should add one then. |
| 2026-08-31 | gate-speedup agent (equivalence run) | `src/passes/label-clean` | **INFINITE LOOP**: `decompile()` never returns for `37-destructuring-array` (all versions) and `48-optional-chaining-nullish` (v84/v94) with label-clean enabled. Pass DISABLED in registry.ts 2026-08-31 (`--no-pass` confirmed the fix). Re-enable after fixing the hang. | pass disabled; needs hang fix + regression fixtures |
| 2026-08-31 | gate-speedup agent | decompiler (v84/v94) | `01-if-else-chain.min` decompiles to WRONG output (`-5 -> undefined` instead of `-5 -> negative`). Excluded via KNOWN_WRONG_OUTPUT in tiers.ts; needs root-cause. | open |
| 2026-08-31 | integration/report task (real-app `--split` end-to-end + readability check) | `src/emit/function.ts` (`shapeKeyFor`) + `--split`'s all-or-nothing failure mode | `node src/cli.ts tests/fixtures/bundles/react-navigation-example-0.85.3/react-navigation-example.hbc --split <dir>` **crashes the entire split**, exit 3, zero files written, on a real, unmodified, committed fixture (v94, 15,551 functions): `E_EMIT_UNSUPPORTED: slot 1 of r3 has no known object shape at offset 186`, isolated via a scratch script (`analyseModule`+`emitModule` directly, catching the `Hbc2jsError` for its `context`) to `functionIndex: 8640` of 15551 — `shapeKeyFor(register=3, slot=1, offset=186)` finds no `keys[]` recorded for r3 at that point, i.e. some code path reads an object-literal property by shape-table slot index without a `recordShape(3, …)` having run first on that register in that control-flow path (likely a shape-tracking gap across a branch/loop/re-entry — not further root-caused here). The `.debug.hbc` build of the same app hits the identical message at the identical offset. Reproduced with both `--passes=none` (explicit) and default (passes are already off for `--split` per `src/split/index.ts`'s own design, so this is not pass-related) and with plain non-split decompile (same crash, same fn#8640) — this is an `src/emit` bug, not `src/split`/`src/passes`. Separately, this exposed a design gap worth its own row: `--split` (and plain decompile) has **no per-function isolation** — `emitModule`'s `emitOne` recursion throws on first failure and the CLI's top-level `catch` aborts the whole run with no partial output, so at real-app scale (thousands of functions) a single unsupported construct anywhere blocks 100% of the output, not just the one affected module. Confirmed NOT a general real-app-scale problem: the same `--split` succeeded cleanly on a proprietary Bloomberg APK's bundle (HBC 96, 10.5 MB, 4995 Metro modules, 18s, 4995/4995 `node --check` clean) and on `tests/fixtures/bundles/rn-template-0.72/index.android.hbc` (435/435 modules, 2.2s) — so this is construct-specific, not size-specific. | not root-caused past the isolated `functionIndex`/offset (needs the actual construct at fn#8640 offset 186 identified — likely a real bytecode-level object literal / shape-table interaction the shape tracker doesn't cover); no regression fixture yet, and `--split`'s all-or-nothing failure mode is a separate, larger design question (per-module error isolation) out of a report-only task's scope | src/emit lane + split lane (unassigned) |
| 2026-08-31 | pipeline-speed task (PUSHBACK P-1 consolidation) — found while timing the constructs corpus with passes on | `src/passes` stage B on a `.obf` generator: `23-generator-basic/v94.obf` and `26-infinite-generator-take/v94.obf` | `decompile()` with every pass on throws `E_UNBOUND_IDENT: emitted identifier "r3" is not declared in any enclosing scope (module > _fn0 > sequence > _fn7)`; `--passes=none` is fine, and the base/`.min` variants are fine. Reproduces at HEAD before the P-1 commit (verified by stashing it), so it is a pre-existing stage-B rewrite dropping a register decl (or `pruneRegisterDecls` pruning a live one) inside a generator frame at v94 `.obf` only. | Out of the P-1 task's scope (performance); needs its own bisect by `--no-pass` to find the rung. The gate does not run these two (version, variant) pairs through `decompile()` with passes on today. | unassigned |
