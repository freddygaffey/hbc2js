# Known bugs ledger

Every bug found that is not yet fixed-with-a-regression-test lives here until it is. Rows are removed only when a test exists.

| date | source | component | description | why no test yet | owner |
|---|---|---|---|---|---|
| 2026-08-30 | review M4-M8 | `src/deps/dscan.ts` | calls `readLiterals(mod.literalValueBuffer, …)` without a version, so at v≥97 (every `literalValueBuffer` module) a tag-6 run is read as a 1-byte ByteString instead of payload-less `Undefined` and the rest of the run desynchronises | **fixed in code** (deps review, 2026-08-30: the version is now passed); no gate fixture is a v≥97 Metro bundle, so the regression check is the sweep-tier react-navigation-example (hbc98) inventory test until one exists | deps lane |
| 2026-08-30 | deps --confirm sweep verification | `src/deps/confirm.ts` (`resolveCandidateVersion`/`nearestVersionByDate`) | "nearest npm release by date" can resolve a fast-moving package (`react-native-gesture-handler`, `react-native-screens`, `react-native-safe-area-context`, `react-native-pager-view` — all publish nightlies continuously) to an unbundleable nightly build, so `tests/sweep/deps/confirm-react-navigation.test.ts`'s own `>= 5 confirmed` floor sometimes isn't met on a live network run (2/5 confirmed one run, 5/5 another, same code) — precision stayed 100% every time observed, only recall is network-luck-dependent | sweep-tier, real-network, nondeterministic by nature (npm's nightly cadence); would need version-selection logic (e.g. preferring a non-prerelease nearest release) to fix, which is a design change out of the verification task's scope | deps lane |
| 2026-08-31 | adversarial fixture generation: 28-this-binding-extracted, 29-var-hoisting-redeclaration | `tests/fixtures/adversarial/README.md`'s committed `expected.txt` generation recipe (`node -e "...; require('./source.js')"`) | this repo's own `package.json` has `"type": "module"`, so Node's file-based module resolution force-parses every fixture `source.js` as an ES module when generated with that recipe — for **28** that makes a bare function call's `this` stay `undefined` (always-strict ESM) instead of substituting `globalThis` (sloppy-mode spec behaviour), turning `this.value` into a caught TypeError; for **29** a top-level `function myVar(){}` + `var myVar` collide under ES-module top-level scoping rules, throwing a `SyntaxError` before the program ever runs at all. **Neither is a real Hermes-vs-Node/spec divergence**: forcing CommonJS explicitly (`node --input-type=commonjs -e '<source>'`, and separately renaming to `.cjs` and `require()`-ing it) reproduces the fixtures' actual, correct sloppy/CJS behaviour on Node — `extracted: undefined-value` for 28, `hoisting trace: function\|function\|string\|string-value\|is-string` for 29 — which matches the Hermes VM (v94/v96/v99, both fixtures) **and** the real decompiler's output exactly. The harness's own sandbox (`src/harness/*` trace runner used by `runOracleLadder`) already sidesteps this — `runTier({tier:"adversarial",...})` reports **PASS** for both — so no fixture reclassification or `reference-policy.ts` change was needed; only the two committed `expected.txt` files (human-facing documentation, not consumed by the harness) are wrong and should be regenerated with an explicit CJS invocation (or the generation recipe in the README fixed to force `--input-type=commonjs`) so they stop misleading a reader who compares by eye. | not yet regenerated (expected.txt ownership is the fixture-authoring lane, not this triage's) | fixture lane |
| 2026-08-31 | `src/harness/ladder.ts`'s D14 Hermes-VM cross-check (adversarial/36-optional-chaining-sideeffect, all versions) | harness comparison method | the cross-check compares `printLines(candidateTrace)` (`src/harness/trace.ts`'s `printLines`, which by design only keeps `"out"` records, never `"err"`) against the Hermes VM's *raw combined stdout+stderr* text (`hermes-vm.ts`'s `runHermes`, which naturally includes Hermes's own crash line when the program throws uncaught). For any fixture whose top-level program deliberately/legitimately throws an uncaught error — 36 is exactly this, `null.method?.()` throwing `TypeError` by spec — the candidate side's projection never has a crash line while the VM side's raw text always does, so `candidatePrint !== hermesPrint` fires and the ladder reports DIVERGENT even when the two sides agree on everything: same print sequence, same crash site, same error *type*, differing only in each engine's own native message wording (Hermes: "Cannot read property 'method' of null"; V8/Node: "Cannot read properties of null (reading 'method')") — exactly the class of thing the existing `--relax error-messages` flag already exists to acknowledge is unsound to compare. Verified directly for fixture 36 at v94/v96/v99: VM and decompiled-under-Node agree on `r1`/`r2`/`r3`/`trace` and the crash line/location, both engines. Fix belongs in `ladder.ts`'s cross-check, not the decompiler: either drop the VM's trailing error line before the print-projection compare, or extend the projection on both sides to include a normalised (type-only) terminal-error record. Left unfixed here — `ladder.ts` is shared core harness code (gate + hardened + sweep + this new adversarial tier all call it), out of this task's "minimal runner hook" scope. | no fix, no regression test yet (would need a construct/adversarial fixture that legitimately crashes uncaught to be the regression case) | M5/harness lane |
| 2026-08-31 | adversarial fixture metadata: 02-proxy-trap-counting, 06-closure-loop-var-vs-let | `tests/fixtures/adversarial/<name>/versions.txt` | both fixtures' committed `versions.txt` blanket-claims `v94: FAILS`, `v96: FAILS`, `v99: FAILS` ("check source.js for details"), but all three `.hbc` files are present, compile, and run fine under every Hermes VM (v94/v96/v99) — the file is simply stale/wrong, most likely leftover boilerplate never cleaned up after the fixtures were actually compiled successfully. `src/harness/tiers.ts`'s new `discoverAdversarialInputs` deliberately does not consult this file at all (trusts `.hbc` file presence instead) for exactly this reason — see that function's own comment. | cosmetic/metadata only, no functional impact on the new adversarial tier; low priority, delete or correct the two `versions.txt` files whenever convenient | fixture lane |
| 2026-08-31 | adversarial/20-symbol-keyed-properties (v99.hbc), adversarial/21-class-private-fields (v99.hbc) | undetermined — surfaced by wiring the new adversarial sweep tier (`tests/sweep/adversarial/report.test.ts`), **not part of the 6-fixture Haiku triage this row's siblings above cover** | **21** looks like a real decompiler bug, high confidence: VM prints `initial: 0` / `after set: 100`, decompiled-under-Node prints `initial: undefined` / `after set: undefined`, while `after inc: 1 2` / `final: 101` match on both — the private-field/accessor *storage* is evidently correct (the final value is right) but some *read* path (a getter or field-access site) returns `undefined` instead of the stored value; needs its own investigation, no root cause identified yet. **20** is unresolved either way: VM and decompiled candidate both crash at the same `for (const v of iterable)` call site over a custom (non-array) `Symbol.iterator` object, with the values printed before it matching exactly, but with *different* native error text (Hermes: `Can't apply() to non-callable`; V8/Node: `CreateListFromArrayLike called on non-object`) — this could be the same already-documented `tools/hermes-vm/v99` iterator-machinery limitation as `reference-policy.ts`'s `VM_LIMITATIONS` table (currently scoped to `for await`/async-iterator constructs only; this fixture's `for-of` is synchronous, so if confirmed it would broaden that table), or it could be a genuine decompiler bug in for-of/custom-iterator lowering that happens to also crash. Do not treat either verdict as settled without further work. | no regression test yet; needs its own investigation pass before triage | M5 (unassigned) |
| 2026-08-31 | adversarial/06-closure-loop-var-vs-let (v94.hbc) | decompiler closure capture | for-loop `let` bindings should create fresh variables per iteration, allowing closures to capture different values (0,1,2), but decompiled code treats it like `var`, causing all closures to capture the final value (3,3,3) | regression test in place (adversarial fixture 06) | M5 |
| 2026-08-31 | adversarial/02-proxy-trap-counting (v94.hbc) | decompiler Proxy handler | `'x' in proxy` should invoke the `has` trap (count=1), but decompiled code doesn't call it (count=0) | regression test in place (adversarial fixture 02) | M5 |
| 2026-08-31 | adversarial/28-this-binding-extracted (v94.hbc) | decompiler this binding | method extracted and called without context should error or get undefined-this behavior; decompiled code shows undefined-value instead of expected error | regression test in place (adversarial fixture 28) | M5 |
| 2026-08-31 | adversarial/29-var-hoisting-redeclaration (v94.hbc) | decompiler hoisting | var redeclaration after function with same name — decompiled output diverges from expected | regression test in place (adversarial fixture 29) | M5 |
| 2026-08-31 | adversarial/30-tdz-shadowing (v94.hbc) | decompiler TDZ | shadowing with TDZ (accessing `inner` before declaration in a scope where `inner` exists in outer scope) should throw ReferenceError, decompiled code diverges | regression test in place (adversarial fixture 30) | M5 |
| 2026-08-31 | adversarial/36-optional-chaining-sideeffect (v94.hbc) | decompiler optional chaining | optional chaining with side effects throws during decompiled execution; check source for error details | regression test in place (adversarial fixture 36) | M5 |
| 2026-08-30 | T14 adversarial pass-ladder testing | `src/passes/global-access/{match,check}.ts` | `isProvenGlobal` proves a register global by scanning every write across the *whole function* and requiring exactly one write literally valued `globalThis`, chronologically first — it never checks whether a *later* write in the same statement list (after the guarded read) can re-execute before that read on a repeat visit. When the guard+read+a later clobbering write all live in one loop body, the fold is accepted anyway: `check()` re-derives the identical unsound proof, so it is no independent guard either. Confirmed by directly executing both the pre- and post-rewrite forms under Node (no D14 ambiguity — no `let`-in-loop, no TDZ, no `arguments`, plain property reads): the un-rewritten program correctly prints `GLOBAL, OTHER` over two iterations; the rewritten one wrongly prints `GLOBAL, GLOBAL`, reading the real global on every iteration instead of the clobbered local from the 2nd iteration on. Not VM-dependent — reproducible with plain `node -e`. | regression test in place, pinned to the current (wrong) behaviour: `tests/gate/passes/adversarial-ladder.test.ts` ("global-access BUG (pinned)"); flip the assertions to the correct refusal in the same commit that fixes `isProvenGlobal`/`classifySite` to account for repeat-visit (loop-body) re-entry | M5 pass-ladder lane |
| 2026-08-30 | T14 adversarial pass-ladder testing | `src/structure/structure.ts` (`ramsey`'s `maxDepth` recursion guard, default 1500, `src/structure/index.ts`) | The guard exists so a pathologically deep function refuses cleanly (`E_TOO_COMPLEX`) instead of crashing, but on a cold process (the realistic case — every `hbc2js` CLI invocation starts fresh) a flat chain of basic blocks with no actual nesting (e.g. a very large switch, or thousands of sequential jump-separated statements) overflows Node's real V8 call stack with an uncaught `RangeError: Maximum call stack size exceeded` well before the counter reaches 1500: measured boundary ≈1030 blocks still structure cleanly, ≈1075+ crash — over 400 short of the documented limit. The guard's per-recursion-unit stack cost was evidently sized without a cold-process, un-JIT-warmed measurement: the same chain lengths structure fine *later in the same process* (after other recursions have run), which is why this must be measured in a fresh child process, not merely in-process. Not VM-dependent, not D14-related — a pure engineering/robustness fact about `ramsey`'s recursion. | regression test in place, pinned to the current (wrong) behaviour: `tests/gate/passes/adversarial-ladder.test.ts` ("structure limits BUG (pinned)"), using an isolated child-process probe (`tests/gate/passes/fixtures/chain-structure-probe.mts`) so the measurement is not warmup-dependent; flip the assertion once `maxDepth`'s default (or the guard's stack-cost-per-depth-unit) is recalibrated for a cold process. Related but distinct from the already-documented `lowerTree` stack overflow on a real Shopify bundle noted in `docs/STATUS.md` (worked around there with `--stack-size=6000`) — same class of problem, different function, not yet cross-checked whether one fix covers both. | M5 pass-ladder lane |
