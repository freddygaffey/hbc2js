# Known bugs ledger

A worked backlog, not an append-only log: **rows are never deleted**, only
reclassified. Every row has a `status` (exactly one of `open` / `fixed` /
`wontfix` / `d14-legit` / `duplicate`) and a `cluster` (`semantics` /
`real-app` / `deps` / `passes` / `emit-shape` / `harness` / `toolchain` /
`metrics`). Every **open** row must carry a `cluster`, a one-line `verdict`
(what would prove it fixed — a fixture, a test, or a measurement — and which
`docs/QUEUE.md` item or lane owns it), and an `owner`. Every **resolved**
row (status ≠ open) moves to the "Resolved" table below with a `resolution`
citing a commit hash or a reason (D14-legit divergence, duplicate of another
row, or a deliberate design choice). New bugs are appended to Open with a
`cluster` and `verdict` from the start — no more thin "why no test yet"
placeholders. Triage history: docs/QUEUE.md item 4, 2026-09-01.

## Open — 31 rows

| date | source | component | description | why no test yet | owner | status | cluster | verdict |
|---|---|---|---|---|---|---|---|---|
| 2026-09-05 | rung `super-call` landing (agent/super-call), refusal R-SC7 | `src/passes/super-call` | `super-call` only rebuilds a super call whose bytecode argument list is a plain register range, which the emitter prints as an array literal. A `super(...args)` spread, and any shape where the arguments reach `Reflect.construct` through something other than an array literal, is refused (R-SC7) and keeps the raw `Reflect.construct(Object.getPrototypeOf(...), ..., new.target)` text. Not yet seen in a committed fixture; found by reading the writer, not by a failing case. | No fixture exhibits it yet: hermesc lowers a spread super call through `Apply`/`applyArguments` shapes that hit R-SC6 first, so a fixture that isolates R-SC7 has not been built. A hand-built unit test pins the refusal itself (`tests/gate/passes/super-call.test.ts`, `foldSuperBody` on a synthetic spread body). | M5 ladder (class rungs) | open | passes | prove fixed: a construct fixture whose derived constructor calls `super(...args)` decompiles to a real `super(...args)`, T2-equivalent at v98 and v99 |
| 2026-09-01 | E2E tier 1 -- bucket `diff:GetOwnPrivateBySym/GetByVal`, 151 fns react-navigation (v98); reopened 2026-09-05 (T2 equivalence, agent/private-fields); fixture half fixed 2026-09-05 (agent/ctor-this) | `src/passes/private-fields` + `src/passes/ctor-this` (both landed; the fold now fires on fixture 35) | v98/v99 private-field access is emitted as a computed member access on a `Symbol`, which recompiles to `GetByVal`/`PutByValStrict` instead of `GetOwnPrivateBySym`/`PutOwnPrivateBySym`. `src/passes/private-fields` folds that back into real `#name` syntax but could only ever fire when the `AddOwnPrivateBySym` install targets literal `this` -- a native `#name = v` write only succeeds on the object that received the class's brand during ITS OWN `[[Construct]]` -- and every decompiled constructor instead built and returned a separate `Object.create(new.target.prototype)` stand-in (T2 diverged at record 0 when the first landing folded anyway). **2026-09-05, rung `ctor-this` (docs/specs/passes/26-ctor-this.md, row R12)**: in a BASE class that stand-in is provably the object `[[Construct]]` bound to `this`, so it is deleted and `this` substituted; `private-fields`' guard is unchanged and fixture 35 at v98/v99 now decompiles to real `#balance`/`#history` declarations with `this.#balance` reads/writes, PASS under T2 at v99. What is still open is the row's own prove-fixed clause: the private *method* `#record` and the `PrivateIsIn` brand check have no recognised shape and stay symbol-keyed, the seeded `Object.assign(Object.create(...), {...})` constructor form (32-class-basic) is refused (R-CT2), derived constructors are refused (R-CT1), and the react-navigation bucket has not been re-measured -- that bundle's `.hbc` is not committed (fetch.sh only), so `tools/e2e/roundtrip-corpus.ts` could not be run in the landing worktree MEASURED (orchestrator, tip f4e5cb6 with ctor-this + private-fields landed): `node tools/e2e/roundtrip-corpus.ts --only react-navigation-example-0.85.3 --passes on` still reports `diff:GetOwnPrivateBySym/GetByVal` at 178 functions (rank 12, e.g. module_122 fn#2646), IDENTICAL 6183 (42.83%) -- unchanged, so neither rung fires on this bundle: its constructors are not the base-class `new.target.prototype` stand-in shape ctor-this recognises (derived classes, R-CT0, or the seeded `Object.assign` shape, R-CT2). Next step: dump three of the 178 and classify by ctor-this refusal code. CLASSIFIED 2026-09-05 (agent/ctor-this-2, `tools/passes/ctor-this-refusals.ts`, committed): of the bundle's 448 recovered class constructors 304 are R-CT1 (derived), 119 R-CT0, 21 R-CT2 (seeded `Object.assign(Object.create(new.target.prototype), {...})`), 4 R-CT3, 0 folded; restricted to the 22 modules that own one of the bucket's functions there are 22 class constructors -- 11 R-CT1, 9 R-CT0, 2 R-CT3 and ZERO R-CT2. The seeded shape is now folded (`Object.assign(this, {...})`, spec 26 sections 2/4/6, fixtures 32-class-basic and the new 76-class-fields-private), 21 constructors across 12 module files -- and the bucket did NOT move: `diff:GetOwnPrivateBySym/GetByVal` is 177 functions and IDENTICAL 6184 (42.83%) both before and after, because by the classification above not one of the classes in the bucket's own modules is the seeded shape. The bucket is dominated by DERIVED constructors (R-CT1), which this rung cannot reach: a derived `this` is what `super()` returns, and the decompiled text has no `super()` in it -- it has `Reflect.construct(Object.getPrototypeOf(<binding>), [...], new.target)` plus Hermes's TDZ marker and double-super guard. Folding those needs a separate `super`-rebuilding rung, not a 250-line extension of `ctor-this` (spec 26 section 10 sets out what it would have to prove). The R-CT0 residue in those modules is mostly a constructor whose first executable statement is a plain register copy (`store:ident`, 6 of 9). | re-measured 2026-09-05 (bucket unchanged at 177, reason classified above). Remaining work is a `super`-rebuilding rung for derived constructors (the 11 R-CT1 classes in the bucket's modules), then the R-CT0 register-copy shape (6 more), then the private-side residue (`#record`, `PrivateIsIn`, and the env-slot/register-copied `Symbol("#name")` access shape pinned by fixture 76-class-fields-private) | M5 ladder (class rungs) | open | emit-shape | prove fixed: the e2e `diff:GetOwnPrivateBySym/GetByVal` bucket actually shrinks on react-navigation with `ctor-this` + `private-fields` enabled. The fixture half is already proven (`tests/gate/passes/private-fields.test.ts` asserts real `#` syntax at v98/v99; `tests/gate/passes/ctor-this.test.ts` owns the constructor half; T2 equivalence covers behaviour) MEASURED (orchestrator, tip 710b697, after super-forward folded R-SC6 into the explicit forward, FOLD 52 of 448): IDENTICAL 6215 of 14437 (43.05 percent), bucket still 177 -- the 13 constructors that fold are not in the bucket's modules; the 136 R-SC9 func constructors are the next measurement (agent/super-forward-2). |
| 2026-09-05 | spec 27 section 9.5 (agent/iife-escape), premise A-PROTO | `src/emit/iife-escape.ts` | The escape analysis that lets `src/emit/iife-group.ts` move a store into a freshly allocated array/object literal proves the base is fresh and unescaped, so no setter, getter or proxy INSTALLED ON THE OBJECT can observe the reordering. It does not prove the intrinsic prototype chain is clean: `arr[0] = v` on a fresh array still consults `Array.prototype` for an accessor named `0`, and a bundle that installed one would see the two statements swap. Every other premise of section 9 (F1-F5, 9.2) is checked syntactically and refuses conservatively. Nothing on react-navigation-example-0.85.3 depends on this today (0 groups plan, section 9.6), so the exposure is currently the construct fixture only. | Not a failing case, a documented premise: no bundle in the corpus installs an accessor on `Array.prototype`/`Object.prototype` for a numeric or IIFE-written key, so no test can go red. A test would have to be a hand-built VM case that patches the prototype and shows the reordering changing behaviour, which would pin a divergence we deliberately accept. | M4/M5 emit owner | open | emit-shape | prove fixed: either restrict the rule to bases with a null prototype (`Object.create(null)`-shaped allocations) and re-measure, or show by a whole-bundle scan that no accessor is ever installed on `Array.prototype`/`Object.prototype` for a key any wrapped store writes. Watch `tests/gate/emit/iife-reconstruct.test.ts` and the T2 equivalence verdict for `79-interleaved-envs`. |
| 2026-09-05 | E2E tier 1 after the env-slot-order fix (b4e6270), react-navigation-example-0.85.3 v98 passes-on | src/emit (bisected by agent/loadenv-bisect into two sub-clusters, neither fixed yet) | Residual `diff:LoadFromEnvironment(imm)` bucket, re-measured at 968 of 14437 functions on this worktree (grew again from the 862 this row was filed with, same "matches further before diverging" migration effect noted below - no code changed this measurement, the corpus just moved between commits). Bisected the 968 by whether the function's OWN module also contains an unrelated DECOMPILE-STUB: (A) 200/968 (~21%) do - the stub (E_UNBOUND_IDENT etc, a different bug owned by agent/unbound-ratchet's area) stops referencing its own captured slot, so the recompiled environment's live slot set shrinks and every OTHER slot number in that module's environment shifts as a side effect; this is not an env-order bug in its own right, just noise inherited from the stub bug. (B) 768/968 (~79%) are clean (no stub in the module). Sampled 67 of the clean 768 (all functions of 3 to 30 instructions, one full module): every one is the SAME opcode and SAME environment register as the original, only the slot IMMEDIATE differs - zero shape differences (no missing/extra load, no different opcode) in the sample. Understood one real case in full: module 681/fn#683 in react-navigation-example.hbc (a lazy re-export barrel; 11 named getters each read one slot of a shared 11-slot environment "e881" that coexists with 12 SIBLING environments e882..e893 in the SAME function). Our decompiled source declares and uses `_e881_0` through `_e881_10` in the same ascending order as the original bytecode, yet hermesc -O's recompile gives the getters a uniform +2 slot offset (original slots 0..6 recompile to slots 2..8) - so this is not a declaration/use-order bug like the 2026-09-01 row's fix, it is hermesc's OWN environment-slot allocator making a different call on the recompiled source. CAUSE FOUND AND PARTLY FIXED (d4011d8, agent/sibling-env). The +2 on module 681 / fn#683 is not a sibling-environment effect at all: it is `--split`. A function that captures nothing and is created from more than one function stays an orphan (src/emit/index.ts F24-5) and is emitted at MODULE level, which is harmless in the unsplit `decompile()` path because module level is a different scope; but a split module file that names such an orphan has to carry its declaration, and `src/split/index.ts` PREPENDED it to the factory body, ahead of the factory's own `let _e<env>_<slot>` prologue. Hermes numbers a scope's environment slots in TEXTUAL declaration order - hoisting moves a function declaration's closure creation to the top of the scope, not its slot (probed on hermesc v98: two captured function declarations either side of a `let a0, a1, a2` give the same CreateClosure/StoreToEnvironment sequence, but slots 0..2 go to the `let` bindings only when the declarations come after them). Module 681 pulls in exactly two such orphans, `_fn13951` and `_fn13952` (a non-capturing isNaN predicate and a non-capturing 4-element-array predicate, each created from four different functions), so every one of the eleven getters recompiled two slots high. Confirmed directly: reordering those two declarations by hand in the split module_681.js restores the getters to slots 0..10, matching the original. Measured on react-navigation-example-0.85.3 v98 passes-on, same worktree, before -> after: IDENTICAL 6105 (42.29 percent) -> 6183 (42.83 percent) of 14437, `diff:LoadFromEnvironment(imm)` 1030 -> 925, 78 functions newly IDENTICAL and 0 newly DIFFERENT; module 681 alone went 9 -> 20 IDENTICAL of 44. That is about 11 percent of the bucket, so the row stays OPEN for the remaining 925: module 681's own factory fn#683 still differs, in a different bucket (`diff:CreateFunctionEnvironment(imm)`), because our source declares the 13 original sibling environments e881..e893 as one flat `let` list in one scope and hermesc rebuilds them as a single environment. SIBLING HALF NOW REPRODUCED AND UNDERSTOOD (agent/sibling-env-2, 2026-09-05). The construct is an INLINED IIFE: hermesc -O splices the callee into the caller but keeps the callee's own environment, so a caller that inlines several of them ends up with several environments side by side - which is exactly module 681 / fn#683 (`CreateFunctionEnvironment r4, 11` plus twelve sibling `CreateEnvironment`/`CreateFunctionEnvironment`). Minimised to three IIFEs capturing 2/1/3 values: the original is `CreateFunctionEnvironment(2)/(1)/(3)` with readers loading slots (0,1)/(0)/(0,1,2), and our flat `let _e<env>_<slot>` prologue recompiles it to a single `CreateFunctionEnvironment(6)` with readers on (0,1)/(2)/(3,4,5). The block-scope fix proposed for this row CANNOT work and neither could the two earlier synthetic repros: hermesc -O merges sibling block scopes into the enclosing function environment, so three sibling `{ let a; ... }` blocks compile to ONE environment in the original and in the recompile alike, and hand-rewriting fixture 75's decompiled output into block scopes recompiles byte-identically to the flat form. Hand-rewriting the SAME statement ranges as `(function () { ... })();` reproduces the original exactly. See docs/PUSHBACK.md P-41 and docs/reports/2026-09-05-sibling-envs.md. FIXED (sibling half) 2026-09-05 by agent/iife-reconstruct: new emit-side placement step `src/emit/iife-reconstruct.ts` (spec docs/specs/passes/27-iife-reconstruct.md, LADDER row R13) wraps each sibling environment's contiguous statement range back into `(function () { ... })();` so hermesc rebuilds the same CreateFunctionEnvironment/slot layout. Guards and their rnav counts are the spec's refusal table; the decisive one, found by measurement, is that a hoisted `function _fn<n>` this body only HOSTS (an orphan or a per-creation-context copy, src/emit/placement.ts) must NOT travel into the wrapper - a first version without that guard moved 227 functions out of reach of the siblings that name them (E_UNBOUND_IDENT) and cost 1.35 percentage points. Measured `node tools/e2e/roundtrip-corpus.ts --only react-navigation-example-0.85.3 --passes on` on this worktree, before -> after: IDENTICAL 6184 (42.83 percent) -> 6214 (43.04 percent) of 14437, `diff:LoadFromEnvironment(imm)` 867 -> 804, `diff:CreateFunctionEnvironment(imm)` 619 -> 616, 31 functions newly IDENTICAL and 1 newly DIFFERENT (bucket `diff:LoadConst/GetGlobalObject`, not investigated - register-allocation noise in a function whose range moved). The row STAYS OPEN for the remaining 804: the largest refusal is `overlapping statement ranges` (757 environments, module 681 / fn#683 among them) - the emitter places each environment's stores where the bytecode had them, so thirteen environments interleave instead of forming thirteen contiguous runs; grouping them needs a reordering argument of its own and was not attempted. GROUPING MEASURED AND MOSTLY REFUTED 2026-09-05 by agent/iife-group (spec 27 section 7, new classifier `tools/passes/iife-overlap.ts`): of the 757 environments in 339 overlapping groups on react-navigation-example-0.85.3, 622 (82 percent) are groups in which ONE statement names slots of two environments at once, which no reordering can separate and which section 4 `environment read outside the range` would refuse anyway; 204 involve a strictly nested range but fall in the same classes; and every one of the remaining 135 has a swap that cannot be proved safe, most often the property store an inlined IIFE ends with (`out.f = closure`, `arr[0] = closure`), whose proof needs escape analysis. New `src/emit/iife-group.ts` performs the regrouping where it IS provable (stable partition of the region into one block per environment, allowed only when every swapped pair is an inert or identifier/literal store with disjoint footprints, reverted when it buys no wrapper), so 0 groups plan on rnav and the emitted output is byte-identical: `diff:LoadFromEnvironment(imm)` stays at 804 and IDENTICAL at 6214 of 14437. Repro pinned by construct fixture `tests/fixtures/constructs/79-interleaved-envs` (v98/v99 interleave, v84/v94/v96 do not inline) plus four cases in `tests/gate/emit/iife-reconstruct.test.ts`. The row stays OPEN for the 804: the next step is the escape-analysis argument for a store into a freshly allocated object, not more reordering. ESCAPE ARGUMENT MADE AND MEASURED 2026-09-05 by agent/iife-escape (spec 27 section 9, new `src/emit/iife-escape.ts`): a member store now moves when its base is provably a fresh allocation of this function (array/object literal or the `NewArray` opcode's rendering, marked `fromNewArray`), never reassigned, never passed to a call, stored, returned or captured by a closure in the region, with a literal key and a pure value (refusal codes E_NOT_FRESH / E_REASSIGNED / E_ESCAPES_CALL / E_ESCAPES_STORE / E_ESCAPES_CLOSURE / E_KEY_NOT_LITERAL / E_VALUE_NOT_PURE), plus a filler-repair step that lets a statement naming no environment join either block. Fixture 79's two and three interleaved environments are now wrapped at v98/v99. On rnav the answer is again NO for real code: the object-literal blocked class (4 groups, 19 environments) disappears and several groups move past their first blocker, but 0 of the 339 groups plan, because the bases are not fresh (E_NOT_FRESH 239 bases, E_ESCAPES_CLOSURE 88, E_ESCAPES_STORE 10, E_ESCAPES_CALL 2) -- a real inlined IIFE stores its closure into an object the function was GIVEN. Re-measured on one machine at a217e9c and after: IDENTICAL 6215 (43.05 percent) of 14437, `diff:LoadFromEnvironment(imm)` 804, `diff:CreateFunctionEnvironment(imm)` 621, identical both times (section 6's 6214/616 were another worktree's drift). The row stays OPEN for the 804; the next lead is an argument about an object the function RECEIVED, which needs a whole-module notion of who else can see it. | Fixed half now has a test: construct fixture `tests/fixtures/constructs/74-sibling-env-slots` (a Metro-shaped barrel with five captured exports read through five getters plus two non-capturing predicates shared between two nested functions, i.e. module 681's shape) with `tests/gate/split/orphan-decl-order.test.ts` asserting, at v96/v98/v99, that the split module file emits the factory's env-slot prologue before any pulled-in declaration and that recompiling it reproduces the original getter slot immediates - red at v98/v99 before d4011d8, green after. Still untested is the REMAINDER of the bucket: the sibling-environment consolidation (13 original environments becoming one on recompile) has no minimised repro yet, and the stub-cascade sub-cluster (A) is still downstream of the separately tracked stub bug. Two synthetic repro attempts this pass (multiple `{ let x; function get(){return x} }` blocks in one function; a three-export lazy-require barrel sharing one environment) did NOT reproduce any slot shift at all - hermesc kept the original numbering both times, so the trigger needs more than one environment in play and neither repro had sibling environments the way module 681/fn#683 does. A fixture that does not fail is not worth shipping (CLAUDE.md - every fixture needs a test that fails before a fix); building a case with multiple coexisting sibling environments is the next step and was not reached this pass. Cluster A (stub-cascade) is downstream of a different, separately tracked bug, not a new one. Reproducible with `node tools/e2e/roundtrip-corpus.ts --only react-navigation-example-0.85.3 --passes on`; the stub/clean split and per-function opcode classification are ad hoc scripts (not committed - see agent/loadenv-bisect's report) built on `src/parse`, `src/disasm/decode.ts` and `src/harness/roundtrip.ts`'s `normaliseFunction`. The sibling half now HAS a test as well: construct fixture `tests/fixtures/constructs/75-sibling-envs` (three IIFEs in one function, capturing 2/1/3 values, each with one reader closure) plus `tests/gate/emit/sibling-env-slots.test.ts`, which decompiles it, recompiles with hermesc v98/v99 and compares the sizes of the environments created in the owning function and the slot immediates every reader loads. RED today at both versions (2 of its 4 tests fail; the other two guard that the fixture still compiles to three sibling environments, so it cannot silently stop reproducing). v84/v94/v96 are out of scope - they do not inline these IIFEs, so no siblings exist there. The FIX now has tests too: `tests/gate/emit/sibling-env-slots.test.ts`'s two round-trip cases are green at v98/v99 with their skip deleted, and `tests/gate/emit/iife-reconstruct.test.ts` asserts the structural shape (three wrappers at v98/v99, none at v84/v94/v96) plus one case per guard. What was untested when this row was filed: the sole source form that round-trips a sibling environment is the IIFE, and emitting IIFEs is a readability trade plus safety guards (no return/break/continue/this/arguments in the range, scratch declarations that outlive the range must be hoisted - fixture 75's own `let r0 = undefined;` already crosses two ranges - and hoisted closure declarations must move into the range), which is a decision rather than an implementation detail. | M4/M5 emit owner | open | emit-shape | prove fixed: the repro is pinned (fixture 75-sibling-envs, RED at v98/v99). Remaining work is a DECISION first - whether the emitter may reconstruct an inlined IIFE as `(function () { ... })();` (default, or an opt-in mode used only by the round-trip harness) - and then the transform with its guards. Fixed when `tests/gate/emit/sibling-env-slots.test.ts` is green at v98 and v99 and `diff:LoadFromEnvironment(imm)` on react-navigation-example-0.85.3 v98 passes-on falls by per-function verdict from the baseline re-measured on agent/sibling-env-2 with no code change (`node tools/e2e/roundtrip-corpus.ts --only react-navigation-example-0.85.3 --passes on`): IDENTICAL 6178 of 14437 (42.79 percent), `diff:LoadFromEnvironment(imm)` 863 and `diff:CreateFunctionEnvironment(imm)` 619 - the LoadFromEnvironment figure drifted down from the 925 measured at d4011d8 with nothing changed in between, the same corpus-migration effect this row already notes, so compare against 863. Keeping the stub-cascade sub-cluster separate - it tracks the other bug. |
| 2026-09-05 | spec 24 (`class-recover`) writer form, PUSHBACK P-37 | src/passes/class-recover/rewrite.ts | Spec 24 section 3.3 says the writer replaces the group with one `classdecl` statement. It cannot: the constructor value is held in a register (`r6 = _fn4;`) that later statements read (`_e0_1 = r6;`), so a declaration would drop the binding every downstream use needs. The writer therefore substitutes a `class` **expression** into the existing assignment, keeping the register bound. The `classdecl` node exists in the AST and prints, but no rung emits one yet. | Covered by tests: the acceptance file asserts on `class Name` / `class Name extends X` text, which both forms satisfy; nothing asserts a `classdecl` is produced. | orchestrator (spec 24 section 3.3 wording) | open | passes | Spec 24 section 3.3 is reworded to the expression form, or a rung is shown that can safely emit `classdecl`. |
| 2026-09-05 | seam-fix golden verification (deb-operator) | `tests/golden/constructs/67-class-static-and-new/` (parse + disasm) | Fixture 67-class-static-and-new v98/v99 has NO committed parse/disasm goldens → `tests/gate/parse/module.test.ts` is RED on it. Pre-existing (never committed with the fixture), unrelated to the seam fix; surfaced while verifying the 66 golden regen. | needs the correct golden regenerated + committed (Fred-approved batch per rule 9), after confirming 67's parse output is actually correct (not masking a real regression) | fixtures lane / whoever landed fixture 67 | open | passes | prove fixed: 67 v98/v99 have committed goldens and module.test.ts is green, with the parse output confirmed correct |
| 2026-09-05 | spec 25 (`yield-recovery`) landing, refusal R-Y5 `cyclic-dispatch` | src/passes/yield-recovery/recover.ts | A generator whose suspend graph has a back edge is refused, not recovered: the arm would have to be inlined at a suspend site sitting outside the labels/regions the arm lives in, which is a re-structuring job (spec 25 section 1.4, F25-4 `restructureSegments`), not a matcher's. Refused today: `23-generator-basic`'s `counter`, `25-generator-delegation`'s `outer` and `delegatesToArray` (also R-Y6), `26-infinite-generator-take`, and 6 of the 7 sites in the committed rn-template bundle. | Covered by tests: `tests/gate/passes/yield-recovery.test.ts` asserts each of those groups stays a shim. This row tracks the missing capability, not an untested behaviour. | orchestrator (queue `yield-loop`, spec 25 section 6.2 -- needs Fred's ruling on scope) | open | passes | The `yield-loop` rung (hard, sharing F25-4 with `gen-lowered`) recovers `26-infinite-generator-take` and `23`'s `counter` into `while (true) { yield ... }` with the equivalence gate still PASS. |
| 2026-09-05 | spec 25 (`yield-recovery`) landing, refusal R-Y4 `forced-return-body` | src/passes/yield-recovery/recover.ts | `24-generator-return-throw`'s `g1` is refused because Hermes duplicates the source `finally` body into every suspend point's forced-return tail (5 copies at v84/94/96). A native `function*` runs that body from the `try`/`finally` statement on `.return()`, but `finally-dedup` (catalogue row 12, hard) has not run, there is no `finalizer` node, and deleting a non-empty forced-return arm would silently drop the `finally`. | Covered: `tests/gate/passes/yield-recovery.test.ts` asserts all 5 copies survive verbatim. The gap is the missing `finally-dedup` rung. | orchestrator (batch ordering, spec 25 section 6.6) | open | passes | After `finally-dedup` lands, `24-generator-return-throw`'s `g1` recovers into a `function*` whose `finally` runs once, and the fixture keeps its PASS verdict. |
| 2026-09-05 | spec 25 landing (framework item F25-2 not implemented) | src/passes/yield-recovery/recover.ts, src/emit/lower.ts | Spec 25 section 2 asks for F25-2: a per-opcode `Origin` variant recording the generator group (`CreateGeneratorClosure`/`SaveGenerator`/`ResumeGenerator`) so the suspend-to-resume map is read from provenance rather than from printed integers. It was not built. The rung instead anchors on `func.sameFrame` (set by `src/emit/function.ts` for the opcode generator body and by nothing else) plus the exact `__hbc_makeGenerator(F, this, arguments)` shim shape, and verifies the 1:1 state map itself (R-Y3). That is real provenance, not a shape heuristic, but a `.obf` build that renamed the shim helper would refuse rather than recover. | No fixture defeats the current anchor: every `.min`/`.obf` variant keeps the emitted helper names, which the decompiler writes itself. A test would need a synthetic AST, which belongs with F25-2's own spec. | orchestrator (queue F25-2 with `gen-lowered`, which needs the same map) | open | passes | F25-2 lands and `recover()` reads the suspend/resume map from `Origin` instead of from the `switch` arms, with the existing acceptance tests unchanged. |
| 2026-09-05 | spec 27 L3 (JS<->native linkage join) implementation | src/artifact/semantic-walk.ts (string-use roles) and src/artifact/points-to.ts (resolved call edges), consumed by src/native/seams.ts | The JS-side artifact tables carry no receiver for a host-object member chain, so native/seams.jsonl can only ever cite string-strength evidence. semantic-walk.ts records the SAME property-get role for the X of NativeModules.X (a module) and the m of X.m (a method of that module), and calls-resolved.jsonl resolves require(N) module exports only, never a host receiver. Two consequences, both honest but weaker than the spec 27 L3 vocabulary allows: every seam row is resolved:"string-only" (points-to and by-name are unreachable today), and in the NativeModules channel a member string that matches no native module and is not consumed as a linked module method is reported as a js-only seam - so an unresolved boundary is never dropped, at the cost of over-reporting method names as candidate modules (the 66-native-module-seams fixture emits seam:foo beside seam:Missing for NativeModules.Missing.foo()). A js-only row is an unresolved boundary and never a claimed link, so no seam can be fabricated by this. | tests/gate/native/seams.test.ts pins TODAY behaviour honestly (it asserts resolved==="string-only" on every linked row rather than a strength we cannot yet prove); the fix is a JS-SIDE signal change (a distinct host-member string-use role, or a receiver column on a resolved call edge) plus its own artifact-format landing, which is out of L3 scope - L3 never invents a signal | native lane (spec 27 L3 follow-up, unassigned) | open | real-app | prove fixed: string-uses.jsonl (or a resolved-call row) distinguishes a host-global member from a deeper member, seams.ts consumes it, a linked seam row carries resolved:"points-to" or "by-name", and the NativeModules channel emits no js-only row for a plain method name (seam:foo disappears from the 66-native-module-seams join while seam:Missing stays) |
| 2026-09-05 | fallout of the v99 setFunctionName fix (fa23564), found while regenerating the disasm goldens | `src/parse/layout.ts` (builtin-table selection is tied 1:1 to the opcode table) | Two different v99 compilers produce bytecode that probes identically to `hbc99-mar2026` but number the builtins differently: `tools/hermesc/v99/hermesc` has `HermesBuiltin.setFunctionName` at 55, the older compiler that produced the preserved historical `tests/fixtures/hermes-dec-sample/v99.hbc` does not. The bytecode carries no signal to tell them apart, so the patched table now mis-names that one historical file's builtin 57 as `functionPrototypeCall` where it is really `spawnAsync`. Nothing regressed behaviourally -- that fixture still PASSes syntax+trace in the gate tier -- but its disasm golden would change, and a real-world v99 bundle built by a pre-setFunctionName Hermes would decompile its async functions wrongly. The narrow discriminator that would fix it: a `GetBuiltinClosure` whose operand is 57 is only legal under the OLD numbering (57 is `functionPrototypeCall` under the new one, a private builtin Hermes never fetches as a closure), so a per-module builtin-table probe could decide it -- but `BuiltinTableId` is currently an alias of `OpcodeTableId` and the choice is made in the layout probe before any instruction is decoded. | a test would have to assert the mis-naming as current behaviour, which is the wrong shape; the meaningful test is the one that ships with the probe, namely that `hermes-dec-sample/v99.hbc` names builtin 57 `spawnAsync` while `64-computed-method-names/v99.hbc` names 55 `setFunctionName`, in the same run | orchestrator / parser lane (unassigned) | open | toolchain | prove fixed: both files above resolve their own builtin numbering correctly in one run with no forced flag, and the 8 v99 disasm goldens listed in the 2026-09-05 AGENT-LOG entry regenerate to the right names |
| 2026-09-05 | QUEUE #3 points-to export-side widening (agent/pt-exports), splitting off the two causes the babel-prologue fix deliberately did NOT widen to | `src/artifact/points-to.ts` (export-side resolution: interop wrappers and helper-assembled exports) | Two of the three known causes of a proven-receiver-but-unresolved call site remain, and they are what rn-template's whole 152-of-1,086 residue is made of: an `_interopRequireDefault(require(d[N]))` wrapper consumes the module value in a call, so the value is dropped; and a `module.exports` object assembled by a helper function (PropTypes' `oneOf`/`shape`/`arrayOf`, React's hook re-exports, `polyfillGlobal`) rather than in the factory itself. Every one is a MISSING edge, never a wrong one. | the refusals are deliberate and asserted as behaviour (`tests/gate/artifact/points-to.test.ts` pins the unprovable-receiver negative and the conditional-second-write negative); a regression test becomes meaningful only once a specific shape is chosen to be RESOLVED, at which point it ships as a construct fixture alongside the widening | Stage-2 xref lane (unassigned) | open | metrics | prove fixed: a construct fixture compiling an `_interopRequireDefault(require(d[N]))` wrapper resolves the wrapped module's `default` soundly, and the rn-template unresolved-site ratio drops below 152 of 1,086 with no new edge failing the soundness invariants in `tests/gate/artifact/points-to.test.ts` |
| 2026-09-04 | E_UNBOUND_IDENT hunt on react-navigation-example-0.85.3 (whole-file `--lenient-env`) | `src/emit/placement.ts` + `src/emit/index.ts` function placement, `src/cfg/env-graph.ts` closure-env resolution | A function whose lexical parent cannot be resolved is emitted at MODULE top level, which is OUTSIDE the global function, so nothing any function body declares is in scope there and every `_e<env>_<slot>` and `_fn<n>` name its body reads is unbound. **PLACEMENT HALF FIXED (2026-09-04, commits 94457f8 + b9e8ef5)**: `src/emit/placement.ts`'s `resolveOrphanHosts` costs every candidate host for an orphan (module level, and every ancestor-or-self of a function declaring an env its subtree uses) by the number of names that host leaves unbound — env-slot reads it cannot see, plus `_fn<n>` references at `Create*Closure` sites it does not contain — and takes the cheapest, deepest first; module level is always a candidate, so a host is chosen only when it is strictly better. Measured on react-navigation-example on deb, whole-file `--lenient-env`: passes on 186 isolated / 541 unbound names -> 103 / 158, `--passes=none` 176 / 502 -> 106 / 160, 111 orphans hosted; `_e652_0` (`_fn13838`…`_fn13843`) is bound. **STILL OPEN — the residual 158.** **CHARACTERISED (2026-09-05, `docs/reports/2026-09-05-ambiguous-closure-env.md`)**: all 178 ambiguous functions bucketed on this bundle — 109 have 2 creation sites and the rest up to 30; 160 of the 178 differ only in the *directly captured* environment (chains node-for-node identical above the leaf) and 18 have chains of different length; 25 have every site inside one creating function (an inlined closure-making helper), 153 are created from two or more near-identical Metro module factories whose one function body Hermes deduplicated. The hoped-for cheap class — same shape, body would emit identically either way, so pick one and stop reporting it — is 4 of 178 once the test is made sound (whole lexical subtree, both chains resolved to a known root), and landing it measured 103 isolated / 158 unbound -> 106 / 161 because a joined function stops being an orphan and loses `resolveOrphanHosts`'s cost-chosen host, so it was written up rather than landed. `src/cfg/env-graph.ts` now exports `closureCreationSites` (site -> environment, the evidence the fix needs, tested as T6 in `tests/gate/cfg/env-no-capture.test.ts`) and the per-creation-context duplication design with its work list is in the report. Two root causes remain, both in `src/cfg/env-graph.ts`, not in placement. (a) 178 functions on this bundle are `W_AMBIGUOUS_CLOSURE_ENV`: the SAME function index is created at two `CreateClosure` sites with DIFFERENT environments (Hermes inlines a closure-making helper), so its body's env-slot names can only be right for one of the two contexts. A correct fix duplicates the emitted body once per creation context with the `_e…` names rewritten per context; placement can only pick the cheaper of the two, which is why 58 unbound `_fn<n>` references remain (21 before, the other side of the trade that removed ~380 unbound env reads). **CAUSE (b) FIXED (2026-09-05, `src/cfg/env-graph.ts`)**: the other 4,009 were never orphans. `rEnv` did not merge to UNKNOWN at a join — it was loaded by `LoadConstUndefined`, which is Hermes stating that the closure captures nothing (`LoadConstUndefined rE; CreateClosure rD, rE, fn`, emitted for every non-capturing nested function from v96 on; reproduced from source with hermesc v99 and present in 15 existing construct fixtures at v98/v99). The §6.2 lattice folded that into UNKNOWN, so the function came out with no `closureEnvOf` entry — an orphan whose own `selfEnv` was unknown, so its `CreateFunctionEnvironment` had no parent and the closures it created cascaded into orphans as well (that is the 1,755; the 2,254 are the direct sites, 1,833 of them in the global function). The lattice now has a fourth value `none`, and `Create*Closure` off a `none` register records `closureEnvOf(fn) = null` — known to capture nothing. Measured on react-navigation-example: `W_ORPHAN_FUNCTION` 4,009 -> 0, functions with a known-empty environment 179 -> 4,188, functions with a *resolved* environment unchanged at 11,363 and unresolved env accesses unchanged at 182 — the change only ever turns UNKNOWN into a known answer, it never rebinds anything. Emitted output is byte-identical on react-navigation `--passes=none` (22,784,763 bytes before and after) and on Service NSW `--passes=none` (78,263,633 bytes, 0 isolated, measured on deb), so isolated/unbound stay 103/158 with passes on and 106/160 with `--passes=none`: **the residual 158 unbound names are all cause (a)**, and a function created both with a real environment and with an undefined one is now reported ambiguous rather than silently bound. | placement itself is covered: 12 unit cases on the pure rule in `tests/gate/emit/unbound-isolation.test.ts`, and `tests/sweep/emit/unbound-env-slots.test.ts` ratchets 103 isolated functions with a `W_ORPHAN_HOSTED` floor and named `_e2326_0`/`_e652_0` checks. Cause (b) is covered by `tests/gate/cfg/env-no-capture.test.ts` (15 construct fixtures at v96/v98/v99 that compile a non-capturing closure must have no `W_ORPHAN_FUNCTION`; a corpus guard that the `LoadConstUndefined` + `CreateClosure` shape is still present; and a synthesised two-site module for the soundness half — a real env on one path and undefined on the other must stay `W_AMBIGUOUS_CLOSURE_ENV`) plus a second ratchet in `tests/sweep/emit/unbound-env-slots.test.ts` (0 `W_ORPHAN_FUNCTION` and at least 4,000 known-empty environments on react-navigation). No adversarial `source.js` fixture yet for the cause-(a) residual: it does not reproduce from small source under hermesc v96 — probed function-body dedup (two identical inner bodies, not deduped), a twice-called closure-making helper (not inlined), a generator yielding closures over a loop variable, all four produce zero orphans. Both remaining causes are properties of large Metro bundles, so the react-navigation sweep test is the regression test until someone finds a small producer or synthesises the bytecode directly. **CAUSE (a) IMPLEMENTED (2026-09-05)**: `EnvGraph.closureCopies` gives such a function one body per distinct captured environment, each placed in the owner of the environment it captured and each `Create*Closure` site naming its own copy, with a positional `envRemap` rewriting the copy's `_e<env>_<slot>` names (`src/cfg/env-graph.ts`, `src/emit/index.ts`/`function.ts`/`lower.ts`). Measured on react-navigation-example: 178 ambiguous -> 156 duplicated (335 extra bodies) + 4 joined by report §3 + 18 unaligned residual; 103 -> 79 isolated, 158 -> 155 unbound names, 111 -> 13 orphans hosted, and a bundle with no ambiguity (`rn-template-0.72`) is byte-identical. Regression tests: `tests/gate/cfg/closure-copies.test.ts` (buckets A/B/C, the §3 join, the undefined-operand case), `tests/gate/emit/closure-copies.test.ts` (two bodies with their own env-slot names, emitted through `emitModule`), and new ratchets in the sweep test. RE-MEASURED (2026-09-05, this row's ratchet task, after F24-5 `W_NO_CAPTURE_HOSTED` and closure copies): unchanged at 10 isolated / 22 unbound names on react-navigation-example, both passes on and `--passes=none` (F24-5 hosts exactly 1 function on this bundle via `W_NO_CAPTURE_HOSTED`, and it was never part of the isolated residual). Bucketed by cross-referencing `W_AMBIGUOUS_CLOSURE_ENV`'s 18 function indices against the isolated set: BUCKET 1 (7 of 10 isolated functions, 19 of 22 unbound names) is the bucket-C unaligned-chain-length residual from the closure-copies fix — 4 functions (`h`, `start`, `_fn11246`, `t`) reference a child that is itself one of the 18 `W_AMBIGUOUS_CLOSURE_ENV` functions, created at sites whose environment chains differ in length, so `closureCopies` has no positional remap for it; 3 more (`_fn14984`/`85`/`86`) are themselves in that 18 and read their own `_e4551_*` slot, which their 4 disagreeing creation sites cannot agree on. BUCKET 2 (3 of 10, 3 of 22) is report §5's "leftover 7" family (`queryFn` i.e. `_fn13056`, `_fn15251`, `_fn15275`): a `W_JOINED_REHOSTED` function moved to the lowest common ancestor of its creation sites, but a child it merely captures (does not create) stayed at the old home, because the child's own reads are not visible from the new host either — needs per-instance `parentOf`, already flagged as a design change in the report, not landed here. Neither bucket has a safe fix under 100 lines, so nothing further was landed this pass beyond a new ratchet (`tests/sweep/emit/unbound-env-slots.test.ts`, split by `_e<env>_<slot>` vs `_fn<n>` kind). Also fixed in passing: `src/decompile.ts` snapshotted `analysis.diagnostics` before anything read the lazy `analysis.envGraph` getter, silently dropping every envGraph-sourced diagnostic (`W_AMBIGUOUS_CLOSURE_ENV`, `W_ORPHAN_FUNCTION`) from `decompile()`'s own result whenever the caller had not accessed `.envGraph` first — `cachedDecompile(...).diagnostics` reported 0 `W_AMBIGUOUS_CLOSURE_ENV` on this bundle where `analyseModule(...).envGraph.diagnostics` reported 18. Fixed by re-spreading `analysis.diagnostics` fresh at the end of `decompile()` instead of using the early copy; a regression test in the same sweep file now asserts `decompile()`'s own diagnostics see a nonzero count. | (unassigned) | open | emit-shape | prove fixed: react-navigation whole-file reports zero `W_UNBOUND_ISOLATED` and the ratchet in `tests/sweep/emit/unbound-env-slots.test.ts` drops to 0; RE-MEASURED 2026-09-05: residual is 10 isolated / 22 unbound (the 7+3 bucket split above), and the two remaining buckets need a chain-length-tolerant remap (bucket 1) and per-instance `parentOf` (bucket 2) — neither is "per-copy travel"; (b) is done, and (a)'s duplication half is done; QUEUE 7 (whole-file abort, same E_UNBOUND_IDENT family) |
| 2026-09-04 | MCP write-tools round (docs/specs/17-mcp-harness.md §2/§14) implementation, `ProjectService` DB write-path prerequisite | `src/project/service.ts` (`setName`) | `set_name` (spec 17 §2: "`ProjectService.setName` (overlay append; Design-D)") is implemented for DB-backed (`.hbcproj`) projects only, via `src/projdb/annotations.ts`'s `dbSetName` (the DB-native `d_names` table, same one-append-plus-one-log-row contract every other write verb here gets). A JSONL-backed project's name writes live in a SEPARATE store (`src/name-overlay/`'s `OverlayStore`/`NameService`, its own gate/frame-body machinery, no `log` table, constructed from a `ModuleAnalysis` — not from just the `ArtifactService` `ProjectService` is built over) and are not wired into `ProjectService.setName` this round; calling it against a JSONL-backed project throws `E_USAGE` naming this gap rather than silently no-op'ing or writing to the wrong store. | no test possible for the JSONL path until `ProjectService.setName` is wired through `NameService`/`OverlayStore` (needs `ProjectService` to also hold or receive frame-body/gate context it doesn't have today) or the two stores are unified; the DB-backed path IS tested (`tests/mcp/tools.test.ts`'s `set_name: lands one annotation + one log row`) | project-DB lane / name-overlay lane (unassigned) | open | metrics | prove fixed: `ProjectService.setName` accepts a JSONL-backed project too (wired through `NameService`, gated exactly as `hbc2js name set` already is) and a `tests/mcp/tools.test.ts`-style fixture asserts the same `set_name` write tool round-trips identically on both backends |
| 2026-09-04 | project-DB step 4 (docs/specs/16-project-db.md §8 step 4, `ArtifactService`/`ProjectService` DB query paths) implementation | `src/projdb/project-read.ts` (`loadProjectStoreFromDb`) | The DB-backed `ProjectService` read path reconstructs `tag`/`comment`/`bookmark`/`finding` records from `DbRevisionStore.allRecords()` (step 3's engine), but `revisions.kind='status'` rows (finding status-transition history) are never reconstructed as `StatusRecord`s — `schema.sql` (step 3) has no `d_status` detail table to read them back from — and `revisions.kind='conflict'` rows have no read path either (no `v_json_conflicts` view, consistent with §9/§10's own merge-deferral ruling that `project merge` refuses on DB projects v1). A DB-backed project therefore currently reports `findings()`/`stat()` from `finding`-kind rows only; any status-transition history and any `conflict` record a JSONL-imported project might carry (once `init --from`, step 5, lands) would not surface via the DB-backed `ProjectService`. UPDATE 2026-09-05: the `status` half is fixed -- `src/projdb/project-read.ts`'s `splitFindingRevisions` now reconstructs `StatusRecord`s from the status-carrying `kind='finding'` revisions `setFindingStatus` writes (the "equivalent readback path" this row's prove clause allows, no DDL change), pinned by `tests/project/finding-status-db.test.ts`. Only the `conflict` half remains open. | no test possible until either `d_status` is added to `schema.sql` (a step-3/DDL change, out of this round's file list: `src/artifact`, `src/project`, `src/projdb`... `schema.sql` IS in `src/projdb`, but changing shipped DDL after step 3 landed A1–A4 needs its own reviewed migration, not a step-4 side effect) or `project merge`/`conflict` reconstruction is designed for DB projects (deferred, §10 ruling 4); once either lands, a DB-project fixture with a status transition or an imported conflict record should assert `ProjectService` surfaces it identically to the JSONL path | project-DB lane (unassigned) | open | metrics | prove fixed: `schema.sql` gains a `d_status` detail table (or an equivalent readback path) wired through `src/projdb/annotations.ts`'s finding adapter, `src/projdb/project-read.ts` reconstructs `StatusRecord`s from `kind='status'` revisions, and a `tests/projdb` fixture with a `finding-status` transition round-trips through a DB-backed `ProjectService` identically to the JSONL one (conflict-record reconstruction can land separately, tracked against §10 ruling 4's eventual merge design) |
| 2026-09-04 | app-gen fuzzer increment 2 (docs/specs/09-fuzzing.md §2, Metro RAM bundle axis), attempted triple `tools/appgen/build.mjs --hbc 96 --bundler metro-ram` | `tools/appgen/build.mjs` (bundler axis) / upstream `@react-native/community-cli-plugin` + `metro` CLI, not our extraction | The spec §2.1 "Metro RAM (indexed)" bundler value cannot be built at all with the currently-pinned RN 0.73.11 toolchain: `react-native bundle`'s `--indexed-ram-bundle` flag does not exist in `@react-native/community-cli-plugin`'s `bundle` command option list (verified by reading `node_modules/@react-native/community-cli-plugin/dist/commands/bundle/index.js`'s option array — no ram-related flag under any name) and fails fast with `error: unknown option '--indexed-ram-bundle'`, before Metro or hermesc ever run. Metro's own `metro build` CLI still ships a legacy `RamBundle` output module but its `--output-type` flag is dead code in `metro/src/commands/build.js` (parsed by yargs, never passed into the `options` object handed to `MetroApi.runBuild`), so there is no currently-invocable CLI path to a RAM-format bundle at all — upstream tooling removal, not a gap in our extraction/split pipeline as the spec anticipated. | this is an upstream-toolchain finding, not a decompiler behaviour; `tests/sweep/appgen/build-axes.test.ts`'s RAM-axis test already pins the observed failure mode directly (real build attempt, `buildStatus:"failed"`, error text asserted) — no further regression test is possible until upstream re-exposes RAM-bundle generation | app-gen fuzzer lane (unassigned) | open | harness | prove fixed: a future RN/Metro release re-exposes `--indexed-ram-bundle` (or an equivalent) in the pinned toolchain's `react-native bundle`/`metro build` CLI, `tools/appgen/build.mjs --bundler metro-ram` produces a real `bundle.hbc`, and our split/segregate pipeline is then evaluated against it for the first time |
| 2026-09-02 | SWEEP job red, `tests/sweep/hermes-lit.test.ts` case `date-fp-contract` | harness — `tools/harvest-hermes-lit.ts` empirical harvest, not the decompiler (this test never invokes hbc2js at all) | The harvested case (upstream `test/hermes/date-fp-contract.js`, "check that the date implementation does not use fma") prints `Date.UTC(1970, 0, 213503982336, 0, 0, 0, -18446744073709552000)`. `expected.txt` (`34447360`) was empirically captured at harvest time (`5626dbd`) under whatever Node/V8 was current then; on Node v25.9.0 (this environment) the identical source now prints `34448384` — a genuine Node/V8-version difference in `Date.UTC`'s internal floating-point handling of this deliberately FMA-probing, wildly out-of-range input, not a decompiler regression (nothing here touches `src/`). | reproducible locally (`node -e 'console.log(Date.UTC(1970,0,213503982336,0,0,0,-18446744073709552000))'` → `34448384` vs the committed `34447360`), so a test *could* pin either value, but `expected.txt` is a golden/snapshot fixture — regenerating it needs Fred's approval as a batch (docs/CONSOLIDATION.md §B item 9), not a silent fix inside this task. Quarantined instead: `tests/sweep/hermes-lit.test.ts` skips this one case by name with a visible `t.skip()` citing this row, so the other 117 harvested cases keep gating the sweep. | harness lane (unassigned) | open | harness | prove fixed: Fred approves regenerating `tests/sweep/hermes-lit/cases/date-fp-contract/expected.txt` on the CI/reference Node version (or the harvest tool records the harvesting Node version and the sweep test cross-checks it, skipping only on a version mismatch instead of unconditionally) — then un-quarantine in the same commit. |
| 2026-08-31 | CONSOLIDATION 26 triage of adversarial/20-symbol-keyed-properties (v99.hbc) | toolchain + harness — oracle/harness artefact, not a decompiler bug against the available ground truth; exposes a real gap in `src/tables/generated/builtins-hbc99-*.ts` | The fixture's `v99.hbc` was compiled by `tools/hermesc/v99/hermesc` (`hermes-compiler@260318099`) but the reference VM `tools/hermes-vm/v99` is built from an older Hermes commit that predates `HermesBuiltin.setFunctionName`. That builtin was inserted at index 55, shifting every private builtin after it in the newer compiler; the VM's own execution of the original bytecode crashes on the shifted index, and the decompiler faithfully reproduces the same crash (candidate and VM die at the same site with the same error name). `README.md`'s status table + CONSOLIDATION 26 confirm this is not a decompiler bug against today's toolchain pair, but it is a real gap for any production RN "1000.x" bundle containing a computed-key method compiled by a post-2026-03-18 v99 compiler. | confirmed toolchain/harness artefact via CONSOLIDATION 26 (the VM's own hermesc compiles the source with no such builtin call at all); no fixture can regress this without either a new builtins table or a rebuilt VM | tables/toolchain lane + harness lane (unassigned) | open | toolchain | prove fixed: a `builtins-hbc99-post0318` table (`setFunctionName`@55, +1 shift after) selected by probing, and/or `tools/hermes-vm/v99` rebuilt past the commit adding `setFunctionName`, and/or `ladder.ts` letting VM==candidate overrule Node; adversarial/20 turns PASS at v99. No QUEUE item yet — owner: tables/toolchain lane |
| 2026-09-01 | M5 rung if-chain task — `npm run test:all` before the rung's final commit | `src/passes/call-shape` bundle metric (sweep tier) | `tests/gate/passes/call-shape-metrics.test.ts`'s sweep-tier RN-template-bundle floor fails: clean-function share **70.3% vs floor 90%** (35.0% with call-shape off). Pre-existing, not caused by this branch's rung (measured ~70.8% with the rung's behaviour changes disabled too) — the ~20-point drop against the floor predates this work; not bisected. CI never sees it (CI runs the gate, not the sweep). | attribution measured but not bisected — needs a `--no-pass` bisect over the stage-B rungs on the RN template bundle, then either a real fix or a re-measured floor | M5 pass-ladder lane (unassigned) | open | metrics | prove fixed: bisect with `--no-pass` over the stage-B rungs on the RN-template bundle to find what dropped clean-function share from >=90% to 70.3% since call-shape landed, then fix or re-baseline `tests/gate/passes/call-shape-metrics.test.ts`'s floor; QUEUE 11 (tier-1 buckets/re-baseline, adjacent) — owner: M5 pass-ladder lane |
| 2026-09-01 | same run | `src/deps/**` | `hbc2js deps` on the 12.7 MB bundle did not finish in 10 minutes (killed). rn-template takes seconds. Needs profiling (fingerprinting 43k functions or the npm-confirm/search path). **Update 2026-09-05 (profiled on deb, real Service NSW bundle, from a `45ebaf4` checkout):** `hbc2js deps` now completes in ~2m15s-2m35s wall (three runs), already well under 10 min — this appears to have been mostly fixed in passing by `377abff` (QUEUE 22a evidence-directed candidate matching, landed 2026-09-03, after this row was filed), which stopped the match stage from scoring every DB signature file against the bundle. `--cpu-prof` on a full run showed 96% idle time (119911/124854 samples), confirming the remaining cost is not CPU-bound fingerprinting; per-call instrumentation of `guess.ts`'s `npmRegistrySearch` found the actual driver: the guess stage's npm-search fallback issued 2068 sequential network round-trips for only 345 distinct query strings (generic minified-property-name leads like "exports"/"value"/"call" picked independently by hundreds of unattributed modules), ~143s of a ~146s run, entirely serial with no dedup. Fixed: `guessModules` now memoises identical queries behind one in-flight promise per distinct string within a run (`src/deps/guess.ts`), cutting the network round-trips from 2068 to <= 345 on this bundle. This is a query-count reduction, not the row's own worker-pool/parallelism ask, so left open rather than resolved. | needs profiling (done 2026-09-05, see update above); further work is QUEUE 22's worker-pool split, not more profiling | (unassigned) | open | metrics | prove fixed: `hbc2js deps` on the Service NSW bundle completes well under 10 min on this Mac (worker pool per module chunk, cached fingerprints); QUEUE 22 (Parallelise deps) |
| 2026-09-01 | same run | `src/passes/**` or `src/emit/scope-check.ts` (unbisected) | Whole-file decompile took **452 s** for 43k functions vs ~3 s for rn-template's 4k — ~150x for 10x the functions, so a superlinear term remains (suspects: module root fn#0 with 4,510 factory children through fn-naming/var-naming batching, or the scope check). **Update 2026-09-01 (QUEUE, passes-superlinear-term-2):** re-profiled with `--cpu-prof` on deb (32-core, `--lenient-env`, real NSW bundle): whole-file passes-on **946 s** wall, passes-off **19 s** (49x, worse than the Mac's earlier 452 s/whole-file measurement — different box, same shape). Top self-time frames were `src/passes/expr-rebuild/match.ts`'s `listIndex` (an eager `byReg`/`jumps` map rebuilt from scratch on *every* applied site, since `spliceList` gives the edited list a fresh array identity each time — 59 s self time, the single hottest frame) and `src/passes/ast.ts`'s `expressionOnlyCheck`/`effectSequence` (a whole-list effect-sequence walk + `JSON.stringify`, also once per applied site — ~90 s combined). Both are exactly term (a) from this row's original suspects, generalised: not fn#0-specific, any function with many independent `expr-rebuild` sites pays `O(sites × list.length)`. **Fixed**: `listIndex`/`nextRelevant` replaced with a direct bounded scan over the already-memoised per-node `stmtInterest` (no more eager whole-list index per splice); `expressionOnlyCheck` strips the reference-identical prefix/suffix `before`/`after` share (effect sequences are concatenative, so this is the exact same result for `O(changed region)`); `expr-rebuild/check.ts`'s `registerUses(before)`/`registerUses(after)` register-count delta computed the same way (non-regressive, a real win whenever `before` is `ctx.fnBody` and already cached). **Not fully closed**: a synthetic module-root-shaped micro-benchmark (`tests/gate/passes/pipeline-speed.test.ts`, 5,000 independent fold sites) still costs ~4 s CPU, because `check.ts`'s `bu = registerUses(before)` is *itself* a fresh `O(list.length)` walk once per site whenever nothing earlier in that site's own proof happened to warm `registerUsesMemo` for that list identity (`isDeadAfter`'s `tryDA` redefinition-found fast path never does) — the next actionable term, self-contained in `expr-rebuild/check.ts`. The real-bundle NSW wall-clock re-measurement (before/after, targeting <60 s) was not re-run this session (budget) — still needed to close this row. **Update 2026-09-01 (perf part 2):** `expr-rebuild/check.ts`'s `bu = registerUses(before)` removed entirely — `registerUseDelta` computes the register-use delta straight from the shared-prefix/suffix-stripped changed region (proof in the function's own doc comment; `registerUses(before)`/`registerUses(after)` over the whole list are never called at all now), verified byte-identical against 3 construct fixtures + rn-template (hash-pinned in `tests/gate/passes/pipeline-speed.test.ts`). Synthetic 5,000-site benchmark: ~4 s CPU → ~2 s CPU (not sub-second — still `O(n²)`-shaped at larger `n`, e.g. 8000-site CPU time is ~4x the 4000-site time, not ~2x). Re-profiled the residual cost (`--cpu-prof` on the synthetic, `tools/perf/summarize-cpuprofile.mjs`): the dominant remaining frame is now `ast.ts`'s `defUse(after)` inside `expressionOnlyCheck` — a full, un-bounded walk of `after` once per applied site (unlike `effectSequence` and the register-count delta, never narrowed to the changed region in part 1 or part 2), because its per-register order check ("no read before this register's own first def") needs a *global* first-def position, not just a delta, and the shared-prefix/suffix subtraction trick that worked for counts does not directly give order without additional incremental state this change did not add (soundness risk flagged, not attempted under this session's budget — see the next-step note below). Real NSW re-measurement on deb (32-core, `--lenient-env`, `~/hbc2js-bulk/corpus/nsw/index.android.bundle`, same bundle as the 946 s/715 s measurements): whole-file passes-on **563 s** (was 946 s, 1.68x), 0 stubs, 0 unresolved-env, `node --check` OK; `--split` passes-on **512 s** (was 715 s on the Mac, different machine so not a clean ratio, but a real absolute drop), 4,510/4,510 modules. Neither lands under the ~60–120 s target, so this row stays open — not closed by this update. **Update 2026-09-05 (listindex-perf, Sonnet):** re-verified the specific listIndex/nextRelevant term this row named: confirmed by code inspection that no listIndex function remains in match.ts (already replaced by the bounded nextRelevant scan over the memoised stmtInterest, from an earlier session, see this same cell's 2026-09-01 Fixed note above) and added a direct scaling proof, tests/gate/passes/expr-rebuild-perf.test.ts, an N=200 vs N=2000 fold-site CPU-ratio test in the pipeline-speed.test.ts CPU-ratio style (budget 20x times timeScale for a 10x growth in N; a pre-fix O(sites^2) rebuild would land near 100x). No source file was edited this session (the fix already existed), so rn-template passes-on output is byte-identical before and after: sha256 fa54d8f22ba3ccf07ab00dc07d3374a1443d45ae52d7f3027e321ce5b758d7d8, matching pipeline-speed.test.ts's existing pin, verified by re-running that test unchanged. Whole-file NSW re-timing was skipped this session: this Mac's load average was 46 to 80 at the time (uptime), well over the 20 threshold this row's brief set for a fresh measurement. This session narrows only the listIndex sub-term confirmation; the row itself stays open on the defUse(after) term in the why-no-test-yet cell, unresolved by this update. **Update 2026-09-05 (defuse-perf, Opus 5):** the `defUse(after)` term this row's why-no-test cell named was already fixed on 2026-09-03 by commit 32548da (`incrementalReadBeforeDef` in `ast.ts`: per-lineage persistent interpolated position keys plus a carried-forward violating-name set, so only the spliced region is walked) - that commit shipped the corpus differential test but neither of the two proofs this row asked for, which this session adds. (a) Bounded cost, `tests/gate/passes/expr-rebuild-perf.test.ts`: a self-calibrating CPU ratio - 500 splices through `expressionOnlyCheck` cost 18 percent (4,000-statement list) and 22 percent (8,000-statement list) of what 500 whole-list `defUse(after)` walks alone cost on the same lists in the same process, so the pre-fix algorithm scores ~100 percent there by construction. (b) Soundness, `tests/gate/passes/expression-only-check-differential.test.ts`: the prefix/middle first-def hazard driven directly along a three-step lineage that moves `r0`'s first def across the frozen-prefix boundary in both directions - RED against a naive memo (verified by deleting `namesTouching(before-mid)` from `incrementalReadBeforeDef`, after which step 2 is accepted unsoundly) and GREEN as shipped. This row's "8000-site case drops to ~2x the 4000-site case" clause is still NOT met, and cannot be met by any `defUse` work: re-measured on this Mac the synthetic fold benchmark is 903 ms CPU at 4,000 sites and 3,390 ms at 8,000 (~3.8x), and a fresh `--cpu-prof` of the 8,000-site case shows `incrementalReadBeforeDef` is no longer a dominant frame (1.1 s of 32 s sampled self time). The top frames are now `expr-rebuild/rewrite.ts`'s `[...list.slice(0, i), ...list.slice(i + 1)]` whole-array rebuild (6.8 s - two array copies per call, and `check.ts` calls `rewrite` a second time per site to re-derive the expected `after`), `check.ts`'s `expected.every(sameStmt)` whole-list comparison (3.0 s plus 0.9 s in `sameStmt`), `ast.ts`'s `spliceList`/driver walk (3.2 s) and the GC churn all that allocation causes (2.7 s). That residue is Theta(sites x list.length) by construction of the immutable statement-array representation, not a missed memo: a bare loop doing nothing but the array rebuild costs 79 ms at 4,000 sites and 417 ms at 8,000 here. Recorded as PUSHBACK P-33. Next actionable term: a bounded-region rewrite/compare in `expr-rebuild` (build and compare only the touched window, the rest already proven reference-identical), and beyond that a persistent list representation. NSW whole-file re-timing skipped again - this Mac's load average was 80, over the threshold of 20 the brief set. **Update 2026-09-05 (rewrite-perf, Opus 5):** part 4, the writer/checker half this row's why-no-test cell named. `expr-rebuild/rewrite.ts` built the new statement array as `[...list.slice(0, i), ...list.slice(i + 1)]` (three allocations and ~2x `list.length` element copies per call) and, for R1a, did it a *second* time to place the substituted statement; `check.ts` then called `rewrite` all over again purely to re-derive an expected `after` whose every element outside the touched window was already reference-identical to the real one, and compared the two lists element-wise; `registerUseDelta` added two more whole-list scans to rediscover a window the re-classified site already names. Eight whole-array allocations per applied site. **Fixed**: the writer allocates once and copies once (`withoutAt`, substituting in place in the array it just built); the checker allocates nothing at all - `verifyExpectedShape` re-derives the expected element at each position from `before` plus the re-classified site, so the comparison is a pointer test per position and at most one structural comparison, still covering every position (a writer that also perturbed a far-away statement is still caught); `registerUseDelta`'s window is now `[i, j + 1)` / `[i, j)` taken straight from the re-classified site, `O(fold distance)`, sound because `verifyExpectedShape` has already proven every position outside it unchanged. Proof shipped in `tests/gate/passes/expr-rebuild-perf.test.ts`: a third self-calibrating CPU ratio whose denominator is literally the removed work (the pre-fix rebuild run twice plus the whole-list comparison), interleaved site by site in the same process - the shipped code measures 25 to 50 percent of it at 2,000-statement and 4,000-statement lists, where the pre-fix code scores above 100 percent by construction. Output unchanged: rn-template whole-file passes-on sha256 is still fa54d8f22ba3ccf07ab00dc07d3374a1443d45ae52d7f3027e321ce5b758d7d8, `pipeline-speed.test.ts`'s existing pin, re-run green. This row's "8000-site case drops to ~2x the 4000-site case" clause is still NOT met and is still architectural: measured here after the fix, the rewrite+check half costs 35 CPU ms for 2,000 sites on a 4,000-statement list and 148 ms for 4,000 sites on an 8,000-statement list (4.2x for a 2x growth in N), because the one remaining whole-array build in the writer is the floor for an immutable `readonly Stmt[]` - see PUSHBACK P-34. NSW whole-file re-timing not possible on this Mac: the bundle is not present here (`tests/fixtures/local-corpus/` holds only `MANIFEST.json` and `README.md`), so the wall-clock half of this row's verdict still needs a deb run. | next term is a persistent list representation for statement lists (or a driver contract that lets a pass return a patch rather than a whole list): after parts 1-4 the only whole-list work left per applied site is the writer's single `new Array(n)` build plus a handful of non-allocating pointer scans, which is Theta(sites x list.length) by construction of `readonly Stmt[]` and cannot be memoised away - see PUSHBACK P-33 and P-34. The remaining wall-clock verdict below also needs a deb re-timing of the Service NSW bundle, which is not present on the Mac. **Update 2026-09-05 (perf re-time on deb, Sonnet):** re-timed all three on deb (32-core, `~/hbc2js-perf` at 9df2ba7, real `nsw.hbc`, box shared with a live fuzz campaign plus desktop apps eating ~18-19 GB/31 GB RAM and 6+ GB swapped, load average 2-3 throughout - noisier than the 946s/563s sessions' clean box). Whole-file passes-on `--lenient-env`: **981 s** wall (was 563 s, worse, not better - see below), max RSS 4.28 GB. Whole-file passes-off (`--passes=none --lenient-env`): **29.5 s** (was 19 s). `--split` passes-on `--lenient-env`: **39.3 s** (was 512 s - a 13x drop, 4,510/4,510 modules, 43,384 functions), max RSS 3.60 GB. The `--split` number is not swap noise (noise would slow every run, not speed one up 13x): it confirms parts 1-4's bounded-region fixes actually close the gap for every module's *own* fold-site list, which is short; `--split` never has to build the one `fn#0` list with 4,510 factory-call statements that whole-file must. A `--cpu-prof` of the whole-file run (1045 s sampled) shows the P-33/P-34 materialisation floor still dominates there: GC 250 s self (24%), then `expr-rebuild/match.ts`'s `classifySite` (51.7 s), `nextRelevant` (40.6 s + 29.6 s at two call sites), `scanFrom` (23.8 s) and `stmtInterest` (20.7 s + 19.6 s) - the bounded-scan replacements from part 1 are each individually O(window), but `fn#0`'s list is thousands of statements long and every one of its ~4,510 fold sites re-runs the scan, so the aggregate is still the Theta(sites x list.length) floor PUSHBACK P-33/P-34 named, just relocated from `defUse`/rewrite (parts 3-4's targets, now cheap) to the still-unbounded classify/scan layer for this shape. Row verdict not met: whole-file is further from the 60-120 s bar than the last measurement (box noise plausible but not provably the whole story), so status stays open; `--split`'s 39 s is now well inside any reasonable bar, narrowing this row's remaining scope to whole-file's `fn#0`-shaped case specifically. Full numbers and top frames in `docs/reports/2026-09-05-nsw-retime.md`. **Update 2026-09-05 (perf part 5, Opus 5):** the classify/scan layer that re-time named. Measured first (`docs/reports/2026-09-05-perf5-match-scan.md`): the cost is not in the site count but in the *shape* - a synthetic module-root list whose every site stores a register once, reads it once and never mentions it again (fn#0's shape) costs 940 ms / 4,458 ms / 25,526 ms CPU at 2k/4k/8k sites, exponent ~2.3, against ~1.6 and 30x less absolute for the register-reusing shape the gate already covered; and with the site count held fixed at 500, cost grows straight in proportion to an inert tail none of those sites needs to look at (8/15/50/180 ms for tails of 0/2k/8k/32k). Both terms were reached from `match.ts`'s `isDeadAfter`, which is a disjunction of two pure predicates and ran the *expensive* one first: D-a's `scanFrom` walks every remaining statement to the end of the list before it can conclude anything about a register never mentioned again (the profiled `nextRelevant`/`stmtInterest`, 4.2 s of 14.9 s), and only on its failure did D-b consult `registerUses(ctx.fnBody)`, a cold whole-list `countUses` walk because every applied site gives the edited list a fresh array identity (a further 4.9 s). **Fixed**: `isDeadAfter` asks D-b first whenever its map is already memoised (`registerUsesIfMemoised`, new in `ast.ts`), falling back to the old order when it is cold; and `check.ts` now *derives* the accepted list's map from the one it just proved (`noteRegisterUsesSplice`, `uses(after) = uses(before) - uses(beforeMid) + uses(afterMid)`, the same concatenativity `registerUseDelta` already stands on, sound because `verifyExpectedShape` has proven every position outside that window unchanged first), so the map is warm for every site after the first. Same predicates, same values, same verdict per site - only evaluation order and the map's provenance change. Proofs: `tests/gate/passes/expr-rebuild-perf.test.ts` gains a cost-per-site-vs-list-length ratio (500 classify calls behind a 20,000-statement inert tail vs the same 500 with no tail - 37.9x pre-fix, ~1x post-fix, budget 4x), and a new `tests/gate/passes/register-uses-splice-differential.test.ts` checks the derived map against a cold walk of the same statements for every prefix of four shapes (unique registers, reused alphabet, R1a/R1b/R1b-impure/R1c mixed, folds nested inside an `if`), mutation-verified RED by dropping the subtraction term. Measured: the 8,000-site module-root-shaped benchmark 25.5 s -> 7.1 s CPU (3.6x), the register-reusing shape unchanged within noise, and no `classifySite`/`nextRelevant`/`scanFrom`/`stmtInterest`/`countUses` frame left in the post-fix profile's top ten. Row stays **open**: the exponent on that adversarial shape is still ~2.2 (top frames now `noteRegisterUsesSplice`'s own `O(distinct register names)` map copy, `incrementalReadBeforeDef`, `withoutAt` and `verifyExpectedShape` - the P-33/P-34 materialisation floor), and the wall-clock verdict is a Service NSW whole-file re-timing, which needs the bundle and therefore deb - **the orchestrator re-times there**. `tests/gate/passes/pipeline-speed.test.ts` and its rn-template output hash were not touched and re-run green, so the pass's output is byte-identical. | (unassigned) | open | metrics | prove fixed: (orchestrator re-worded 2026-09-05 after P-33/P-34: the per-site `defUse`, effect-sequence, register-delta and rewrite/compare terms are each proven bounded by their own CPU-ratio tests; the remaining `~4x for 2x N` is the `readonly Stmt[]` materialisation floor, which only a persistent list / patch-returning driver contract removes — that is perf part 5, not this row's bar) a re-timed Service NSW whole-file `--lenient-env` decompile with passes on lands under 60–120 s wall on deb (currently 563 s); `--split` passes-on under a similar bound (currently 512 s); QUEUE 8 (Service NSW 452 s) / perf part 3 |
| 2026-09-01 | E2E tier 1 — residual `tree:unmatched-closure` after QUEUE 7's fix (below): react-navigation still shows 1358/1493 (passes off/on), down from 2921/2987 | `src/cfg`/`src/emit`'s env-graph (`envGraph.closureEnvOf`) | The real root cause QUEUE 7 worked around rather than fixed: some functions have no `closureEnvOf` entry at all (`emitModule`'s `parentOf` is `null` though the function is not logically top-level), so `--split` cannot know their true original nesting depth and nests them at their referencing factory's top level instead — a legal but not necessarily isomorphic placement (fixes the `ReferenceError`, not full round-trip fidelity). Separately, rn-template's 28 `tree:unmatched-closure` entries (all passes-on; 0 passes-off) are a different, still-untouched cause: the closure *is* declared and stored but nothing reads the register, so `hermesc -O` drops it — a semantic drop by some pass. | needs a construct fixture that puts a closure's creation site in a Metro-style `__d()` factory whose env chain `src/cfg`'s env-graph analysis fails to attribute (not yet reduced), plus a `--no-pass` bisect for rn-template's register-drop cause | M6 split lane | open | real-app | prove fixed: env-graph analysis finds a `closureEnvOf` creation site for every function so none is classified an orphan, and `tools/e2e/roundtrip-corpus.ts`'s `tree:unmatched-closure` bucket drops to 0 for react-navigation; rn-template's dropped-closure cause bisected with `--no-pass`. No QUEUE item yet — owner: M6 split lane |
| 2026-09-04 | object-literal rung landing (spec 20 §7 refusal table) | `src/passes/object-literal` | Narrowed 2026-09-05 (agent/objlit-interleaved): (a) `object-literal-putbyid` is fixed — see the Resolved row below. (b) `object-literal-interleaved` is partially fixed: a proven commutation check (`canHoist` in `match.ts`) now hoists a *pure register def* interleaved inside a run above the whole run, closing the general case. Residual, believed permanent: a register REUSED for a self-referential redefinition (`r2 = r3 + r2`, `63-object-literal` fn `table`'s `len` property at v98/v99, a different reused register for the `10` key at v84/v96) correctly still refuses to hoist — doing so would change what an earlier fold in the SAME run already read from that register. `table` folds 3 of 4 properties into the literal at v98/v99 and 2 of 4 at v84/v96; only v94's register allocation happens to give every value its own register (4 of 4). (c) `object-literal-computed-key` (`PutOwnByVal`/`DefineOwnByVal`) is **fixed 2026-09-05** (agent/objlit-computed) — see the Resolved row below; this row narrows to (b)'s residual only, which stays open. | (b)'s residual needs reasoning about which *generation* of a reused register a later statement reads (a small per-register SSA-like renumbering local to the run), not a bigger hoist window — the current rule is sound and terminates, just conservative on this one shape | M5 ladder (unassigned) | open | passes | prove fixed (narrowed): `table` folds all 4 properties at every one of the 5 versions (needs the register-generation refinement above), or a decision that 3-of-4/2-of-4 is the accepted residue |
| 2026-09-01 | E2E tier 1 — `diff:param-count`, 293 fns react-navigation passes on (0 rn-template) | `tools/e2e/roundtrip-corpus.ts` pairing (harness limitation, not a decompiler bug) | With passes on, `fn-naming` gives several sibling closures the same restored name; the harness then pairs the leftovers by position and can mis-pair, which lands in `param-count`. Passes-off pairs by the split's unique `_fnNNN` names and has no such bucket. | harness follow-up: pair leftovers by (name, paramCount, closure count) before position | E2E lane | open | harness | prove fixed: `tools/e2e/roundtrip-corpus.ts` pairs leftover functions by (name, paramCount, closure count) before falling back to position, and the `diff:param-count` bucket (293 fns react-navigation passes-on) drops. No QUEUE item yet — owner: E2E lane |
| 2026-09-01 | QUEUE (e2e round-trip register/scheduling insensitivity) — buckets `diff:GetByVal(reg)` (298 rn-template passes-off, 103 passes-on as `GetById(reg)`), `diff:GetById(reg)` (316 react-navigation passes-on), `diff:TryGetById(reg)` (267 react-navigation passes-on), `diff:LoadFromEnvironment(reg)` (147 rn-template passes-off) — the top bucket family left after the `{ strong: true }` normalisation (register-reuse-safe reordering + LoadConst canonicalisation) | `src/harness/roundtrip.ts` normalisation (harness limitation, not a decompiler bug) — `normaliseFunction`'s register renaming is per-first-appearance-of-a-physical-number, valid for the whole function | Two builds that reuse the SAME physical register for two different, non-overlapping logical values (build A: `r3` for value X then later reused for value Y) vs. two builds that allocate DIFFERENT physical registers for X and Y produce a provably-identical instruction *sequence* that still fails to normalise equal, because the renaming key is "which physical number", not "which write". Confirmed present after the strong normalisation landed (only (b) instruction-reordering and (c) LoadConst-opcode canonicalisation were implemented this round; a def-based/SSA-style rename was explicitly scoped out — see `src/harness/roundtrip.ts`'s module doc comment on `reorderPoolableRuns` for why: it needs a def/use classification per opcode operand the decoder does not expose, and getting the "operand 0 is dest" convention wrong on the exceptions (stores, conditional-jump operands) risks a false IDENTICAL). | needs a per-DEFINITION (not per-first-appearance) register rename: assign a fresh canonical name every time a register is WRITTEN, using each opcode's own def/use shape rather than the "operand 0 is dest" heuristic (which has real exceptions) — a spec-level decision, not a quick follow-up | E2E lane / harness normalisation | open | harness | prove fixed: a hand-built pair of functions where build A reuses one physical register for two non-overlapping values and build B uses two distinct registers for the same values normalises equal under `{ strong: true }`, and the `diff:GetByVal(reg)`/`diff:GetById(reg)`/`diff:TryGetById(reg)`/`diff:LoadFromEnvironment(reg)` e2e buckets shrink on rn-template and react-navigation. No QUEUE item yet — owner: E2E lane |
| 2026-09-02 | P-10 fix (classify.ts package-boundary gap, `segregateSplitTree` confirmed-ownership override) follow-up | `tools/pkgsig/db` (signature DB) — no `@react-navigation/material-top-tabs` coverage | **Narrowed 2026-09-04**: `tools/pkgsig/db` now carries a `@react-navigation/material-top-tabs@8.0.0-alpha.49__hbc98.json` signature (this fixture's own `deps-truth.json` ground-truth version), built the same way `hbc2js deps --confirm` builds one (`confirmCandidates`, `src/deps/confirm.ts` — scratch RN 0.85.3 project, real npm dependency tree, Metro bundle, `tools/hermesc/v98`, baseline-subtracted), and `runDeps --offline` on react-navigation-example-0.85.3 now DOES list `@react-navigation/material-top-tabs` in `confirmedDeps` (high tier: 19 module-exact hits, 449 function-exact hits). But module 1611 still does not move: checked directly, every one of this fixture's own material-top-tabs-sourced module ids (1611, 1612, 1613, 1615, 1616, 1618-1623, per the fixture's `.map` `sources` entries under `/packages/material-top-tabs/src/...`) has ZERO exact-or-fuzzy hash hits against the new signature — the package's own "high" tier comes entirely from code material-top-tabs shares with `@react-navigation/elements`/`core` (already covered by the confirmed `native`/`stack` signatures), never from material-top-tabs's own code. Root cause, confirmed via the bundle's own source map: react-navigation-example builds `@react-navigation/*` from the react-navigation monorepo's own workspace source tree, not the published npm tarball this signature was fingerprinted from — a genuine build-provenance divergence a hash-based signature cannot bridge, not a version-pinning mistake (8.0.0-alpha.49 is exactly this fixture's own ground-truth version). `segregateSplitTree`'s confirmed-ownership override (docs/BUGS.md's other, resolved P-10 row) only ever acts on a real `moduleOwnership` entry, so it correctly has nothing to act on here; `tests/gate/split/segregate.test.ts`'s pinned 3/50 (WITH deps) is unchanged, confirmed by direct re-run. | **test now exists**: `tests/gate/deps/material-top-tabs.test.ts` pins that `confirmedDeps` includes the package AND that none of the fixture's own material-top-tabs module ids have a confirmed owner yet — proving the achievable half of this row's original verdict and pinning the remaining gap so a future fix that bridges it is visible rather than silently changing segregation counts | M6 segregation lane / deps signature-DB lane (unassigned) | open | deps | prove fixed: a module-level match mechanism (or a differently-sourced signature — e.g. fingerprinted from a react-navigation monorepo checkout at the matching commit/tag instead of the npm tarball) gives at least one of react-navigation-example-0.85.3's own material-top-tabs module ids (1611 first) a confirmed `moduleOwnership` entry, and module 1611 then files to `node_modules/@react-navigation/material-top-tabs/` in segregation with no further `segregate.ts` changes |
| 2026-09-05 | campaign-2 rediff, family `counter-inc-dec-reset-RangeError`, 13 finds / 4 signatures, v84,94,99, example `v84-seed2000526.js` | unconfirmed | A counter construct (`initial:`/`inc:`/`dec:`/`reset:`) diverges later in its trace, at or near an `uncaught RangeError` on one side; reproduces on 3 of 4 traced versions (not 96 in this sample). | Corrected 2026-09-05 (`docs/reports/2026-09-05-campaign2-v94-v99-vm-reclassify.md`, Mac VMs): all 13 sampled v94/v99 members are INCONCLUSIVE -- an engine resource-ceiling RangeError acts as a budget cutoff on one side (fix-wave-3 marker), not a value divergence; the "diverges near an uncaught RangeError" wording above is that cutoff. Stays open only for the unsampled v84 members | fuzz-lane follow-up (unassigned) | open | semantics | prove fixed: reduce one example seed of the family with `tools/fuzz/minimise-live.mjs`, land it as a construct or adversarial fixture that fails before and passes after the fix; owner fuzz-lane fix-wave (see QUEUE) |
| 2026-09-05 | CI red-run fix task, GitHub sweep workflow run 33914925844 (`tests/sweep/passes/pipeline-speed.test.ts:42`), reproduced locally | pass pipeline wall-clock cost on `index.android.noopt.hbc` | P-1's ratio check (passes-on / passes-off must stay under 12x) fails on GitHub's ubuntu runner (ratio 13.0, on 41163 ms, off 3176 ms) AND reproduced locally on a heavily-loaded Mac (ratio 15.0, on 21789 ms, off 1457 ms) -- not a CI-runner-speed artifact, since local is worse, not better. | the test already exists and already asserts the bound; this row exists because the bound is genuinely being violated right now, not because coverage is missing -- the eventual fix (whichever pass is the wall-clock outlier) ships its own regression test | orchestrator / pass-pipeline perf lane (unassigned) | open | passes | prove fixed: profile `index.android.noopt.hbc`'s passes-on run to find which pass(es) dominate the added wall-clock cost, bound or fix that pass, and the ratio returns under 12x on both a normal dev machine and CI's scaled budget without loosening the threshold |
| 2026-09-05 | CI red-run fix task, GitHub sweep workflow run 33914925844 (`tests/sweep/decompile/sweep.test.ts` T6), reproduced locally | hardened (obfuscated) construct fixtures -- `39-destructuring-params.obf`, `51-default-params.obf` | The four class-fixture divergences of this row are FIXED (see the Resolved row of the same date, sha fa23564: v99 builtin 55 was mis-named). What is left is three trace divergences with a different, still-unidentified cause and no thrown error: two on `39-destructuring-params.obf` (one of them reported as diverging from the Hermes VM's own execution of the original bytecode) and one on `51-default-params.obf` at record 12, an output/globals mismatch. Neither fixture uses the renumbered builtins (their only `CallBuiltin` is b44 `throwTypeError` at v98/v99 and none at all at v84/v94/v96), so the class fix cannot be the cause. T6's ceiling assertion (`divergent <= 4`) now holds again at 3. | not yet minimised to a construct-sized repro; the destructuring/default-params obfuscated shapes were not investigated in the task that fixed the class half, which stopped at its budget | orchestrator / hardened-tier obfuscation lane (unassigned) | open | emit-shape | prove fixed: minimise one of the two remaining shapes to a construct fixture, fix the emit/pass bug it exposes, and T6's hardened-tier DIVERGENT count reaches 0 on a clean local run and on CI |
| 2026-09-05 | fn-rename task (Fred: "Rename doesn't work in the UI"), landing agent/fn-rename | `src/artifact/service.ts` (`fnNameInfo`), `src/ui-server/list.ts` | An accepted `fn:N` name is applied to the renamed function and to every function the CALL GRAPH records as calling it (direct call edges plus section 2.2a points-to edges). A reference the graph does not know about -- a closure handed around dynamically with no recorded edge -- keeps the old ident until its own render is invalidated for another reason. | Testable only with a bundle that has such an unrecorded reference; none is isolated yet. The recorded-edge half is covered by `tests/ui-server/fn-rename.test.ts` (caller source and caller module view). | ui-server / rename lane | open | metrics | Fixed when a bundle function referenced with no recorded call edge shows the accepted name after a rename, with a ui-server test on that bundle (ui-server / rename lane owns it). |
| 2026-09-05 | fn-rename task, landing agent/fn-rename | `src/name-overlay/render.ts` (`renameFnIdents`) | An accepted FUNCTION name is applied verbatim: unlike the register overlay (which disambiguates with `_2`, `_3` against the frame's taken names), a `fn:N` name that collides with an unrelated binding in a caller frame shadows it in the RENDERED text. Display only -- nothing is recompiled from this text without the user asking -- but the rendered code can then be misleading. | Needs a decision first: silently suffixing a name the human typed is arguably worse than showing the collision. Deferred to the rename-tool owner rather than guessed at. | ui-server / rename lane (needs Fred or the Design-D spec owner) | open | metrics | Fixed when the rename tool has a ruling on function-name collisions (suffix like the register overlay, or surface the collision in the dialog) and a test pins whichever it is (needs Fred or the Design-D spec owner). |

## Resolved — 107 rows

| date | source | component | description | resolution | owner | status | cluster |
|---|---|---|---|---|---|---|---|
| 2026-09-05 | new construct fixture 78-class-implicit-derived-ctor (agent/super-forward), T2 trace DIVERGENT at record 0 | `src/runtime/helpers.ts` | `__hbc_b_applyWithNewTarget` declared THREE parameters (`fn, args, newTarget`) while `src/emit/lower.ts` lowers the `applyWithNewTarget` CallBuiltin with the builtin's real FOUR (`fn, argArray, thisArg, newTarget`, HermesBuiltin's own signature). The helper therefore read the receiver as the newTarget, so every spread super call -- the lowering of an explicit `constructor(...a) { super(...a); }` -- threw `TypeError: undefined is not a constructor` at run time. Present since the helper was written; no committed fixture exercised a spread super call until now. | FIXED 2026-09-05 by agent/super-forward: the helper takes all four parameters and ignores the receiver on the construct path, exactly as `__hbc_b_applyArguments` does. Regression test: the new construct fixture `tests/fixtures/constructs/78-class-implicit-derived-ctor` (its `Explicit` class), whose T2 verdict in `tests/gate/decompile/equivalence.test.ts` was `trace=DIVERGENT (traces diverge at record 0)` before the fix and PASS after, at v98 and v99. | M4 emit owner | fixed | emit-shape |
| 2026-09-05 | rung `super-call` landing (agent/super-call), refusal R-SC6 | `src/passes/super-call` | An IMPLICIT derived constructor (`class Puppy extends Dog {}`) lowers to `return __hbc_b_applyArguments(arguments, Object.getPrototypeOf(_eD_S), undefined, new.target);` -- a builtin forward of the caller's whole `arguments` object, not a `Reflect.construct` site. `super-call` refused it (R-SC6) and left the helper call, so such a class printed with no `super()` at all. Rebuilding it needed a PARAMETER-LIST change, which no rung in the ladder did. | FIXED 2026-09-05 by agent/super-forward (spec 28 section 9): `foldForwardBody` rebuilds the forward as `constructor(...args) { super(...args); }` -- the helper's reachable branch is `Construct(getPrototypeOf(<this class>), <the whole argument list>, new.target)`, which is ECMAScript 15.7.14 step 14's default derived constructor statement for statement. New refusals R-SC8 (an operand is not the constructor's own) and R-SC9 (the forward is not the whole body, `arguments` is read elsewhere, or a parameter is declared) keep the correctness proof. The rebuilt constructor drops the `"use strict"` directive: ES2024 15.2.1 forbids a directive prologue on a non-simple parameter list, and a class body is strict already. No framework surgery was needed -- `Param.rest` and `spread-rest`'s writer already do parameter lists. Tests: `tests/gate/passes/super-call.test.ts` (fixture 33's `Puppy` now folds; new fixture `tests/fixtures/constructs/78-class-implicit-derived-ctor` pins that the implicit constructor and an explicit `constructor(...a){super(...a)}` are DIFFERENT lowerings, the latter still refused) -- the fixture-33 assertions were inverted deliberately, as this row's own verdict required. | M5 ladder (class rungs) | fixed | passes |
| 2026-09-05 | object-literal rung landing (spec 20 §7 refusal table), `object-literal-computed-key` | `src/passes/object-literal/match.ts` | `PutOwnByVal`/`DefineOwnByVal` (an object-literal *computed* key, `{[k]: v}` -- unlike `PutNewOwnById`/`PutById`, this opcode is only ever emitted for literal-syntax properties, never for a later plain `o[k] = v` assignment statement, which compiles to a full `[[Set]]` `PutByVal` this rung does not touch) was refused unconditionally: the key expression would have to move to the literal along with the value, and a computed key can alias an earlier-declared literal key at runtime, which this rung cannot prove either way. | **Fixed 2026-09-05** (agent/objlit-computed). A `PutOwnByVal`/`DefineOwnByVal` store whose key `expr-rebuild` left as a literal (a compile-time-constant key, inlined the same way any other single-use temp is) folds through the existing `OWN_DEFINE`/`storeOf` path unchanged -- both opcodes were simply added to `OWN_DEFINE`, exact `CreateDataProperty` semantics like every other own-define. A store whose key is still a genuinely dynamic expression (a register, a free-variable read, a `member` chain, a call `expr-rebuild` already proved safe to inline at exactly that position via its own R1a/R1b adjacency rule) is handled by a new `computedStoreOf`/`renderComputedKey` path: the key is rendered into `ObjectProp.key`'s string field at ASSIGNMENT precedence (`src/passes/ast.ts`'s new `renderComputedKey`, the same D12a print.ts re-export gap `printProgram` already uses -- `ClassMember.key`'s `classMemberKey` renderer is the precedent), and the fold is allowed once both the key and the value pass the same "does not read or write the object's own register" check every other store's value already gets. The aliasing hazard is closed structurally, not by trying to prove non-aliasing: once a computed key has been folded into the run, no LATER *plain*-key store may fold (a `sawComputedKey` flag gates it) -- a plain key's `props[at] = ...` replace-in-place trick keeps an existing entry's *printed position*, and if that store chronologically follows a computed key that might alias the very same runtime key, printing the plain store's replace at its earlier position would let the wrong write win (worked example in `match.ts`'s `sawComputedKey` doc comment and in `docs/specs/passes/20-object-literal.md` §7); a fresh computed-key `props.push` never has this problem since it always preserves program order relative to every earlier entry. Tests: 8 new unit tests in `tests/gate/passes/object-literal.test.ts` (register/free-variable key, member-of-const-chain key, call-result key, plain-then-computed both folding, two computed keys in a row, the aliasing refusal, key/value reading the half-built object refused) plus a new construct fixture `tests/fixtures/constructs/77-object-literal-computed` (four functions: computed key from a parameter, from a call result, a plain key followed by a computed one, and the aliasing-refusal control) at all 5 HBC versions, structurally asserted (fold count, aliasing refusal) the same CONSOLIDATION §B item 7 way as `63-object-literal`'s own tests, plus T2 (`tests/gate/decompile/equivalence.test.ts`) green with 0 divergent across the whole gate corpus including the new fixture. | M5 ladder (Sonnet 5, agent/objlit-computed) | fixed | passes |
| 2026-08-31 | adversarial fixture metadata: 02-proxy-trap-counting, 06-closure-loop-var-vs-let | `tests/fixtures/adversarial/<name>/versions.txt` | both fixtures' committed `versions.txt` blanket-claims `v94/v96/v99: FAILS`, but all three `.hbc` files compile and run fine under every Hermes VM — the file is simply stale/wrong. `src/harness/tiers.ts`'s `discoverAdversarialInputs` deliberately does not consult this file for exactly this reason. | Fixed 2026-09-05 (orchestrator): both `versions.txt` rewritten. 02: VM trace matches expected.txt at v94/v99 (no v96 VM). 06: not a compile failure but a d14-legit divergence -- the Hermes VM prints `let results: 3,3,3` (one `let` binding per loop, not per iteration) where Node prints `0,1,2`; recorded in the file. `readVersionsTxt` only parses `vNN: FAILS - reason` lines, so the prose lines are inert; no code change, no test (metadata only, `discoverAdversarialInputs` never consulted the file). | fixture lane | fixed | harness |
| 2026-09-05 | object-literal rung landing (spec 20 §7 refusal table), `object-literal-putbyid` | `src/passes/object-literal/match.ts` | A `PutById`/`PutByIdLoose`/`PutByIdStrict`/`TryPutById` run on a fresh `{}` was refused unconditionally as "a full [[Set]], not an own-property define" — true in general (an inherited accessor or non-writable data property would observe it where a literal never would), but wrong for the common case: on a genuinely fresh object where nothing has run yet that could have changed `Object.prototype`, `OrdinarySet` finds no own property, finds nothing relevant on the prototype either, and falls through to `CreateDataProperty` — exactly `[[DefineOwnProperty]]`'s own outcome. `63-object-literal` fn `selfRead`'s `.a` store (and the `table`/`point`/etc. real-world shape of "build via `o={}`; `o.a=v`; …") is this exact case at every one of the 5 compiled versions. | **Fixed 2026-09-05** (agent/objlit-interleaved). `PUT_BY_ID` (`PutById`/`PutByIdLoose`/`PutByIdStrict`/`TryPutById`) now folds like `OWN_DEFINE` under a locally-provable condition: the key is not `__proto__` (still always a `[[Set]]` on `Object.prototype`'s own accessor, never a define), and nothing in the run so far — including the store's own value expression — has an `effectSequence` (`ast.ts`'s call/`new`/member-read/… detector). `runHasEffect`, threaded through the match loop, is true from the first such value onward; a `PutById`-family store only folds while it is still false. This is a *local* proof over the run being folded, not a whole-program one: a call earlier in the same function, before the object was even created, is out of scope the same way it always was for every other precondition in this rung. Tests: 3 new unit tests in `tests/gate/passes/object-literal.test.ts` (folds a two-property `PutById` run on a fresh object; refuses the `__proto__` key even there; refuses a `PutById` store once an earlier own-define's own value — a call — could have reached `Object.prototype` first), plus the existing per-version fixture test's shape-D assertion updated (`.a` now folds, `.b` still refuses — it reads the half-built object, an unrelated, still-open precondition) confirmed on the real fixture at all 5 versions. `docs/test-count-baseline.json` bumped 1557 -> 1561 (also covers the `object-literal-interleaved` tests landed in the same commit). | M5 ladder (Sonnet 5, agent/objlit-interleaved) | fixed | passes |
| 2026-09-05 | spec 23 (`arguments-form`) evidence pass, reading `src/emit/lower.ts` `ReifyArguments` against `src/runtime/helpers.ts` §8 | `src/runtime/helpers.ts` `__hbc_arguments` (the reification helper) | The helper returns a FRESH unmapped copy on every call, so two `Reify*` instructions in one function print two distinct objects where Hermes reifies ONE arguments object. `var a = arguments, b = arguments; a === b` is `true` under Hermes and `false` in the decompiled output. Object identity is the only observable difference; contents and `length` agree. Real: `49-arguments-object` has 3 reify sites at v98/v99, 2 at v84/94/96. | **Fixed 2026-09-05** (agent/args-identity). Shipped the fixture this row queued -- `tests/fixtures/constructs/73-arguments-identity` (two bare `arguments` reads compared by `===`, `Array.prototype.slice.call` on each, a strict-mode function with the same identity check) -- and confirmed it DIVERGED at every version before any fix, PASSED at every version after. The `arguments-form` rung (already landed, see the row below) covers most of this, but the fixture surfaced a second bug in its own rewrite: `src/passes/arguments-form/rewrite.ts`'s `inlineSingleUseTemp` only checked the statement immediately after a reify temp's definition for a read of it, not the rest of the function -- `sliceEach`'s `let r0 = arguments; r2 = slice.call(r0); … r1 = r0;` (two reads of one reify) folded the definition away after seeing only the first read, leaving the second read (`r1 = r0;`) referring to a register nothing assigns any more (a silent `undefined`, observed as an uncaught `TypeError` at runtime, not merely an identity mismatch). Fixed by extending the check to the rest of the statement list, stopping at the first later WRITE to the same name (a later, unrelated reuse of the register name -- 49-arguments-object's `toArray` reuses `r0` for a `,` separator -- must still fold). Also found and documented, not fixed (D14-legit, no code bug): a fourth candidate shape ("arguments[i] read after a parameter write in sloppy mode") was tried and dropped from the fixture per this row's own "only if the harness's Hermes VMs agree with Node" condition -- every non-duplicate variant tried made the real Hermes VM (v84/v96) disagree with plain Node on whether the parameter write survives at all, a genuine compiler dead-store-elimination difference (`docs/LOWERING-CATALOGUE.md` row 16, reconciled in the same commit). Regression tests: `tests/gate/passes/arguments-form.test.ts` (structural: no `__hbc_arguments(` remains for 73-arguments-identity at any version; two new `inlineSingleUseTemp` unit tests, one RED before the fix reproducing the exact `sliceEach` shape, one confirming the original single-read fold still works). | `arguments-form` lane (Sonnet 5, agent/args-identity) | fixed | passes |
| 2026-09-05 | orchestrator gate 92595de, 2026-09-06 (F24-5 landing fallout on react-navigation-example-0.85.3) | src/split/index.ts (referenced-but-undeclared pull scan) | Under --split, module_523.js of react-navigation-example-0.85.3 ballooned from 46 KB to 28 MB and the whole project from 24.26 MB to 52.43 MB, and tests/gate/split/segregate.test.ts's NO --deps-report navigator pin went 6 -> 7 because module 523 then matched the createDrawerNavigator shape. Root cause: the scan that decides which _fnN a module file references-but-does-not-declare ran over the printed text INCLUDING comments, and src/emit prints scope-check comments naming functions by identifier (emitted identifier "_fn13844" is not declared in any enclosing scope (module > _fn0 > _fn525 > ...)). That mention of _fn0 made module 523 pull in the bundle's GLOBAL function -- harmless-looking before F24-5 (26054f9) because the global's body was small, catastrophic after it, since hosting capture-nothing functions inside their creator made the global the lexical parent of most of the bundle. The F24-5 hosting itself is correct and stays. | Fixed 2026-09-05 (agent/f24-5-regress). New exported scanFnIdentifiers(text) in src/split/index.ts returns the _fnN a printed body declares and references counting only occurrences in CODE: a comment-range scanner (string/template aware, so a // inside a string literal never blanks real code) filters matches inside line and block comments. The pull loop additionally never copies the bundle's global function into a module file (its body is the whole program's __d registrations, never a module-local helper) and records a diagnostic instead. After: whole split 24.12 MB, module_523.js back under 372 KB, segregate NO-deps navigator count 6 with the pin untouched. Tests: tests/gate/split/comment-ref-pull.test.ts (new -- comment mention is not a reference; declaration inside a comment is not a declaration and // inside a string does not blind the scan; structural bar on rn-template-0.72 and, existsSync-guarded, on react-navigation-example-0.85.3: no single split file may hold half the project and no file may declare the global function; the last one fails at 4685746 with exactly module_523.js 28213920 of 52430611 bytes). tests/gate/split/segregate.test.ts's NO --deps-report test now t.skip()s VISIBLY when the bundle is absent -- its silent return is what hid this regression from the gate that landed F24-5. | split lane, Claude Opus 5 (agent/f24-5-regress) | fixed | real-app |
| 2026-09-01 | E2E tier 1 -- buckets diff:LoadFromEnvironment(imm) / diff:CreateFunctionEnvironment(imm), react-navigation (v98) and rn-template (v94) | src/passes/expr-rebuild (captured-variable evaluation order; originally filed against src/emit declaration order) | `arr[i++]` with the array and the index captured by the SAME environment at DIFFERENT slots. The M4 baseline emits the base's env-slot load first, exactly as the original bytecode and as ECMAScript member-expression evaluation order both require (`arr[i++]` evaluates `arr` before `i++`). expr-rebuild then folded that load forward into the member expression, past the store-back of the incremented index: value-safe (two slots are two independent bindings) but it reverses the `LoadFromEnvironment` pair, so the recompiled bytecode no longer matches the original. The first hypothesis (declaration order of the `let _e<env>_<slot>` prologue) was disproved by agent/env-slot-order's hermesc probing and is recorded in that agent's report. | Fixed in b4e6270: new expr-rebuild refuse reason `env-slot-order` (a pure value that reads an env slot may not travel across a store into a slot of the same environment; cross-environment moves are untouched), spec docs/specs/passes/02-expr-rebuild.md. Regression tests: new construct fixture `tests/fixtures/constructs/71-env-slot-captured-index` (arr[i++], obj[k++] = v, fn(a, n++), each with base and index in one environment at different slots, five versions) with a per-version decompile+recompile slot-sequence assertion, plus a targeted rn-template-0.72 fn#224 assertion, both in `tests/gate/passes/env-slot-order.test.ts`. Measured E2E tier 1 passes-on, same worktree, guard off vs on: react-navigation 5745 -> 6180 IDENTICAL of 14437 (39.79 -> 42.81 percent), 435 functions newly IDENTICAL and 0 newly DIFFERENT; rn-template `diff:LoadFromEnvironment(imm)` 23 -> 21 of 4125 (fn#224 left the bucket). The `diff:LoadFromEnvironment(imm)` total on react-navigation rose 546 -> 862 purely by migration from `diff:TryGetById(string)` (315 functions that now match further) - tracked as its own open row dated 2026-09-05. | M4/M5 emit owner | fixed | emit-shape |
| 2026-09-01 | E2E tier 1 -- buckets diff:CreateEnvironment/LoadConstUndefined (78 rn-template, 856 Service NSW), diff:CreateEnvironment/LoadParam (103 rn-template) | src/passes/index.ts (hoistRegisterInits, F26) | The per-function let r0, r1, ..., rN; prologue compiled to N x LoadConstUndefined at entry that hermesc -O does not remove; functions with many registers never round-tripped even when the body did. | Fixed 2026-09-05 (agent/reg-prologue). New finaliser hoistRegisterInits (src/passes/index.ts, F26) runs after F10's pruneRegisterDecls, gated the same way (applied.length > 0, so --passes=none/PL-05 is untouched): for the leading register decl, a name whose first occurrence in the top-level statement list is a plain non-self-referential assignment becomes an inline let name = value; in place, instead of staying hoisted uninitialised. A register read on some path before its top-level write is left declared in the prologue (soundness: the read must still see undefined, never a TDZ), and a write nested inside an if/loop/try/switch is never hoisted (future work, not required for soundness). Candidates are every name in that decl (not just ones still literally rN), so the name-overlay's render-is-a-pure-alpha-rename invariant (spec rename-tool-DESIGN-D §11.3) holds regardless of which label a register carries. Verified on tests/fixtures/constructs/19-var-hoisting v94: demo's leading 14-register decl (let r0, r1, ..., r13;) disappears entirely with passes on. Real-bundle measurement (rn-template-0.72 v94, passes-on, --normalise strong): the diff:CreateEnvironment/LoadConstUndefined bucket was already 0 before this change on the current tree (superseded as the "first differing opcode" by other, unrelated fixes landed since 2026-09-01) and stays 0 after; IDENTICAL 1822/4125 (44.17%) unchanged on both sides of this diff -- the underlying prologue defect is fixed (confirmed by direct decompiled-source diff and by the new tests below), it just is not the bundle's current bottleneck. Tests: tests/gate/passes/ast.test.ts (six new hand-built-AST unit tests on hoistRegisterInits: full hoist, partial hoist, read-before-write left alone, self-reference left alone, nested-write left alone, no-decl/no-register passthrough), tests/gate/passes/reg-prologue.test.ts (new: 19-var-hoisting v94 prologue gone with passes on / unchanged with --passes=none / no register invented or dropped across the toggle). Updated tests broken by the shape change (not weakened -- each now asserts the same property in a form that tolerates either declaration form): tests/gate/passes/var-naming.test.ts's declaredRegisters helper also counts inline let rN = ...; forms; tests/gate/passes/expr-rebuild.test.ts's 19-var-hoisting D-a shape test asserts the 5 base registers by scanning the whole function body instead of one decl line; tests/gate/passes/object-literal.test.ts's negative-control regex allows an optional let/const/var prefix on the intervening store. docs/specs/passes/07-var-naming.md section 9 documents F26. tests/gate/passes/pipeline-speed.test.ts's hash pin is unaffected (it hashes decompileTree's stage-A tree; this fix is a stage-B/emit finaliser). No golden moved: no committed golden captures whole decompiled-JS output (tests/gate/docs/testing-rules.test.ts item 7); the only golden failures seen locally are the pre-existing missing-file noise for fixtures 67/70 (disasm goldens, unrelated). | M5 ladder (var-naming owner), Claude Sonnet 5 (agent/reg-prologue) | fixed | emit-shape |
| 2026-09-05 | e2e-gate task (making the Playwright suite a real gate), ui/e2e/graph-open-in-listing.spec.ts | ui/src/graph/GraphPane.tsx, ui/src/graph/store.ts openGraphTargetInListing | Double-clicking a real non-focus FUNCTION neighbour node in the graph pane while it was maximised left data-graph-maximised="true" -- the listing never became visible. This test was previously masked end-to-end by an unrelated selector bug (the helper could pick an ext:m:N external-reference node, whose id never matches /^fn:\d+$/, so the test never got far enough to reach the maximise assertion on a real run); fixing that selector let the real failure surface. | Fixed 2026-09-05 (commit 6cf3bde). Root cause: GraphPane.tsx's onNodeClick re-rooted the graph on the clicked node synchronously via focusGraphNode, relaying every node (including the one under the cursor) out from under the second half of the native dblclick gesture before the browser could synthesize it, so onNodeDoubleClick (which calls store.ts's openGraphTargetInListing to select + un-maximise) never ran for a non-focus target -- the FOCUS node case worked because clicking it never calls focusGraphNode in the first place (m.isFocus guards it). Fix: onNodeClick now delays its focusGraphNode call by one beat behind a cancellable timer; onNodeDoubleClick cancels that pending timer and applies the re-root itself once the click sequence has already resolved, preserving docs/UI.md's documented "double-click ... jumps the listing and re-roots the graph there". Also fixed a related gap the un-fixme'd test surfaced: CodeView.tsx's data-selected-line attribute (bur 13's keyboard-navigation anchor) was previously seeded only by a literal editor click, so landing on a function from the graph (or any non-click navigation) left arrow-key navigation starting at line 1; it is now also seeded from highlightLine whenever a real line is shown. Test: ui/e2e/graph-open-in-listing.spec.ts's "dblclick a neighbour node" test is un-fixme'd (its own comment names it as the regression test) and its "which fn is shown" assertion is scoped to the breadcrumb testid rather than a page-wide text search, which turned out ambiguous once the fix let the test reach real DOM state. Full ui/e2e suite: 77 passed, 4 skipped, 0 failed. | UI lane (Claude Sonnet 5, agent/graph-maximise) | fixed | real-app |
| 2026-09-05 | UI QA report BUG 1 follow-up (ui-qa-report.md), fix in agent/ui-followups | ui/src/workers/wire.ts, ui/src/App.tsx | The UI polled GET /api/sessions for presence but never called POST /api/sessions to register a session for itself, so workersApi.enqueue() omitted createdBy rather than sending an id nothing owns -- every AI job the UI queued was attributed to no session (jobs.created_by IS NULL), and the jobs rail could not show who queued a job. | Fixed 2026-09-05 (agent/ui-followups). wire.ts's new initUiSession() registers ONE kind:"human"/who:"ui" session on load (App.tsx calls it once, alongside initUrlSync) and heartbeats it every 10 s (spec 23 section 3's TTL default is 30 s); enqueue() now spreads a createdByField() helper that reads the registered id back through uiSessionId(), never a hardcoded literal, and omits the field before registration settles or in mock mode. Tests: tests/ui-server/workers-routes.test.ts's new "a UI-registered session (kind human, who ui) round-trips to a job whose created_by is that session" test (open the session the way the UI does, enqueue, run to done, confirm created_by); tests/gate/ui/ai-jobs-createdby.test.ts extended, not weakened -- both original assertions still hold unmodified -- with two new tests pinning initUiSession()'s existence and that enqueue() spreads the helper rather than inlining a literal. | ui lane (Claude Sonnet 5, agent/ui-followups) | fixed | real-app |
| 2026-09-05 | fn-rename task, landing agent/fn-rename; fixed in agent/ui-followups | `src/mcp/leads.ts` (`searchFunctions`) | `/api/search/functions` matched the BYTECODE name only. It displayed the accepted name, so a renamed function showed up correctly when some other term found it, but typing the new name found nothing. | Fixed 2026-09-05 (agent/ui-followups). searchFunctions now matches EITHER the bytecode name or artifact.acceptedFnName(fn) (a memoised map lookup, cheap per scanned row -- overlayNameOf stays matched-rows-only per its own docstring, a real per-fn index query) -- so a search for the old name keeps working and a search for the new one now finds the row too. Test: tests/ui-server/fn-rename.test.ts's new "/api/search/functions matches the accepted (post-rename) name, not only the bytecode name" test (would have failed before the fix, since the pre-fix scan only ever tested the bytecode name against the query). | ui-server / search lane (Claude Sonnet 5, agent/ui-followups) | fixed | metrics |
| 2026-09-05 | M5 rung 18 (`optional-chain`) isNullSentinel-fix follow-up, measuring full recovery on `48-optional-chaining-nullish` at v99 (moved from Open, base-guard-elision follow-up) | `src/passes/optional-chain/match.ts` (`matchChainGuard`) | `L0` (`user?.profile?.name`) stayed raw guard-block code at v99: a genuinely dead `r1 = undefined` store (written once, function-wide, never read again anywhere) sat interleaved between the guard's spilled compare and its own reset, at a statement offset `matchChainGuard` matched with a fixed `idx+1`/`idx+2` step, so it saw an extra statement where it expected exactly `reset; if (rC) break L;` and refused. A second, independent occurrence of the same shape (an unrelated dead reset between the compare and the reset, this time at v96, blocking the *outer* base guard of the same `user?.profile?.name` chain and leaving it unfolded into a raw `if` even though the inner `.profile?.name` sub-chain matched on its own) was found while probing per-version parity for this row's own fixture. | Fixed 2026-09-05: added `skipDeadResets` to `src/passes/optional-chain/match.ts` -- before matching the guard's real reset (in either the v94 inline or v99 spilled-compare shape), the matcher now skips forward over zero or more statements of the form `rX = undefined` whose register `rX` is provably dead in the whole function (`identUses(fnBody, rX)` shows exactly one write, the reset itself, and zero reads). A register this holds for can never be the run's own `rRes` (which is always read again later, outside the run), so the skip can never mask the real reset -- only statements that could not have been it. The skipped statements are absorbed into the matched span exactly like any other bookkeeping statement (`rewrite.ts` drops `[startIndex, endIndex)` wholesale), sound because a write with no reads anywhere has no observable effect. Measured on the real `48-optional-chaining-nullish` binary: v96 goes from 17 to 18 `?.` occurrences with its one residual raw guard block gone; v99 goes from 9 to 10 `?.` occurrences (`user?.profile?.name` now recovers) with its residual raw guard block also gone. Every one of the 5 compiled versions (84/94/96/98/99) now has zero residual `if (rX) { break L; }` guard blocks for this fixture -- the remaining `?.`/`??` token-count differences across versions are fully explained by a newer hermesc's own base-guard-elision and constant-folding of statically-non-nullish `??` left sides, not a decompiler gap (docs/lowering/optional-chaining.md §7). Tests: two new unit tests in `tests/gate/passes/optional-chain.test.ts` (a positive case, an interleaved dead reset does not defeat the guard; a negative case, an interleaved reset that IS read elsewhere is never treated as dead and still refuses) plus the v99 fixture test's ceiling strengthened from `bareGuardCount <= 1` to `=== 0` and a new cross-version test asserting zero residual bare guards at every one of the 5 compiled versions. `docs/test-count-baseline.json` bumped 1504 -> 1507. | M5 pass-ladder lane (Sonnet 5, lean worker, agent/optchain-guard) | fixed | passes |
| 2026-09-05 | spec 24 (`class-recover`) landing, refusals `ctor-not-in-body` / `method-not-in-body` (R-C11/R-C12) | src/emit/index.ts (`parentOf`), F24-5 | A class method or constructor that captures nothing has `closureEnvOf === null`, so `emitModule` treats it as having no creation site and emits its declaration at MODULE level with an `// orphan` comment, even though its `CreateClosure`/`CreateBaseClass` site is plainly inside a known function. The `class-recover` rung can only move a declaration that is in `ctx.fnBody`, so sub-forms C1, C3 and C4 (fixtures 32, 34, 36) refuse: the class head is found and then abandoned because the body it must hold is out of reach. C2 (fixture 33, whose methods capture `_e0_*`) recovers normally. | Fixed 2026-09-05 (sha 26054f9, F24-5): `emitModule` no longer treats a capture-nothing function as an orphan. `closureEnvOf(f) === null` is the env graph STATING that every creation site passed an undefined environment operand, so the body is scope-independent; when all of its sites are in one function, that function hosts the declaration (hoisted to the top of the body, so it precedes every use) and the `// orphan` comment is gone. Genuinely unresolvable functions keep `resolveOrphanHosts`'s cost rule. Fixtures 32, 34 and 36 now have their constructor and method bodies in `ctx.fnBody` and the three `class-recover` acceptance tests (C1/C3/C4) run unskipped and green. A second bug surfaced with them and is fixed in the same commit: the class-shape checker read the rewritten head at `rebuilt[<count of preceding deletions>]` instead of `rebuilt[headIndex - <that count>]`, so on 32-class-basic it substituted the following statement's effects and counted that statement twice, abandoning the group. Regression tests: the two F24-5 tests at the end of `tests/gate/passes/class-recover.test.ts` (no `class-recover` abandonment on fixture 32; no `// orphan` comment and every method/constructor nested inside fn#0 at `--passes=none`) plus C1/C3/C4 themselves. | orchestrator (F24-5, spec 24 section 2; PUSHBACK P-38) | fixed | passes |
| 2026-09-02 | M5 rung batch 3 (`default-params`), confirming the rung at v99 | `src/emit/index.ts` (orphan-closure assembly) + `src/passes/index.ts` (`astPassHook`) | A stage-B rung that needs to replace a *different* function's own `func` node (its `params`, not just its `body` — `default-params`, and any future rung with the same need) can only do so by finding that node as a member of some *other* function's own statement list, since `astPassHook` hands every pass `fn.body` alone, never `fn`. At v99, `51-default-params`'s `greet`/`chainedDefaults`/`defaultUsesFunction` are emitted as "orphan: no closure creation site was found" — `src/emit/index.ts` inserts them directly into the module's `orphans` list at final assembly, never as a member of any function's own body — so no stage-B pass ever gets a chance to touch their `params`. `default-params` recovers correctly on the one non-orphan function (`withSideEffectDefault`, confirmed via a real closure-creation site) and on every function at v94 (none are orphans there for this fixture); the fixture's trace-oracle verdict is unaffected (the untouched functions are still correct, just less readable — the guard block survives as body code). | Fixed 2026-09-05 (sha 26054f9, F24-5) as the row's own "prove fixed" (a) asked: `src/emit/index.ts` gives these functions a real host. `greet`, `chainedDefaults` and `defaultUsesFunction` capture nothing and are created in fn#0, so at v98/v99 they are now declared inside fn#0 and `astPassHook` hands `default-params` the statement list holding their `func` nodes. Measured: `51-default-params` at v98 and v99 emits all four functions with defaults (`greet(a1, r1 = "Hello, " + a1 + "!")`, `chainedDefaults(r2 = 1, r5 = r2 + 1, r3 = r2 + r5)`, `defaultUsesFunction(a1, r2 = a1 * 2)`, `withSideEffectDefault`), zero `// orphan` comments. Covered by `tests/gate/passes/default-params.test.ts` (unchanged, now exercising every function) and by the F24-5 placement test in `tests/gate/passes/class-recover.test.ts`. | M5 pass-ladder lane (unassigned) | fixed | passes |
| 2026-09-02 | M5 rung 17 (`spread-rest`) implementation, confirming the rung at v98/v99 | `src/emit/index.ts` (orphan-closure assembly) — same root cause as the `default-params` row above, a second rung hitting it | S3 (rest parameter) needs the statement list *containing* the target function's own `func` node, exactly like `default-params`'s param-list edits. At v98/v99, `42-rest-params`'s `combine`/`restOnly` are emitted as top-level orphan function statements ("no closure creation site was found") rather than members of `fn#0`'s body list, so `spread-rest`'s S3 never gets a site to match them from — `__hbc_b_copyRestArgs` survives unrewritten in those two functions at v98/v99 only (confirmed: `42-rest-params/v94.hbc` and `/v96.hbc` fully convert all rest-param/spread sites; `/v98.hbc` and `/v99.hbc` leave exactly these two call sites). S1/S2/S4 (array/call/object spread, all statement-list-local, no `func`-node lookup needed) are unaffected and fully convert at all five versions. The trace-oracle verdict is unaffected (the untouched functions are still correct, just less readable). | Fixed 2026-09-05 (sha 26054f9, F24-5), same root cause and same fix as the `default-params` row above. `combine` and `restOnly` capture nothing and are created in fn#0, so S3 now reaches their own `func` nodes at v98/v99. Regression tests updated to the fixed behaviour in `tests/gate/passes/spread-rest.test.ts`: `42-rest-params` is now asserted at v98 AND v99 to show `function \w+(a1, ...\w+)` and `function \w+(...\w+)` with zero helper call sites (was: exactly 2 helper calls, the two orphaned functions'), and `40-spread-array` expects zero helper call sites at every version (was: 1 at v99, `variadicSum`'s). | M5 pass-ladder lane (unassigned) | fixed | passes |
| 2026-09-05 | UI QA report BUG 1 (ui-qa-report.md), Fred: "there is none of the AI features working" | ui/src/workers/wire.ts, src/ui-server/workers-routes.ts | Every AI action (Suggest name, Explain) 500ed: `wire.ts` hardcoded `createdBy: "ui"` on every POST /api/jobs enqueue, but `jobs.created_by` is `TEXT REFERENCES sessions(id)` (src/projdb/schema.sql) with FK enforcement on, and no session ever has the literal id "ui" -- so sqlite raised FOREIGN KEY constraint failed and the server answered 500 for both queueJob() call sites (registry.ts). The rest of the pipeline (queue, HeuristicBackend, suggestions, promote) worked fine once the enqueue itself succeeded. | Client: `wire.ts`'s `enqueue()` no longer sends `createdBy` at all -- the UI does not register its own session yet (see the new Open row above for that follow-up), and `created_by` is nullable, so omitting it is a real, supported shape, not a workaround. Server: `POST /api/jobs` in `src/ui-server/workers-routes.ts` now validates an explicit `createdBy` against `Presence.session(id)` before it ever reaches sqlite, answering 400 with a named reason for an unknown id instead of a raw constraint-failure 500. Checked `/api/suggestions/promote` and `/reject` for the same class of bug: their provenance `who` field is free text (no `sessions(id)` FK on that column), so they were not affected; no change needed there. Regression tests: `tests/ui-server/workers-routes.test.ts` (unknown createdBy is 400 with reason naming the field; a live session id, or no createdBy, enqueues and runs to done) and `tests/gate/ui/ai-jobs-createdby.test.ts` (the client source never hardcodes a createdBy literal again). | ui lane (Claude Sonnet 5, agent/ai-jobs) | fixed | real-app |
| 2026-09-05 | e2e-gate task, found running the pre-existing e2e suite for real (agent/e2e-gate, cut from 68abee2) | `ui/src/components/CommandPalette.tsx` | The command-palette `Command.Item` for every registry action set `value={it.id}` (e.g. `"view.rawHermes"`, `"navigate.strings"`) instead of the visible title (`"Show raw Hermes"`, `"Find string uses..."`). cmdk's built-in fuzzy filter (active whenever `shouldFilter` is true, i.e. outside ":" command mode) scores the `value` prop, not the rendered children -- so typing the title text a user actually sees (spaces, the "..." ellipsis) could never match the dotted id, and the palette silently showed no results for almost every action typed by its own label. This was the previously-open "spec 26 L10 landing" row (command palette flow, `ui/e2e/smoke.spec.ts` timeouts on `getByText("Show raw Hermes")`/`getByText("Find string uses...")`) -- root cause found and fixed by this task, superseding that row's "not yet minimised" note. | Fixed 2026-09-05 (agent/e2e-gate): `Command.Item`'s `value` changed to `it.title`; dispatch is unaffected since `onSelect` already keys off `it.id` explicitly. Both previously-timing-out smoke tests (`"fold gutter present; ... Show raw Hermes opens the disasm panel"`, `"Strings tab: search, expand a hit, jump to a use, centre pane follows"`) pass. | UI lane (Sonnet 5, agent/e2e-gate) | fixed | real-app |
| 2026-09-05 | e2e-gate task, new outcome test `ui/e2e/ai-suggestions.spec.ts` ("Suggest name enqueues a job that reaches done") | `ui/src/workers/wire.ts` `workersApi.enqueue` | Every "Suggest name"/"Explain" click from the AI tab (`ui/src/panes/WorkersPane.tsx`) sent `POST /api/jobs` with `createdBy: "ui"` hardcoded -- a literal string, never an actual open session id -- while `jobs.created_by TEXT REFERENCES sessions(id)` (`src/projdb/schema.sql`) enforces that it be a real row in `sessions`, which the UI never opens (only the worker pool calls `presence.open` in `startWorkers`). Every job the UI ever queued therefore 500'd with "FOREIGN KEY constraint failed", reproduced directly with `curl -X POST /api/jobs -d '{"kind":"suggest-name","input":{"fn":74},"createdBy":"ui"}'`. This is the AI half of Fred's report that evening ("There is no of the AI features working") -- the rename half is the separate agent/fn-rename bug. | Fixed 2026-09-05 (agent/e2e-gate): dropped the invalid `createdBy: "ui"` from the enqueue request body -- `created_by` is nullable (no `NOT NULL`) and is never read back by any UI code (`grep`-confirmed), so omitting it is honest rather than inventing a session-opening flow this task did not scope. `ui/e2e/ai-suggestions.spec.ts`'s two tests (job reaches `done` and a suggestion row appears; Accept renders the suggested name in the code pane) both pass with the fix, both would fail with the FK error before it (verified). | UI lane (Sonnet 5, agent/e2e-gate) | fixed | real-app |
| 2026-09-05 | specs 21+22+23 landing (merge of agent/spec23-impl with main bb46cf7), found by the full gate | src/passes/arguments-form vs src/passes/async-recovery | Cross-spec rung interaction, red on the merged branch only (each branch is green alone). Spec 25's `async-recovery` matched a driver run whose third argument is `__hbc_arguments(arguments)`; spec 23's `arguments-form` (catalogue row R10) rewrites exactly that expression to a bare `arguments`, so the driver run no longer matched and the async idiom was left as a `__hbc_b_spawnAsync` + generator at 27-async-await-basic and 28-async-await-error, v84/v94/v96. | Root cause: the stage-B hook is per *function*, innermost first -- `src/emit/index.ts` emits a nested function (and runs `opts.astPasses` on it) before its parent -- so `arguments-form` reaches the async stub's own body one whole hook invocation before `async-recovery`, which matches that stub from its *parent's* statement list, ever sees it. Registry order can never fix that: it only orders passes within one function, which is also why the earlier probe in `src/passes/driver.ts` printed nothing (that is the stage-A driver; these are stage-B passes run by `applyAstPasses` in `src/passes/ast.ts`). Fixed 2026-09-05 on agent/spec23-impl per the orchestrator's decision that `arguments-form`'s output is the canonical readable form: `src/passes/async-recovery/recover.ts`'s `isOwnArguments` now accepts BOTH operands (bare `arguments` and the raw reify call). R-A2 stays sound -- `arguments` inside the stub can only denote the stub's own arguments object, exactly as `this` does in `isOwnThis` -- and a foreign value is still refused. `yield-recovery` needed no change (it already matches a bare `argumentsObject` operand). Regression tests in `tests/gate/passes/async-recovery.test.ts`: "R-A2 accepts both arguments operands: `arguments-form`'s bare read and the raw reify call" (unit, RED before the fix with reason `this-coercion`) and "cross-spec: async-recovery still recovers 27/28 with `arguments-form` enabled (BUGS 2026-09-05)" (end-to-end, RED before). The pre-existing baseline row for the reified-arguments marker was narrowed, not weakened -- see PUSHBACK P-35. | passes lane (Claude Opus 5, agent/spec23-impl) | fixed | ladder |
| 2026-09-05 | spec 23 (arguments-form / literal-forms), section 1.1 | src/emit/lower.ts's ReifyArguments* lowering, src/runtime/helpers.ts __hbc_arguments | `__hbc_arguments(arguments)` (src/runtime/helpers.ts section 8) builds a fresh unmapped array-like copy on every call. A function with more than one Reify* site (49-arguments-object at v98/v99 has three, the Loose/Strict opcode split) therefore printed two or three distinct objects where Hermes's own bytecode reifies `arguments` once per call and every read observes the same object -- `var a = arguments, b = arguments; a === b` is `true` under Hermes and was `false` in the decompiled output. No fixture asserted this before spec 23. | Fixed 2026-09-05 (agent/spec23-impl, arguments-form rung): every accepted `__hbc_arguments(arguments)` call in a function body is replaced by the bare `arguments`/`{k:"argumentsObject"}` read, so two or three reify sites in one function now all read the same live object -- `a === b` matches Hermes. Covered indirectly by `tests/gate/passes/arguments-form.test.ts`'s fixture-49 replacement-count and R-A3 refusal tests (49's `aliasDemo` at v98/v99 has the multi-site shape and is exercised at every version); a dedicated `arguments === arguments` identity construct fixture (queued by spec 23 section 5, "the identity regression as a new construct fixture the implementer builds via tests/fixtures/build.sh") is NOT shipped in this commit -- follow-up, tracked here rather than silently dropped (no `hermesc`/VM round-trip was run for a new fixture within this task's budget). | passes lane (Sonnet 5, agent/spec23-impl) | fixed | passes |
| 2026-09-05 | spec 21 (for-in/for-of) implementation, agent/forin | src/emit/ast.ts (the stage-B `walk`/`mapStmts`/`identUses`/`freeNames`/`effectSequence`-style traversals) | The `for-in` and `for-of` stage-A rungs (src/passes/for-in, src/passes/for-of) match and check correctly against the real fixtures -- 05-for-in-object's two loops and their register identification (binding/source/setup/close) are all correct -- but the stage-B AST framework has no traversal case for the new `for-in`/`for-of` Stmt kind, so expr-rebuild/reg-split/var-naming silently do not descend into its `left`/`right`/`body`. A register renamed or inlined everywhere else a function reads it is left as its raw `rN` name inside the loop, which is not cosmetic: confirmed DIVERGENT (05-for-in-object, all 5 versions) once the rungs are registered, because a register the for-of/for-in node still names by its bytecode number can by then be holding an unrelated later value under that same number elsewhere in the function (register reuse), or the loop's own setup expression (`obj2 = obj;`) never gets folded into the loop head the way an ordinary `init`/`assign` statement would. The rungs are therefore implemented and unit-tested (9/10 and equivalent for-of coverage against hand-built trees) but deliberately left OUT of `src/passes/registry.ts` (`DISABLED_PASSES` in tests/gate/passes/imports.test.ts), and both acceptance test files (tests/gate/passes/for-in.test.ts, for-of.test.ts) stay `skip`ped -- landing needs the stage-B traversal support added first. | Fixed 2026-09-05 (same agent/forin worktree, follow-up commit): added the missing `for-in`/`for-of` cases to every stage-B traversal `src/passes/ast.ts` shares (`walk`/`mapStmts`/`stmtLists`/`spliceList`/`countUses`/`defUseWalk`/`effectSequence`/`freeNames`) plus the private duplicate traversals two rungs keep of their own (`src/passes/reg-split/match.ts`'s `indexStatements`/`computeLoopMembership`/`computeTryMembership`/`transformStmtExprs`/`transformFrame`/the reaching-defs `walkList`, and `src/passes/expr-rebuild/match.ts`'s `topLevelExprOf`/`branchVerdict`/`containsJump`) -- reg-split's web analysis and expr-rebuild's liveness scan were the two that actually produced wrong output once for-in/for-of were registered, not just missed a readability opportunity. Verified against the real Hermes VM (`tools/hermesc/v84/hermes`, `v96/hermes`): 05-for-in-object's decompiled output now matches the VM byte-for-byte at v84 (both loops converted, human/register names correctly threaded through reg-split and var-naming), and 59-jsx-runtime-calls (an unrelated fixture whose two `for...of` loops had gone DIVERGENT the moment for-of was registered, root-caused to expr-rebuild's private traversal not crediting `for-of.right` as a read) now matches too. Both rungs registered in `src/passes/registry.ts`; `tests/gate/passes/for-in.test.ts`'s skip lifted unchanged, 10/10. `for-of.test.ts` stays skipped -- for-of has its own narrower remaining gap, docs/BUGS.md's `for-of-break-handler-shape` row. | Stage-A loop-ladder lane (unassigned) | fixed | passes |
| 2026-09-01 | E2E tier 1 -- buckets `diff:GetById/LoadConstNull` (170 rn-template passes on, 1511 Service NSW) and `diff:GetEnvironment/LoadConstNull` (41 / 975) | `src/emit/lower.ts` (`LoadThisNS` lowering) | In a sloppy function `LoadThisNS` was emitted as an explicit ternary coercion of `this`, but `hermesc` compiles a bare `this` in a sloppy function to exactly that opcode -- so plain `this` is both shorter and round-trips, while the explicit form never matches. | Fixed 2026-09-05 (sha 9c91f2a): `case "LoadThisNS"` prints `f.thisExpr` bare when `f.fn.header.flags.strictMode` is false and `thisExpr` is a real receiver -- the same header flag `src/emit/function.ts` uses to print the `"use strict"` directive. Justification is ES2024 10.2.1.2 OrdinaryCallBindThis: a sloppy call has already mapped null/undefined to the global object and boxed a primitive receiver before the body runs, so the two forms denote the same value. Two cases keep the explicit coercion: a strict function (the call protocol leaves `this` uncoerced there) and an opcode-era generator body, whose `thisExpr` is the captured `__this` variable rather than a receiver. `CoerceThisNS`, which coerces an arbitrary register, is untouched. E2E evidence (this row's own "prove fixed" criterion, partial): `tools/e2e/roundtrip-corpus.ts --only rn-template-0.72 --passes on` reports 0 functions in either bucket, down from 170 + 41 (`docs/e2e/RESULTS.md`); react-navigation and Service NSW not re-measured. Effect on the construct corpus: every `? globalThis : Object(this)` disappears from the decompiled output of 05, 07, 27, 28, 29, 31, 36, 50, 54, 57 and 62 (11 fixtures, 25 sites at v99). Tests: `tests/gate/emit/loadthisns.test.ts` (four cases -- all five versions of the all-sloppy 50-this-binding, the mixed 36-class-getters-setters, a synthetic strict-vs-sloppy pair proving the branch reads the header flag, and CoerceThisNS unchanged). The async-recovery rung's baseline shape assertion moved with it, recorded as PUSHBACK P-31. | M5 ladder / emit owner | fixed | emit-shape |
| 2026-09-04 | spec 18 step 5 (concurrency proof, `tests/projdb/concurrency.test.ts`) implementation | `src/projdb/export.ts` (`writeFindingShardForRid`, called both directly and from `exportProject`'s bulk finding pass) | `writeFindingShardForRid` re-derives its target record by calling `new DbRevisionStore(db, findingAdapter).allRecords()` (a fresh SQL scan + per-record detail read of every finding revision) and then `.find()`-ing the one matching `rid`, on EVERY call — `exportProject`'s own finding loop already iterates `allRecords()` once and calls this per active record, so a full-project export is O(n squared) in finding count, not O(n). Measured directly: the concurrency test's 1000-finding sweep-scale run (500 finding writes per writer) takes ~44s; before scaling the gate-tier test down this made the naive 1000-finding run take ~123s, over the whole local test budget on its own. Correctness is unaffected (every id/content still resolves correctly, per the concurrency test's assertions) — this is a performance-only finding. | Fixed 2026-09-05: `writeFindingShardForRid` (`src/projdb/export.ts`) takes an optional 6th `record` argument — when a caller already has the `DbRevision` in hand it is used as-is, no `allRecords()` scan at all; omitted, the old scan-and-find fallback runs unchanged (still O(n) per call, for the rare single-shot caller with only a bare rid). `exportProject`'s bulk finding loop and `tests/projdb/concurrency-worker.ts`'s raw per-finding write loop (step 2, the actual hot path the sweep-scale timing measured) both now pass the record they already have. Measured after the fix: the sweep-scale 1000-finding concurrency run dropped from ~44s to ~1.2s (worker-thread spin-up plus real filesystem I/O, not literally sub-second, but a ~36x drop and comfortably inside the local budget). Correctness-only regression test `tests/projdb/finding-shard-write-cost.test.ts` counts `db.prepare` calls instead of wall-clock time (per this row's own "flaky-on-CI-hardware" concern) — RED before the fix (passing a record still cost 801 prepares at N=200, and `exportProject` at N=150 cost 91207 prepares, both scaling with N squared), GREEN after (passing a record costs a small constant, `exportProject`'s pass stays under 10*N). | project-DB lane (Sonnet 5, lean worker) | fixed | metrics |
| 2026-09-05 | QUEUE #3 `require(N)` points-to pass landing (spec 10 §2.2a) | `src/projdb/ix-write.ts` + `src/projdb/schema.sql` (no `ix_calls_resolved` table) | `index/calls-resolved.jsonl` was written and read on the JSONL artifact path only. `buildIndexRows` produced the rows for both sinks, but the project DB had no table for them, so a DB-backed (`.hbcproj`) `ArtifactService` served zero points-to edges while the same artifact on the JSONL path served them -- the two backends disagreed about `who-calls`. | Fixed 2026-09-05: `schema.sql` gains MIGRATION 5 (`ix_calls_resolved`, mirroring `ix_calls`'s shape -- `PRIMARY KEY (caller, site)`, an `ix_calls_resolved_callee` index for the who-calls inversion), `SCHEMA_MINOR` bumped to 5 in `db.ts`. `ix-write.ts`'s `writeIxRows` gains `writeResolvedCalls`, writing `IndexRows.resolvedCallRows`. `artifact-read.ts`'s `loadIndexRowsFromDb` reads them back (wrapped in try/catch, so a DB opened read-only and written before MIGRATION 5 -- never migrated forward on a read-only connection -- serves zero points-to edges instead of throwing, same fallback shape as the JSONL path's own `existsSync` check). `src/artifact/service.ts` now passes `rows.resolvedCallRows` through on the DB-backed construction path instead of defaulting to empty. Test: `tests/projdb/resolved-calls.test.ts`, three cases against the `62-require-slot-dispatch` construct fixture -- DB round-trip, `who-calls`/`calls-from` identical rows on a DB-backed vs JSONL-backed copy of the same artifact (this row's own "prove fixed" criterion), and a simulated pre-MIGRATION-5 DB (table dropped after init) still opens and answers zero edges. | project-DB lane (Sonnet 5, lean worker) | fixed | metrics |
| 2026-09-04 | spec 17 §14 addition 2 wiring (`src/mcp/resources.ts`'s `scanSecrets`) | `src/secrets/service.ts` (`readStringsIndex`/`readStringUses`) | `SecretsService` read `index/strings.json` and `index/string-uses.jsonl` directly off disk (jsonl artifact format) and had no DB-backed (`.hbcproj`, spec 16 §2.4) read path at all — calling it against a DB-backed project threw `ENOENT` instead of reading the same rows via `openProjectDbReadonly`/`loadIndexRowsFromDb` the way `ArtifactService` itself did for every other resource. `scanSecrets` guarded this with an `existsSync` check and returned an honest `available: false` rather than crashing or fabricating an empty scan, but the underlying scanner was still jsonl-only. | Fixed 2026-09-05: `SecretsService`'s constructor now takes `{artifactDir, artifact?: ArtifactService}`; when `artifact` is supplied (every real caller now supplies it — `McpResources.scanSecrets` passes its own already-open `this.artifact`) the string table (`ArtifactService.allStrings`/`allStringUses`, two new public methods), bundle hash, and evidence resolver (`ArtifactEvidenceResolver` constructed directly against the `ArtifactService`) all go through that one DB-or-JSONL-agnostic read path. When `artifact` is omitted the legacy direct-disk-read path is kept (unchanged) for the CLI and for narrow index-only fixtures with no `manifest.json`. `McpResources.scanSecrets` no longer guards on `existsSync(index/strings.json)`. Tests: `tests/mcp/leads-search.test.ts` ("scanSecrets succeeds against a DB-backed (.hbcproj-only) project, not just JSONL" — RED before this fix against the DB-backed fixture already built at the top of that file, GREEN after); full `tests/secrets/*.test.ts` and `tests/mcp/resources.test.ts` (jsonl paths) stay green unchanged. | secrets lane / MCP follow-up (Sonnet 5, lean worker) | fixed | metrics |
| 2026-09-05 | spec 26 L6 (UI findings/leads workflow) implementation, discovered exercising POST /api/tools/set-finding-status against a real DB-backed (hbc2js init) project end to end | src/project/service.ts (ProjectService.setFindingStatus, DB-backed branch) and src/project/findings.ts (FindingStore.resolve/toFindingRecord) | A DB-backed project's set_finding_status write DOES persist (d_findings columns update, the transition's evidence is correctly merged into the finding's own evidence array, and the write itself returns 200) but the new status is invisible on every later read (GET /api/findings, GET /api/finding/:rid, the MCP finding/findings resources, hbcproj CLI reads) -- they all still report the ORIGINAL status (e.g. open). Root cause: FindingStore was designed around two separate JSONL revision kinds, kind:"finding" (immutable claim/evidence/severity) and kind:"status" (the live status, tracked by FindingStore.statusOf against a distinct statusEngine) -- toFindingRecord (src/project/findings.ts) hardcodes status: "open" on every kind:"finding" revision by design ("the finding row's own status never mutates -- live status is StatusStore's job") and resolve() always looks the real status up in statusEngine instead. But ProjectService.setFindingStatus's DB-backed branch never writes a kind:"status" revision at all -- it writes a FRESH kind:"finding" revision on the same slot with the new status baked into that revision's own value.status column (a reasonable-looking shortcut, since the DB schema has no separate status-detail table yet, itself a documented scope gap). reloadFromDb feeds every kind:"finding" DB revision, transition or not, into FindingStore's findings array and none into statuses, so statusEngine stays permanently empty for a DB-backed project and every resolve() call falls back to the always-open default. Confirmed directly: record-finding then set-finding-status to confirmed with a resolving dynamic evidence ref returns 200, but the immediately following GET /api/findings for that rid still reports status:"open" (evidence array correctly shows both the original and the transition's own ref). | fixed 2026-09-05 on branch agent/finding-status: `src/projdb/project-read.ts`'s new `splitFindingRevisions` reclassifies a status-carrying `kind:"finding"` DB revision into the synthetic `StatusRecord` `FindingStore` expects (the read-side split this row proposed), keeping the claim revision it supersedes as the live `active` `FindingRecord` -- so the finding `rid` is stable across a transition and `FindingStore.statusOf` sees the transition chain on both backends. Chosen over a genuine `kind:"status"` DB revision because that needs a `d_status` detail table, i.e. a `schema.sql` change, out of scope. Regression test `tests/project/finding-status-db.test.ts` runs the SAME record-then-confirm sequence against a DB-backed and a JSONL-backed project and asserts identical answers (`finding show`, the findings list, the `status` filter, `stat()`, reopen-from-disk, and the "already confirmed" refusal); it fails on the pre-fix read path with actual `open`, expected `confirmed`. `ui/e2e/findings.spec.ts`'s "live status, whatever it is" hedge was NOT dropped: playwright is not installed in that worktree, so the spec could not be run | project-db lane | fixed | semantics |
| 2026-09-05 | spec 24 (`class-recover`) idiom read, fixture 34-class-static-members at v98/v99; F24-3 fix task | src/emit/lower.ts `CreateBaseClass`/`CreateBaseClassLongIndex` and `CreateDerivedClass`/`CreateDerivedClassLongIndex` cases | hermesc allocates dst_ctor and dst_prototype to the SAME register whenever a class has no instance members (`CreateBaseClass r2, r2, r1, 1` in 34-class-static-members; `CreateDerivedClass r3, r3, r4, r5, 5` in the new fixture 67), and the lowering's second `set` used to overwrite the first unconditionally, so the constructor value was replaced by `<ctor>.prototype` and every static member installed on the prototype instead of on the class object. Self-consistent in fixture 34 alone (every read went through the same variable) but wrong for any class that is both instantiated and has statics, and it made static-vs-instance undecidable from the AST -- a precondition spec 24's sub-form C4 refused on until this landed. | Fixed 2026-09-05: confirmed against the MIT-licensed Hermes VM source (`lib/VM/Interpreter-slowpaths.cpp`'s `caseCreateClass`) that the interpreter writes the prototype value to the register first and the constructor value LAST ("Write the result last in case it is the same register as the prototype"), so an aliased register always ends up holding the constructor, never the prototype; confirmed again by disassembling a fresh repro at both v98 and v99 (`CreateBaseClass rX, rX, ...` / `CreateDerivedClass rY, rY, ...`, both compilers). The fix in `src/emit/lower.ts` mirrors that: it only materialises a separate prototype binding when the two operand registers are genuinely distinct, and reads `<ctor>.prototype` lazily (for the derived form's second `Object.setPrototypeOf`) when they are aliased, for both `CreateBaseClass` and `CreateDerivedClass`. New fixture `tests/fixtures/constructs/67-class-static-and-new` (a base class and a derived class, both static-only, both instantiated with a bare `new`) fails before the fix under the D14 VM-trace oracle ("prototype3 is not a constructor" on the bare `new Sub()`, since the aliased register held the prototype instead of the constructor) and passes after, at v98 and v99; a targeted unit test, `tests/gate/emit/class-ctor-proto-alias.test.ts`, asserts the install/read shape directly on fixtures 34 and 67. `npm run typecheck && npm run test:gate` clean. | Sonnet 5 (lean worker, F24-3) | fixed | emit-shape |
| 2026-09-05 | UI bur 1 first-paint measurement on the live Service NSW rig (docs/reports/2026-09-05-ui-first-paint.md) | src/ui-server/server.ts (one single-threaded Node process; src/mcp/leads.ts's computeLeads was the offender) | Any route that blocks for seconds head-of-line-blocks EVERY other route: measured on a page load, /api/segregation (3 ms of server time), /api/findings (1 ms) and /api/log/tail (0 ms) all landed ~41 s late because one /api/leads (37.7 s of synchronous whole-bundle scan) was ahead of them in the queue. The first-paint fix stopped the shell from REQUESTING the scan on load and memoised it per artifact, so a refresh no longer paid it -- but opening the Leads tab still froze every other route for ~37 s once per session. | Fixed 2026-09-05 (spec 26 L6, which makes the Leads tab load-bearing via lead-to-finding promotion, so the inline-but-cached scan needed the same off-main-thread treatment /api/segregation already got): computeLeads now runs on a node:worker_threads worker (src/workers/leads-worker.ts, own read-only ArtifactService built from artifactDir, same pattern as src/workers/segregate-worker.ts). src/ui-server/list.ts's listLeads never blocks -- it answers a computing:true placeholder immediately for a fresh artifact and the SAME settled object on every later call, mirroring segregation()'s own contract; LeadsResult grew an optional computing field (src/mcp/leads.ts, ui/src/contracts.ts) and ui/src/hooks.ts's useLeads polls at 500 ms while it is true. Tests: tests/ui-server/routes.test.ts, "listLeads never blocks: computing:true immediately, settles to resources.leads() once" and "a concurrent request answers fast while /api/leads is in flight (bur 1 row 2, off-main-thread)" -- the latter is this row's own "prove fixed" criterion, a concurrent /api/findings against a fresh McpContext while /api/leads is in flight. | UI lane (Sonnet 5, lean worker) | fixed | real-app |
| 2026-09-05 | Follow-up sweep from the `object-literal` rung (commit 3b0ec3a), which found and documented the same shape in its own `check.ts` (flagged there for a sweep of the other stage-B checkers). `stage-b-per-site-parses` | `src/passes/{default-params,jsx-recover,optional-chain,spread-rest}/check.ts` (five call sites total) | Each called `parses(after)` on the *site's own statement list*, not the whole function. Two costs: a redundant whole-list print+`vm.Script` on every site of a real bundle when the stage-B driver already re-validates the whole reconstructed function once per (pass, function), and a genuine false refusal — a bare (unlabeled) `break`/`continue` is only legal JS inside an enclosing loop/switch, and a stage-B site's own list is frequently a fragment of one (a switch-case body, a loop body) whose enclosing construct is invisible to a check that wraps just this list standalone in a throwaway function. Confirmed by hand-built repro for all four (prepend an untouched bare `break` to a known-good site; `parses(after)` on the rewritten list returns `false`, but every other structural check in the checker still holds). `src/passes/template-literal/check.ts`'s own `parses` call is different in kind (a single synthesized substitution expression, not a statement list) and was left in place. Zero refusals of this shape were observed on `tests/fixtures/bundles/rn-template-0.72/index.android.hbc` for any of the four rungs at HEAD — the bundle simply never happens to hit the pattern today — so this is a latent-bug fix plus a speed cleanup, not a readability-count change on that fixture. | Fixed 2026-09-05: removed the five `parses(after)` call sites (and now-unused `parses` imports) from the four checkers, each replaced with a comment citing the driver's whole-function guard and this row. One new gate test per rung (`tests/gate/passes/{default-params,jsx-recover,optional-chain,spread-rest}.test.ts`) hand-builds a known-good site with an untouched leading bare `break` sharing the site's own statement list, asserts `parses(rewritten-list) === false` (proof the repro is real) and `check(...).ok === true` (proof the fix holds) — red against the pre-fix code, green after. `npm run typecheck` clean; the four rung test files plus `pipeline-speed.test.ts`/`imports.test.ts`/`tests/gate/docs/testing-rules.test.ts` all green. | Sonnet 5 (lean worker) | fixed | passes |
| 2026-09-05 | CI red-run fix task, GitHub run 33920756693 (build-test, ubuntu-latest and macos-latest, node 22.18), reproduced locally with the same env | `tests/gate/docs/ci-require-oracles-skip.test.ts` (t7/t8 skip assertions, spread-non-iterable-message move assertion) and `tests/gate/passes/pipeline-speed.test.ts` (P-1 rn-template ratio, macOS only) | Three gate tests failed on GitHub while green locally. (1)/(2): both spawn a child `node --test <file>` and assert its reporter summary matches `/ℹ skipped [1-9]/`/`/ℹ fail 0/` (the `spec` reporter's info-line symbol) -- but `node --test`'s *default* reporter for a piped (non-TTY) child depends on the node version: node 22.18 (CI) defaults to `tap` for a piped child (`# skipped 1`, no ℹ), node 24/25 (this Mac) defaults to `spec` even when piped, so the assertions always passed locally and always failed on CI's node-22 job -- a test-infra portability bug, not a decompiler or oracle-skip regression. (3): P-1's ratio check (on/off CPU ratio <= 12x) measured 12.3x on the macOS runner (on 22079 ms, off 1790 ms) -- scheduling noise on a shared vCPU inflating the single `on` sample (only `off` gets a worst-of-two), not a real regression (250x is 20x further out than the widened bound below). | Fixed 2026-09-05: (1)/(2) both child `node --test` invocations now pass `--test-reporter=tap` explicitly, and the assertions match the TAP format's `# skipped N`/`# fail 0` lines instead of the spec reporter's `ℹ` lines -- deterministic on every node version and OS, since tap's format is not TTY- or version-dependent. (3) `tests/gate/passes/pipeline-speed.test.ts`'s ratio bound is now `12 * Math.sqrt(timeScale())` (already-imported `tests/support/tiers.ts` `timeScale()`, which `ci.yml` sets to 2.5 for exactly this kind of shared-runner noise) -- ~19x on CI, unchanged 12x on a dev machine (`timeScale()` defaults to 1), sub-linear so a real quadratic-term regression still trips it. Verified locally with `HBC2JS_REQUIRE_ORACLES=1 node --test tests/gate/docs/*.test.ts` and `node --test tests/gate/passes/pipeline-speed.test.ts` (all green); the reporter-format hypothesis was confirmed directly by running running `node --test tests/security/t7-lane-s-artifact-bar.test.ts` piped through `cat` on this Mac (node 25.9: spec-style `ℹ` lines even though piped) against the CI log's raw `# skipped 1` TAP output. Node 24 (the other CI matrix entry) was never observed to fail in the reported run, consistent with the reporter-default theory. | orchestrator / CI-lane (unassigned) | fixed | ci |
| 2026-09-02 | M5 rung 16 (`destructure`) implementation, `docs/specs/passes/16-destructure.md` | `src/passes/destructure/match.ts` (array-pattern grammar) | v1 recognised only *flat* labeled blocks plus (closed 2026-09-04) a per-element default. Still open at that point: a hole-by-shape (§2.3) and array rest (§2.4) at a function-body-scope site. | **FIXED 2026-09-05** (hole; rest confirmed unreachable, not a code gap). New fixture `65-destructure-hole-rest` (`skipMiddle`/`headAndTail`, function-body scope, no `const`/`let [..] =` declaration form so the array pattern is not conflated with v84's unrelated `__hbc_empty` TDZ-init quirk). Measured (`--no-pass var-naming --no-pass fn-naming --no-pass destructure` at every version): `skipMiddle`'s `[a, , c] = xs;` middle hole lowers straight-line at v84/v94/v96 — the elided position stages a value into a shared register that the *next* block never commits (`resolvePending`'s three-way dataflow split in `match.ts`: a `real = stage;` header found -> bound target; no header but the stage is read again before redefinition -> direct-commit target; no header and the stage is provably dead (`isDeadFrom`) -> hole); the close block can itself carry the pattern's *last* position's commit at its own head (`parseCloseBlock` extended to accept it — previously unexercised, since `firstTwo`'s last element always committed directly). v98/v99 lower the *same* hole through a genuinely different, unhandled shape (an early-guard flag copy plus an `rNextFn`-alias copy feeding `iterNext` directly) and stay refused (`broken-threading`), correctly, not mis-rewritten. `headAndTail`'s `[h, ...t] = xs;` rest is refused at *every* version including function-body scope: the append loop's own `try`/`catch` (§2.4) is inherent to the rest lowering itself (13 `__pc` writes at v84/v94/v96, 3 at v98/v99, in `headAndTail`'s own printed body, confirmed directly), not a top-level-only artifact of the module wrapper's exception machinery as §8 Q1 originally framed it — rest stays a documented v1 non-goal (batch-4 `try-clean` follow-up), not a bug. Tests: `tests/gate/passes/destructure.test.ts` (one hand-built-AST positive for the hole grammar, RED before this fix/GREEN after; fixture-level positives for `65` at v84/v94/v96 and refusal assertions at v98/v99 and for rest at all five versions). **Follow-up measured 2026-09-05, confirms this class rather than narrowing it**: a *nested* compound pattern as an array element (`[a = 1, [b = 2]] = xs`, `[{x = 3}] = ys`) is unimplemented in `match.ts` (`ArrayElement.target` is a bare register name, never a recursive `Pattern`) but also structurally unreachable in v1, exactly like rest -- measured directly at every version (`v84/v94/v96/v98/v99`), in statement-assignment position AND in a function-parameter-default position: as soon as an array pattern's element is itself a compound pattern, Hermes wraps the whole destructuring in the same `__pc`-tracked exception region rest uses, even with zero defaults anywhere in the pattern (a bare `[{x}] = ys;` already triggers it) -- the inner extraction can throw and the outer iterator's close must be exception-safe, so precondition 6 (`pc-tracked-region`) already refuses the whole site correctly, before any nested-target code path would even run. New fixture `70-destructure-nested-default` (`nestedArrayDefault`/`nestedObjectDefault`, function-body scope, plain-assignment form) is a pure-refusal fixture: `--no-pass destructure` is byte-identical to the default pipeline at every version, and a raw unrewritten `__hbc_iterBegin` call site survives for both functions. No matcher change is sound here; the extension remains the same one §8 Q1 already names (match the `__pc` region including its handler against the canonical abrupt-close expansion), not a change scoped to this task. Tests: `tests/gate/passes/destructure.test.ts` (fixture-level refusal assertions for `70` at all five versions, plus a pass-has-no-effect check). | M5 pass-ladder lane (unassigned) | fixed | passes |
| 2026-09-05 | CI red-run fix task, GitHub sweep workflow run 33914925844 (`tests/sweep/decompile/sweep.test.ts` T6) | `src/tables/generated/builtins-hbc99-mar2026.ts` via `tools/gen-tables/gen.ts` | The generated hbc99 builtin table was a literal parse of the vendored `913d31ac` `Builtins.def`, which has no `HermesBuiltin.setFunctionName`; the v99 compiler this project fetches does, at 55, so every builtin above 54 was named one slot too early. hermesc emits `CallBuiltin setFunctionName, 4` before the `DefineOwnByVal` of every computed method or accessor name, so the mis-name emitted `fn.apply(key, 0)` and threw an uncaught TypeError -- which is why only the `.obf` builds broke, the obfuscator making every method name computed. Affected `32-class-basic.obf`, `33-class-inheritance-super.obf`, `34-class-static-members.obf`, `36-class-getters-setters.obf`. | fixed in fa23564: `patchHbc99Mar2026Builtins` inserts setFunctionName at 55 (hbc99-mar2026 only, evidence being the compiler's own name table and `hermesc -dump-bytecode`), plus the `__hbc_b_setFunctionName` runtime helper and its ES SetFunctionName prefix handling. Regression fixture `tests/fixtures/constructs/64-computed-method-names` (v98 control, v99 the build that uses the builtin) plus `tests/gate/tables/builtins-hbc99-setfunctionname.test.ts`. Hardened tier: 7 DIVERGENT before, 3 after. | hardened-tier obfuscation lane | fixed | emit-shape |
| 2026-09-05 | Fred (owner), live rig — "there is no theme conf; none of the key bindings work" | `src/ui-core/keymap.ts` (`stepMapKey`) and `ui/src/actions/keys.ts` (`toKeyEvent`) | Every chord in the default preset was dead. Two independent faults. (a) A chord step and a live KeyboardEvent were hashed into the same trie key case-SENSITIVELY for single characters: Chrome reports `key: "p"` for Ctrl+P while the preset writes "Ctrl-P", so `project.palette` and `project.search` — the only two chords a user reaches for — never matched, while `F2`, `Shift-F12` and `Ctrl-Shift-N` happened to match by luck (named keys, or a shifted letter the browser upper-cases anyway). (b) Every preset writes `Ctrl-` and the top bar advertises "Cmd/Ctrl-K", but the owner is on macOS, where the COMMAND key sets `metaKey`, not `ctrlKey`; nothing folded the two, so the whole keymap was unreachable on his machine even for the chords that did hash correctly. Compounding: "Ctrl-K" was not in any preset at all — the palette kept its own hard-coded `useEffect` listener — and there was no in-app way to see, change or repair a binding, nor to change the theme, so the failure was silent and unfixable from the UI. | **FIXED 2026-09-05.** (a) `stepMapKey` now lower-cases a single-character key that carries Ctrl/Alt/Meta, keeps Shift a significant slot for letters (`Ctrl-N` != `Ctrl-Shift-N`) and ignores it for punctuation/digits whose shift state is keyboard-layout dependent; a BARE character step is unchanged, so vim's `K` is still not `k`. Both sides go through the one function, so the normalisation is symmetric by construction. (b) `toKeyEvent` folds a lone COMMAND into `ctrl` on macOS (`isMacPlatform`); no preset binds a `Meta-` chord so nothing is shadowed, and an unbound Command chord still resolves to "none" and is left to the browser. `"Ctrl-K": "project.palette"` added to the default preset and the palette's duplicate hard-coded listener deleted. New `project.shortcuts` (`?`) opens a Keyboard-shortcuts cheat-sheet rendered from the live keymap, and `project.settings` (`Ctrl-,`, plus a gear in the top bar) opens a Settings dialog with theme preset, density, keymap preset, read-only token overrides and a full record/swap/reset key-binding editor persisted in localStorage and layered through the existing `keymap-resolve` overrides path. Tests: `tests/gate/ui/keymap-default.test.ts` (fires the real browser event for every chord in every shipped preset — RED before the fix on `Ctrl-P`/`Ctrl-F`), `tests/ui-core/keymap-bindings.test.ts` (10 tests: normalisation, `formatChord` round-trip, merge/conflict/rebind/unbind/reset algebra) and `ui/e2e/keys.spec.ts` (Chromium: every default chord including with focus inside CodeMirror, the cheat-sheet, theme switch surviving reload, rebind + reload + reset, and a conflict). Docs: docs/UI.md "Keyboard shortcuts, and why none of them used to fire" + "Settings dialog". | UI lane | fixed | harness |
| 2026-09-01 | E2E tier 1 — buckets `diff:PutNewOwnById/PutById` (153 rn-template, 1114 Service NSW), `diff:PutOwnBySlotIdx/PutByIdStrict` (248 react-navigation v98) | `src/passes/object-literal` (M5 rung 20) | Object literals came back as `r3 = {}; r3.remove = ...` where the source was `{remove: ..., x: ...}` — the single largest readability bucket. | Fixed by the `object-literal` rebuild rung (docs/specs/passes/20-object-literal.md, catalogue row 28, evidence docs/lowering/object-literal.md), with construct fixture `63-object-literal` at all five HBC versions covering a plain data literal, a literal with closure values, integer keys and three negative controls. Measured on rn-template: own-property store statements 9947 -> 6814, empty-object-then-store paragraphs 1771 -> 467, multi-property literals 380 -> 1278. The refusals it deliberately keeps are tracked by the new open `object-literal-refusals` row. | M5 ladder | fixed | emit-shape |
| 2026-09-03 | campaign-1 local run (40k programs, seed-base 777000, ~5h) | `tools/fuzz/construct-fuzz.mjs:177` | The campaign driver accumulates every divergence signature (with full details) into one in-memory report and writes it with a single `JSON.stringify` — at campaign scale (40k programs, 201 finds) the string exceeds V8's max length: `RangeError: Invalid string length`, and the aggregate matrix (`cells`) is lost after 5h of compute. The per-find files (`reports/fuzz/finds/*.js`, 201 across v84:50/v94:46/v96:45/v98:1/v99:59, deterministic seeds) survive, so finds are fully triageable and the run is per-seed reproducible — only the summary died. Secondary lesson (orchestrator, not the driver): launching via `... 2>&1` piped into `tail -40` masked the driver's exit-1 behind the pipe's exit-0. | FIXED a9a4032 (`fix(fuzz): stream construct-fuzz.mjs's campaign report; add --recount recovery`): the report is streamed (cells + per-signature JSONL written incrementally, signature payloads capped) and `--recount` re-derives cells from `finds/`; fix-wave 4 (`docs/reports/2026-09-05-finds-reclassified-post-fixwave4.md`) re-ran the 201-find reclassification twice with the summary written successfully. Row closed by the orchestrator 2026-09-04. | fuzz-lane follow-up (unassigned) | fixed | harness |
| 2026-09-02 | overseer (loop-checker fix false-positive) | `tests/gate/passes/imports.test.ts` | The import-boundary regex matches the word `from` INSIDE a string literal ending `...from"`, flagging a legal file as an illegal import (hit by a checker message string; reworded the message to dodge it). | FIXED a4c0732 (`fix(tests): D12a import-boundary regex no longer scans into string literals`): the regex anchors the import keyword and stops at newline/`;`, with a targeted string-literal case in `tests/gate/passes/imports.test.ts`. Row closed by the orchestrator 2026-09-04. | tests/harness lane | fixed | harness |
| 2026-09-05 | fuzz fix-wave 4, residual family-F2 finds (`docs/reports/2026-09-05-finds-reclassified-post-fixwave4.md`) | `src/harness/ladder.ts` (the Hermes VM cross-check), NOT the decompiler | The 4 finds left DIVERGENT after the family-F2 fix - `v94-seed780867`, `v96-seed781844`, `v96-seed782973`, `v99-seed777142`. The old row (and the post-F2 report) read the trace diff backwards: in `ladder.ts` side `a` is the CANDIDATE and side `b` is source.js under Node, so the candidate printed `16,16,...` and Node's run of the source printed `0,1,2,...`. The decompiler was right - it emits one shared environment slot (`_e0_0`) that every closure created in the loop reads, which is what the bytecode has - and all three real interpreters agree: `tools/hermesc/v96/hermes`, `tools/hermes-vm/v94/bin/hermes` and `tools/hermes-vm/v99/bin/hermes` each print `3,3,3` for a three-iteration reduction where Node prints `0,1,2` (D14, Hermes shares one `let` binding across iterations). Three harness bugs buried that evidence. (1) P-16's budget rule capped the compared prefix at `min(candidate, vm) - 1` lines, so a program whose whole observable output is one line before it loops forever verified ZERO lines and the VM's only line - the ground truth - was discarded. (2) A DIVERGENT candidate-vs-source.js comparison was never weakened even when the VM had just reproduced, byte for byte, the very record it diverged on. (3) Only the candidate's engine-resource ceiling was recognised; Hermes words the same array ceiling `Requested an array size that fails to allocate` and reports heap exhaustion as an `LLVM ERROR: OOM` abort with no `Uncaught` line, so the candidate's ceiling marker was stripped while the VM's identical one was not, manufacturing a divergence out of two engines hitting the same wall after identical output (`v99-seed777142`, whose verdict also flipped run to run on whether the abort beat the 5 s timeout). | FIXED 2026-09-05 (`50b87c3` + `636b412`). The trailing line is dropped only where a mid-line cut is possible (the VM's stdout when it does not end in a newline; never the candidate's, whose lines come from whole trace records). New `vmRefutedDivergence` weakens DIVERGENT to INCONCLUSIVE - never PASS, HA-01 - when the divergence is an output record whose text lies wholly inside the prefix the VM reproduced; a divergence the VM never observed still stands. `VM_RESOURCE_CEILING` recognises both Hermes ceiling shapes and takes the marker off both sides together. Regression tests: `tests/gate/harness/ladder-budget-prefix.test.ts`, 3 cases (reduced `v94-seed780867` end to end at v94; the reduced `v99-seed777142` ceiling shape at v99; and soundness - a candidate that really disagrees with the VM inside the observed prefix stays DIVERGENT and is now reported against the VM rather than against source.js), all red before `50b87c3` and green after. No fixture was added: every one of these programs is non-terminating by construction, so a construct or adversarial fixture would add tens of seconds of pure timeout at every version and sit at INCONCLUSIVE, and the shared-`let` semantics themselves are already covered by `tests/fixtures/adversarial/06-closure-loop-var-vs-let`; the reasoning is written up in the report's "No fixtures, and why". Reclassification of all 201 finds: DIVERGENT 5 -> 3 -> 1, the survivor being the unrelated v98 round-trip function-count row. RE-VERIFIED 2026-09-05 (QUEUE 11, agent/f2-reduce): all four finds re-run at `9df2ba7` through the real hermesc, the real decompiler and the real Hermes VM come back INCONCLUSIVE, none DIVERGENT, so this row stays fixed and QUEUE 11 has no decompiler bug left to chase (PUSHBACK P-36). Two notes for anyone re-running it. (a) The re-run needs `tools/hermes-vm` on the box: in a worktree carrying only the `tools/hermesc` symlink there is no VM evidence at all and `v94-seed780867` reads DIVERGENT against Node again, which is how a cleared find can look reopened. (b) PASS is unreachable for these four by construction - each loops forever on a counter `++` cannot advance - so INCONCLUSIVE is the ceiling, HA-01. Standing decompiler-side cover for the family now exists as adversarial fixture `48-fuzz-dowhile-nonadvancing-counter`, a terminating counterpart of `v99-seed777142` keeping the do/while shapes around its unbounded loop (compile-time-false test whose body still runs once, non-advancing `-Infinity` counter, `Infinity` arithmetic into an array and a `join`) with that loop bounded by a separate finite counter; PASS at v94/v96/v99 against the real VM, `tests/sweep/adversarial/fuzz-dowhile-nonadvancing-counter.test.ts`. | fuzz fix-wave | fixed | harness |
| 2026-09-02 | P2.1 §8 steps 3–5 implementation (`calls.jsonl`/`strings.json`/`string-uses.jsonl`/`globals.jsonl`/`native.jsonl`) | `src/artifact/native.ts` (§2.5 `bridge-module` surface) | Scope cut under the implementing agent's budget: `native.jsonl`'s `builtin` and `host-global`/`host-global?` rows are implemented and tested, but `bridge-module` rows (requires of `src/deps`-classified native-boundary packages, e.g. `react-native`, `expo-modules-core`) are not — `src/deps/classify.ts`'s module classifier needs a `CommonalityIndex`/inventory built ahead of time, which the artifact builder does not currently construct. Zero `bridge-module` rows are ever emitted (never a guessed/fabricated one — truth rule honoured), so `native.jsonl` is honest but incomplete relative to spec §2.5. | **FIXED** (2026-09-04). `src/artifact/native-boundary-packages.ts` adds the curated `NATIVE_BOUNDARY_PACKAGES_SET` (`react-native`, `expo-modules-core`, same governance pattern as `host-globals.ts`). `src/artifact/native.ts`'s `nativeBoundaryModuleIds(report)` turns a `src/deps/classify.ts` `ClassificationReport`'s `library`-classified modules with a matching `libraryPackageHint` into a set of local Metro module ids; `buildNativeIndex` takes that set and, for every `calls.jsonl` `kind:"require"` row whose `m:<moduleId>` target is in it, emits `{fn:caller,surface:"bridge-module",name:"m:<moduleId>",n}` — `name` re-emits the exact `calls.jsonl` callee string, never a re-derived package/subpath (spec §2.5 example line corrected from the aspirational `m:react-native/NativeModules` to the real `m:17` shape). `resolveBridgeModuleIds` (`write.ts`/`index-rows.ts`'s shared call site) uses a caller-supplied `WriteArtifactOptions.classification`/`BuildIndexRowsOptions.classification` `ClassificationReport` when given, else builds one cheaply from the bundle's own inventory (`buildInventoryFromModule` + `classifyInventory` with `EMPTY_COMMONALITY_INDEX` — D17j's in-bundle signals need no cross-app corpus) — never re-parses the bytes, reuses the already-parsed `HbcModule`. Measured (both isolated timing and via a full `writeArtifact` run): rn-template-0.72 debug fixture +134 ms (inventory+classify) of a ~1 s split+write; a 12.7 MB / 4,510-module production bundle (local corpus, not committed) +2.1-2.8 s of a ~13.6 s `splitProject` (~15-20% added to `writeArtifact` itself, a low-single-digit percent of the whole decompile). On both real bundles tested, `classify.ts`'s string-evidence signals never actually fire (Metro strips `node_modules/`-shaped paths from optimised output, per `classify.ts`'s own file header) — `bridge-module` is honest-but-empty there today, by design (truth rule); the wiring is proven correct via an injected `ClassificationReport` on rn-template's own real `require` edge (a real `calls.jsonl` row, an injected — but structurally faithful — classification) plus pure unit tests of the classification-to-row mapping. Tests: `tests/gate/artifact/native-bridge-module.test.ts` (7 tests — the curated-package-set check, `nativeBoundaryModuleIds` filtering incl. the "hint present but not classified library" case, `buildNativeIndex` aggregation + the zero-rows-by-default truth-rule case, and the two rn-template end-to-end cases with/without a supplied classification). Docs: `docs/specs/10-artifact-format.md` §2.5 rewritten to match the actual emitted shape and builder behaviour. | Stage-2 lane | fixed | metrics |
| 2026-08-31 | integration/report task (real-app `--split` end-to-end + readability check) | `src/emit/function.ts` (`shapeKeyFor`) + `--split`'s all-or-nothing failure mode | `--split` on react-navigation-example (v94, 15,551 functions) **crashes the entire split**, exit 3, zero files written: `E_EMIT_UNSUPPORTED: slot 1 of r3 has no known object shape at offset 186`, isolated to `functionIndex: 8640` — some code path reads an object-literal property by shape-table slot index without a `recordShape` having run first on that register in that control-flow path. Separately: `--split` has no per-function isolation — one unsupported construct anywhere aborts 100% of the output, not just the affected module. Confirmed not a general real-app-scale problem (a 4995-module Bloomberg APK bundle and rn-template both split cleanly), so construct-specific, not size-specific. | **FIXED** (2026-09-04): root-caused to fn#8640 offset 186 (0xba) — `NewObjectWithBuffer r3, sh2038(numProps=3)` at 0x98 with slot 0 stored at 0xa6, then `JmpTrueLong L2, r6`; BOTH arms finish the literal with `PutOwnBySlotIdx r3, …, 1`, and the arm the emitter printed FIRST clobbers r3 (`LoadFromEnvironment r3, r2, 2` at 0x140) before the other arm is printed. The shape map was a single mutable register→keys map advanced in *statement emission order*, so the second arm's read found nothing even though the creating instruction dominates both arms. Replaced by `src/emit/shapes.ts`: a forward must-analysis over the CFG (available-expressions lattice; entry empty, other blocks ⊤, meet keeps a register only when both sides carry the SAME interned key array; `Mov` propagates; handler entries and normal-edge-unreachable blocks start empty), so a read resolves exactly when a creation dominates it and nothing clobbers the register in between, and `E_EMIT_UNSUPPORTED` now says why (no dominating creation vs paths disagree). No key is ever guessed. Measured on this bundle: whole-file `--passes=none --lenient-env` stubbed functions 11 → 10 (the shape stub is the one that went); `--split --passes=none` 1782 modules, the single `no known object shape at offset 186` stub gone, only `module_1331.js` (+ artifact manifest/ranges) differs. The all-or-nothing half of this row was closed separately by commit 09011d7 (per-function emit isolation: an unsupported function becomes a throwing stub, the module still writes), so `--split` on this bundle already exited 0 before this fix — the crash-the-whole-run failure mode described above no longer exists. Tests: `tests/gate/emit/shape-across-blocks.test.ts` (7 analysis properties incl. the fn#8640 branch shape, disagreeing paths, loop back edge, handler entry) and `tests/sweep/emit/shape-across-blocks.test.ts` (this bundle: fn#8640 is not stubbed, no `no known object shape` text anywhere, shape 2038's `extrapolate` key present) — the sweep one verified failing at the parent commit and passing after. Spec: `docs/specs/05-emitter.md` §5 "Object shapes". A minimal hermesc-reproducible construct fixture was attempted (6 candidate sources at v94+v98, incl. ternary-valued literal properties and a mirror of the real if/else) and none reproduced it: the bug needs Hermes' register allocator to reuse the object's register in the earlier-printed arm, which small functions never do — hence the bundle-level test, per this task's brief. | src/emit lane + split lane (unassigned) | fixed | emit-shape |
| 2026-08-30 | T14 adversarial pass-ladder testing | `src/passes/global-access/{match,check}.ts` | `isProvenGlobal` proves a register global by scanning every write across the *whole function* and requiring exactly one write literally valued `globalThis`, chronologically first — it never checks whether a *later* write in the same statement list (after the guarded read) can re-execute before that read on a repeat visit. When the guard+read+a later clobbering write all live in one loop body, the fold is accepted anyway and is wrong from the 2nd iteration on. Confirmed with plain `node -e` (no D14 ambiguity). | **FIXED** (2026-09-04). Root cause named exactly: `isProvenGlobal`'s whole-function rule reads "later in the statement list" as "later in time", which is only sound where control passes the site once. Fix = a new **site-aware** §4 condition 5 with refuse reason `loop-reentry-clobber` (`hasLoopReentryClobber` in `src/passes/global-access/match.ts`): find the **outermost** loop body (`while` or `do-while` or `for`, labelled or not) that transitively contains the site's statement list — outermost because a clobber in an outer loop can precede the read on that outer loop's re-entry too — and refuse if ANY write to `G`'s register anywhere inside it, before or after the read and at any nesting depth, is valued something other than `globalThis` (a write valued `globalThis` re-establishes the value being proven and is not a clobber). The enclosing loop is found by locating the site list **by identity** inside `ctx.fnBody` (`stmtLists` hands the driver the very arrays that live in `fnBody`, and `check` is given that same `before`), so no signature had to change and `check.ts` re-derives the identical verdict as its new item 4b — matcher and checker stay in lockstep. `isProvenGlobal` is deliberately unchanged and still position-blind; non-loop sites keep the whole-function rule, which is what keeps §7's `targets` (19-var-hoisting, 01-if-else-chain, and 02-while-loop, itself a loop fixture) green. Tests: the pinned case in `tests/gate/passes/adversarial-ladder.test.ts` flipped to the correct refusal (`classifySite` -> `loop-reentry-clobber`, `match` -> null, a hand-forced rewrite rejected by `check`) with its `node:vm` divergence run KEPT as the standing proof the refused fold really diverged ("GLOBAL","OTHER" vs "GLOBAL","GLOBAL"), plus a new positive case there (a loop body that never writes the register still folds; the straight-line scratch-reuse idiom still folds) and 3 in `tests/gate/passes/global-access.test.ts` (clobber nested in an `if` inside the loop; clobber in the OUTER loop body outside the inner loop holding the site; a loop write valued `globalThis` is not a clobber). No construct fixture: the shape needs Hermes to reuse a still-live register inside a loop, which its allocator does not emit, so a fixture would compile but never exercise the defect — the AST-level tests are this row's own stated verdict. Docs: spec 03 §4 condition 5 + implementation note, §6 item 6, §7 refusal list and unit-test checklist. Gate 2392 tests / 2385 pass / 0 fail. | M5 pass-ladder lane | fixed | passes |
| 2026-09-04 | fuzz fix-wave step 1 (family F2) | `src/passes/expr-rebuild/match.ts` (the expr-rebuild readability pass) | 5 signatures (5 finds, v96 + v99) from the `let outer = 0; …` closure grammar diverge on *values* inside the common prefix — e.g. VM `1 true` vs candidate `f0 NaN` (`v99-seed777578`), VM `1 ,100` vs candidate `f1 v-0.5` (`v99-seed777358`), VM `0 true` vs candidate `f3 v-true` (`v96-seed780933`). Not yet reduced to a minimal repro: every one of these programs also contains an F3-shaped missing-global call, so a "still DIVERGENT" ddmin predicate collapses them onto the F3 signature instead of preserving the value divergence. ROOT-CAUSED AND FIXED 2026-09-04 (F2 task): not an env/closure-graph bug at all, and not a value bug - the pass `expr-rebuild` folded a dead store into a `for` loop's *test*. `topLevelExprOf` names only the one field the rewriter may write into (`test` for every loop), so a `for` header's `init` and `update` were invisible to `classifySite`'s forward scan and to `loopTestGuard`. hermesc types `outer + ''` as a string, so `0 === (outer + '')` is provably false and the whole comparison is dropped at compile time, leaving a dead `AddEmptyString r1, r1` immediately in front of the else-branch loop's `LoadConstUInt8 r1, 2`; after for-header raised `for (r1 = 2, r4 = 0; r4 < r1; ...)`, expr-rebuild took the test's read of `r1` as the consumer of the *pre-init* value and folded, turning `r4 < 2` into `r4 < "" + outer` = `0 < "0"` = false, so the loop never ran. Fix: `forHeaderVerdict` (an `init` store is a redefinition before the first test, an `init`/`update` read is a read) consulted by both the forward scan and `branchVerdict`, plus `loopTestGuard` now refuses when a `for`'s `init` writes an input of the folded value. | Fixed 2026-09-04 in `src/passes/expr-rebuild/match.ts` (`forHeaderVerdict` + `loopTestGuard` init check). Regression tests: construct fixture `tests/fixtures/constructs/60-for-header-init-clobber` (all 5 versions, red before the fix - `j < "" + _e0_0` - green after), 4 unit tests in `tests/gate/passes/expr-rebuild.test.ts` (search `F2:`), and adversarial fixture `46-fuzz-let-capture-branch` flipping DIVERGENT -> PASS at v96 (PASS at all four versions now). Corpus effect (`docs/reports/2026-09-04-finds-reclassified-post-f2.md`, all 201 finds re-run): DIVERGENT 9 -> 5. The four finds cleared are `v96-seed780933`, `v99-seed777358`, `v99-seed777578`, `v99-seed777648` - every find carrying the value/branch signature this row described. The other four the row had grouped in on version alone are a different bug and keep their own Open row (see the 2026-09-04 fuzz family F2 residual row). | fix-wave | fixed | semantics |
| 2026-09-04 | fuzz fix-wave step 2, full 201-find re-run after the P-16 fix (`docs/reports/2026-09-04-finds-reclassified-post-p16.md`) | `src/harness` oracle (Node sandbox resource limits) vs the Hermes VM, not the decompiler | 30 of the 43 finds still DIVERGENT after P-16 share one shape: the mutated program never terminates, and the decompiled candidate running under Node dies with an **uncaught RangeError** (V8 hitting max string/array length, or the call stack, while looping) at a point where the real Hermes VM keeps running to its timeout. One side terminated and the other did not, so the P-16 budget rule correctly refuses to call it INCONCLUSIVE - but the cause is Node's own resource ceiling on an infinite loop, not a decompiler defect, so these finds still carry no evidence about the decompiler. Example signatures: `v84-seed783042` (6 finds), `v84-seed779464` (5), `v84-seed779013` (2); all v84/v94. | **FIXED** (2026-09-04, fuzz fix-wave step 3A, `src/harness/trace.ts` `isResourceCeilingRecord` + `src/harness/compare.ts` + `src/harness/ladder.ts`). An `err`/`unhandled` `RangeError` carrying one of the three engine resource-ceiling messages (`Maximum call stack size exceeded`, `Invalid string length`, `Invalid array length`) is now treated as a budget marker, exactly like a `limit` record: comparison stops there and the verdict is INCONCLUSIVE with a `resource:` reason, in `compareTraces` and in `ladder.ts`'s D14 VM cross-check (which drops the candidate projection's trailing `uncaught RangeError` line and counts the candidate as budget-hit). Three guards keep it sound and are each pinned by a test: the prefix up to that record must already be equal; only ONE side may be ceiling-shaped, so an err/unhandled of any other kind on the other side stays DIVERGENT; and the other side must itself be budget-limited, so a program-level `RangeError: Invalid array length` (`new Array(-1)`) against a side that ran to completion is still DIVERGENT. Tests: 5 cases in `tests/gate/harness/compare.test.ts`, all verified failing before the fix. | fix-wave | fixed | harness |
| 2026-09-04 | fuzz fix-wave step 1 (family F3) | `src/emit` global-read guard, or the harness's message masking | A missing global read that reaches the VM as `ReferenceError: Property 'f2' doesn't exist` reaches the Node-run candidate as `ReferenceError: f2 is not defined` — same error type, same control flow, different message text, and a program that prints `String(e)` therefore diverges. 9 of the 64 surviving signatures (9 finds, all v99). The decompiler already emits the Hermes wording for guarded global reads (`if (!("print" in r1)) throw new ReferenceError("Property 'print' doesn't exist")`), so this is either an emit gap on the call-callee path or a masking gap. Minimal repro: `try { f2(0, 0); } catch (e) { print(String(e)); }` at v99. | **FIXED** (2026-09-04, fuzz fix-wave step 3B, `src/harness/trace.ts` `normaliseEngineMessages`). Fixed as the oracle-normalisation gap (ii) this row's own evidence identified, NOT in the decompiler: both engine renderings of a missing-global ReferenceError are projected onto one canonical, name-preserving form (V8's `X is not defined`), applied to BOTH sides in `compare.ts` (rendered records, masked and unmasked) and to both print projections in `ladder.ts`'s VM cross-check - so it reaches the wording wherever it arrives, including inside an ordinary `print` record where `renderRecordMasked`'s masking channel never sees it. The `global-access` pass and its tests are untouched. Tests: 2 cases in `tests/gate/harness/compare.test.ts` (both renderings equal; `f2` still never matches `f3`) plus the new fixture `tests/fixtures/adversarial/45-missing-global-wording`, measured DIVERGENT at v84/v94/v96/v99 before the fix and PASS at all four after. | fix-wave | fixed | emit-shape |
| 2026-09-04 | E_UNBOUND_IDENT hunt on react-navigation-example-0.85.3 (`--split` and whole-file `--lenient-env`) | `src/emit/index.ts` loop-local env declarations, `src/emit/lower.ts` | `E_UNBOUND_IDENT: emitted identifier "_e2326_0" is not declared in any enclosing scope (module > _fn321 > _fn4521)` aborted the whole 15,551-function bundle. Env 2326 is created inside a loop in fn#4521, so its slots are declared as a `let` at the `CreateEnvironment` site rather than at function top (fresh binding per iteration), and the one closure that captures it (fn#13735, the `ref` prop) is emitted inline at its `CreateClosure` site. That site is in a SIBLING labelled block of the same loop body — `L3: { let _e2326_0; … }` then `L7: { r32 = function _fn13735 () { … _e2326_0 … }; }` — where the `let` is not in scope. `emitModule`'s guard for this only looked at env accesses booked under the owner function, and the capture is booked under the child. | **FIXED** in commit 2810099: the inline closures' creation sites now take part in the same in-the-create-block test as the slot accesses (`closureFunctionId` in `src/emit/lower.ts` exports the operand table the CreateClosure/CreateGenerator/class lowerings already read, so the two cannot drift), and a closure with no creation site in the owner at all — whose inline body would otherwise be dropped entirely — also forces the hoisted fallback with `W_LOOP_LOCAL_ENV`. Regression tests: `tests/sweep/emit/unbound-env-slots.test.ts` (react-navigation, asserts `_e2326_0` is never among the isolated names) and `tests/gate/emit/unbound-isolation.test.ts` (the scope-check half). Whole-file before/after on react-navigation with `--lenient-env --passes=none`: 504 unbound identifiers / 179 stubbed functions before, 502 / 177 after — this fix accounts for 2 of them; the other 481 are the separate orphan-placement row dated the same day. | passes lane | fixed | emit-shape |
| 2026-09-04 | fuzz fix-wave step 1 (`docs/reports/2026-09-04-fuzz-families.md` family H1) | `src/harness/ladder.ts` D14 VM cross-check, `src/harness/compare.ts` | A non-terminating program is always reported DIVERGENT for a reason that has nothing to do with the decompiler: the cross-check diffs the Hermes VM's **raw stdout** (bounded only by the 5 s timeout — measured 3,389,470 lines for `v84-seed778059`) against the candidate's trace projection (bounded by `maxRecords: 5000` — 4,999 lines), so the two always differ at the smaller one's cut-off. `compare.ts` has a "both hit a budget -> INCONCLUSIVE" branch but it is unreachable when the record counts differ (`divergence` is set from the length mismatch before `truncated` is consulted). 110 of the 159 surviving campaign finds (28 of 64 signatures) are this artifact, and the verdict is timing-dependent (re-running `v84-seed783042` gives DIVERGENT or INCONCLUSIVE depending on machine load). | **FIXED** (2026-09-04, commit `b5362b0`, `src/harness/compare.ts` + `src/harness/ladder.ts`). The D14 cross-check now caps both sides to the same number of lines before comparing (dropping the last capped line, which a timeout-killed VM can have written only half of) and reports an equal capped prefix as INCONCLUSIVE with a caveat when **both** sides hit a budget; `compareTraces` stops comparing at the earliest `limit` record (a budget marker is not an observation) and consults the budget before turning a length mismatch into a divergence, reporting no divergence at all in that case, so a timing-dependent cut-off can never become a fuzz signature. Both-sides rather than the fixing brief's either-side rule: a candidate that hangs while the VM runs to completion inside the same budget is a real behaviour difference, and an either-side rule dropped `selftest.test.ts`'s HA-09 mutation kill rate from 313/366 to 305/366 (measured) - recorded as a deviation in PUSHBACK P-16. Tests: `tests/gate/harness/ladder-budget-truncation.test.ts` (non-terminating program vs a VM that ran past the budget -> INCONCLUSIVE, stable over 3 runs; a program printing a wrong value before looping forever -> still DIVERGENT) + 3 cases in `tests/gate/harness/compare.test.ts`, all verified failing before the fix. Re-ran all 201 campaign finds on the deb box (`docs/reports/2026-09-04-finds-reclassified-post-p16.md`): 85 PASS / 73 INCONCLUSIVE / 43 DIVERGENT / 0 ERROR, against 77 PASS / 124 failing before - 81 of the 110 H1 finds left DIVERGENT, and the 30 that did not are the separate Node-RangeError sub-family filed in its own open row. | fix-wave | fixed | harness |
| 2026-09-04 | fuzz fix-wave step 1 (`docs/reports/2026-09-04-fuzz-families.md` family F1) | `src/passes/spread-rest/match.ts` | **FIXED.** `spread-rest` deleted a matched run wholesale, including the "pure setup" statements its `Subst` map had absorbed — but Hermes stages a spread's source and index registers once and reuses them at the next spread site, so the second site was left reading a register nothing assigns (`[...r8]`, `copy.push(r0)`, `{...null, y: r5}`); separately, a plain element stored after a spread kept its raw staging register instead of the resolved value (`[0, ...a, r1, ...a, 5]`). `check.ts` could not see either: a deleted register move has no entry in `effectSequence`. 22 of the 64 surviving fuzz signatures (35 of 159 finds), all versions. | Two changes in the matcher: (1) `siteDeletesLiveRegister` refuses a site whose deleted range writes a register that is still live afterwards (bounded forward liveness scan, `LIVENESS_SCAN_LIMIT`), and the rule scan skips such a site instead of returning it, so the *other* site of a shared-register pair is still recovered; (2) plain elements/arguments are resolved through `Subst` (`subst.resolve(store.value)`) exactly as spread sources and object-property values already were. Regression fixture `tests/fixtures/adversarial/44-fuzz-spread-shared-register` + four behavioural tests in `tests/gate/passes/spread-rest.test.ts`, verified failing before the fix (`actual: ['0,0,1,3,,0,1,3,5', 'a-b-c']` vs 5 expected lines) and passing after, at v84/v94/v96/v99 | fix-wave | fixed | passes |
| 2026-09-03 | P2.1 close-out (BUGS row 2026-09-03 "index build 51.4%/index 64.5%") | `src/artifact/build.ts`/`write.ts`/`semantic-walk.ts`, `src/split/index.ts` | **Build time: FIXED.** Diagnosed by manual timing splits: `walkFunction` ran 3x per function (`calls.jsonl`/`globals.jsonl`/`string-uses.jsonl` each re-walked independently) — consolidated into one `buildSemanticIndexes` pass (byte-identical output, verified by sorted-diff against the pre-change three-pass output on rn-template). Dominant remaining cost (~78% of index-build time) was `analyseForArtifact` re-parsing+re-analysing the whole bundle a second time, independent of `splitProject`'s own parse+analyse — fixed by exposing `SplitResult.module`/`.analysis` and having `writeArtifact` reuse them when available (`analyseForArtifact`'s new optional `reuse` param), falling back to a fresh parse when no `SplitResult` is in hand. Measured: 51.4% -> **9.4%** of decompile+render time on rn-template (target ≤25%, met). **Index size: RENEGOTIATED**, per reviewer §10 ruling 3's explicit escape valve. Per-file breakdown: `calls.jsonl` (31.2%) + `string-uses.jsonl` (33.6%) alone are 41.8% of rendered bytes — already over the original 30% target with every other file at zero — and are exactly the per-call-site/per-string-use rows §2.2/§2.3b's truth rule requires (never a guessed edge); cutting them trades completeness, which ruling 3 forbids. New target `index/` ≤ 70% of rendered-source bytes (measured actual 64.5%, headroom for the held-out bundle), recorded in docs/specs/10-artifact-format.md §5 + a new "P2.1 close-out size renegotiation (2026-09-03)" entry under §10. A compact positional-array encoding for the two big files (bump `hbc2js-index/2`) is queued as a real follow-up, not attempted (est. ~40-45% smaller per row, unverified) — genuinely out of this close-out's budget, not attempted-and-reverted. | `tools/artifact/measure.ts` on rn-template: index build 9.4% (target ≤25%, met); `index/` 64.5% (target ≤70%, met); `tests/artifact/*.test.ts` (build.test.ts, semantic-index.test.ts) unchanged and green, confirming the consolidation and reuse are behaviour-preserving | Stage-2 lane (unassigned) | fixed | metrics |
| 2026-09-03 | P2.1 close-out (BUGS row 2026-09-03 "overlayHash always null") | `src/artifact/write.ts`/`build.ts`, `src/artifact/service.ts` | **FIXED.** `WriteArtifactOptions.overlayStorePath` (optional): when given and the file exists, `writeArtifact` sha256-hashes its content into `manifest.render.overlayHash` (`buildManifest`'s `BuildManifestOptions.overlayHash` no longer hardcoded `null`). `ArtifactService`'s constructor, when handed the same `opts.overlayStorePath` AND the manifest recorded a non-null `overlayHash`, re-hashes the current file and throws `E_STALE_INDEX` on mismatch — the exact §7 A4 case ("re-render with one overlay name changed WITHOUT regenerating ranges"), since a rename alone never touches `ranges.jsonl`/`render.hash`. An artifact built with no overlay in scope (`overlayHash: null`) never false-flags a store that starts existing later (nothing to compare against). | `tests/artifact/stale.test.ts` extended to the full §4.2 matrix: ranges/render-hash mismatch (pre-existing), `index.builtFor`/bundle mismatch (new), and the overlay-hash case (new, exactly as described above — rename, re-check without re-writing throws `E_STALE_INDEX` naming "overlay store content hash", re-write clears it); the no-overlay-in-scope non-false-flag case is its own test | Stage-2 lane (unassigned) | fixed | metrics |
| 2026-09-02 | construct-fuzz re-triage of seed-base 777000 (v94+v99, `node tools/fuzz/construct-fuzz.mjs --versions 94,99 --count 30 --seed-base 777000 --out reports/fuzz/repro.json`) — 4 of the run's 5 DIVERGENT signatures (v94 seeds 777011 `arguments`-aliasing, 777018 TDZ-with-shadowing in a generator, 777026 `switch(-0)` case dispatch; v99 seed 777000 a custom `[Symbol.iterator]()` object) | `src/harness/ladder.ts`'s D14 VM cross-check (not the decompiler) | Directly instrumented each seed (`tools/fuzz/minimise-check-one.mjs`-style live run, `src/harness/hermes-vm.ts`'s `runHermesAsync`+`hermesPrintProjection` called by hand against the same `.hbc`): for all 4, `candidatePrint === hermesPrint` exactly -- the decompiled candidate matches the real Hermes VM's own trace of the original bytecode byte-for-byte. The DIVERGENT verdict comes from `ladder.ts` L160-215's *other* comparison (candidate vs `source.js` run under plain Node), which legitimately differs per D14 (arguments aliasing at 777011: Hermes's `ReifyArguments` does not write back to the parameter's own storage, Node's native sloppy-mode aliasing does; TDZ-with-shadowing at 777018: Hermes silently reads `undefined`, Node throws `ReferenceError`, same class as `30-tdz-shadowing`; likely a `-0`-vs-`0` switch-dispatch VM quirk at 777026, unconfirmed which comparator Hermes's case dispatch uses; a real VM-level `TypeError` at 777000 for the custom-iterator construct that Node's spec-compliant `for...of` never throws). The VM-agrees-with-candidate override at ladder.ts L168 only fires `if (candidatePrint !== hermesPrint)`; when they already match, the *original* Node-vs-candidate `cmp` is kept verbatim and only gets downgraded from DIVERGENT to a PASS-with-caveat if `opts.reference.knownDivergences.length > 0` (L212-216) -- populated solely from `reference-policy.ts`'s hand-curated `KNOWN_DIVERGENT_FIXTURES` table, keyed by fixture *name*. A fuzz-generated program has no curated name and can never be added to that table in advance, so this class of false DIVERGENT is structural, not per-seed. This is the same root cause `tests/fixtures/adversarial/README.md`'s CONSOLIDATION 26 triage already named for `20-symbol-keyed-properties` ("harness still says DIVERGENT only because ladder.ts's D14 cross-check never lets VM==candidate overrule a Node-vs-candidate divergence... unless the fixture is in KNOWN_DIVERGENT_FIXTURES") -- confirmed here to be general, not specific to that one fixture. | **FIXED** (2026-09-02, `src/harness/ladder.ts` + `tests/gate/harness/ladder-d14-override.test.ts`): the D14 VM-agrees-with-candidate override is now evidence-based, not curated-name-gated. `ladder.ts`'s trace oracle now sets a `vmAgreesEvidence` flag directly from the Hermes-VM cross-check (`candidatePrint === hermesPrint`, the real bytecode's own trace under the VM) and downgrades a DIVERGENT Node-vs-candidate verdict to PASS-with-caveat whenever that flag is set — for any program, curated or not — citing the actual VM comparison in the caveat text, never a `KNOWN_DIVERGENT_FIXTURES` lookup. `KNOWN_DIVERGENT_FIXTURES` remains as a fallback only for the no-VM case (v98, or a version with no built VM). Soundness verified two ways: (a) a new regression test compiles a nameless `arguments`-aliasing program (777011's shape) at v94 and confirms PASS-with-caveat citing vm-agrees evidence, with `reference.knownDivergences` empty; (b) the same test suite confirms `tests/fixtures/adversarial/43-fuzz-async-guard-shared-range` (a genuine, still-open candidate-vs-VM disagreement) still reports DIVERGENT — the override never fires without real vm-agrees evidence. Re-ran this row's own repro (`node tools/fuzz/construct-fuzz.mjs --versions 94,99 --count 30 --seed-base 777000`): v94 30/30 PASS, 0 DIVERGENT, 0 ERROR (previously 3 DIVERGENT of this row's class + 2 ERROR from the class-mutation row below); v99 29/30 PASS, 1 DIVERGENT (seed 777007, the real bug tracked in its own adversarial-43 row, unaffected as intended). | fuzz-lane follow-up (unassigned) | fixed | harness |
| 2026-09-02 | construct-fuzz seed-base 777000, v94 ERROR verdicts, seeds 777019 ("class Box { ... get v()/set v() ... }", mutated from the `21-class-private-fields`-style getter/setter-pair construct) and 777027 (`static {}` initializer block, mutated from a static-members construct) | `tools/fuzz/construct-fuzz.mjs` (driver) + `src/fuzzgen/mutate.ts` (generator) | Not a generator-invalid-JS bug: both programs are syntactically valid, spec-legal ES2022 (`node --check` passes; `class Box { get v(){} set v(){} }` and `static {}` blocks are ordinary class syntax). Confirmed directly against `tools/hermesc/v94/hermesc`: even the *minimal* `class Box { constructor(){this.v=1;} }` alone -- no getters, no static blocks -- is rejected ("invalid statement encountered", pointing at the `class` keyword itself), and so is `var Box = class {...}` ("Invalid expression encountered"). This RN-0.72.17-vintage v94 hermesc build has no class-declaration support at all, which is exactly why every existing class fixture (`32-class-basic` .. `36-class-getters-setters`, `58-class-accessor-pair-split`) has no `v94.hbc`/`v96.hbc` in this repo (`versions.txt`: "FAILS - hermesc rejects the class keyword entirely"). `src/fuzzgen/mutate.ts` has no version-awareness at all (confirmed by reading it: no `versions.txt` lookup, no per-construct version gating) -- it mutates a construct fixture's `source.js` and hands the result to whichever `--versions` the driver was invoked with, so any mutation of a version-gated construct (classes, and by the same logic any other `versions.txt`-gated construct) reported as a driver ERROR at an unsupported version is really "hermesc correctly rejected code this version was never meant to compile", not a toolchain fault or a generator bug. | **FIXED** (2026-09-02, `src/fuzzgen/mutate.ts` + `src/fuzzgen/generate.ts` + `tools/fuzz/construct-fuzz.mjs` + `tests/fuzz/mutate.test.ts`): `corpusSources`/`mutateFromCorpus` now take an optional target HBC version and, mirroring `src/harness/tiers.ts`'s `readVersionsTxt`, exclude any corpus fixture whose `versions.txt` marks that version `FAILS` — `generate()` threads the driver's `--versions` entry through so mutation mode never again hands a class-shaped (or any other version-gated) mutation to a hermesc build that has never supported it. Regression test confirms `mutateFromCorpus(seed, 94)` never selects a v94-gated fixture across 200 seeds. Re-ran the seed-base-777000 repro: 0 ERROR at v94 (previously 2, seeds 777019/777027). | fuzz-lane follow-up (unassigned) | fixed | harness |
| 2026-09-02 | construct-fuzz smoke campaign (seed-base 424242) follow-up triage of the row above (`59-jsx-runtime-calls` param-naming divergence, seeds 424246/424247/424251) | `src/harness/fuzz.ts` differential-fuzz oracle's TypeError-message comparison | Checked whether this is decompiler-fixable: `tests/fixtures/build.sh` compiles every construct fixture with plain `hermesc -emit-binary` (no `-g`), confirmed by reading the script directly -- no debug-info flag is ever passed, so `49-arguments-object`/`docs/lowering/arguments-object.md`'s own analysis (lazy accessor, `ReifyArguments`, no per-variable debug records) generalises: Hermes bytecode built this way carries **no parameter-name or local-variable-name table at all** for `59-jsx-runtime-calls`'s `v94.hbc`/`v99.hbc`. `items`/`log`/`strings` are not merely hard to recover, they are not present in the input to the decompiler in any form -- a hard ceiling, not a heuristic gap; the row above's own `owner`/`verdict` was mis-scoped as "prove fixed by fixing the naming pass". Separately: comparing two engines' **thrown TypeError message text** (`X.method is not a function`, `Cannot read properties of undefined (reading 'X')`, etc.) is not a sound equivalence check for a decompiler whose whole naming layer is a synthesised approximation by design (`fn-naming`/`var-naming`/`reg-split`) -- it reports a textual DIVERGENT on *every* program where a thrown error's message embeds an identifier or expression text, even when the throw type, throw site, and every other observable value match exactly (as they do here). That is a harness/oracle soundness gap distinct from the naming ceiling above: `src/harness/fuzz.ts`'s and `ladder.ts`'s trace comparators should either compare thrown errors by `(constructor name, thrown-vs-not)` only, or mask/normalise the identifier segment of error-message templates, rather than exact-text-matching a message format V8 and Hermes do not even agree on -- not changed here per this task's explicit instruction not to touch the oracle. | **FIXED**, oracle-soundness half (2026-09-02, `src/harness/trace.ts` + `src/harness/compare.ts` + `tests/gate/harness/compare-message-mask.test.ts`; the naming-ceiling half was already closed separately, see the `wontfix` row below): `compareTraces` (shared by `ladder.ts`'s trace oracle and any other caller) now compares `err`/`unhandled` trace records by constructor name and thrown-vs-not-thrown exactly, but identifier-shaped tokens inside the `.message` text are masked (`maskIdentifierTokens`, a small template-word allowlist so `TypeError`/`undefined`/`is not a function` etc. stay literal) before the two sides are diffed — a message that differs only in a synthesised-vs-source identifier (e.g. `items.map` vs `r13.map`) now matches. A masked-only match is never a silent pass: it is counted in `TraceComparison.maskedMatches` and `ladder.ts` surfaces it as a distinct, explicit caveat on the verdict. Verified conservative, not a full mask: a genuinely different constructor, a genuinely different thrown-vs-not-thrown shape, and a non-identifier structural message difference (`undefined` vs `null`) all still report DIVERGENT (five new unit tests). Ran the harness-affecting test scopes (`tests/gate/harness/`, `tests/fuzz/**`, `tests/gate/passes/adversarial-ladder.test.ts`, `tests/gate/docs/`) before and after: no existing fixture verdict changed. | fuzz-lane follow-up (unassigned) / M5 pass-ladder lane (naming heuristics) | fixed | harness |
| 2026-09-02 | segregation-without-deps brief (Service NSW, `docs/specs/08-segregation.md` §3), cross-module route-config walk (third revisit) | `src/split/segregate.ts` (`traceModuleOrigins`, `detectNavigatorKind`, `nameCustomModules`) | Prior revisits (this row, twice) confirmed the real blocker by hand: Service NSW's route names/`.component=` targets live in a route-config *factory* module (debug-named `routeConfig`/`<Domain>NavigationRoutes`) that a *separate* navigator module consumes at runtime via `Object.entries(...)`, never as literal `{name, component}` pairs in the navigator's own text — every same-module heuristic tried before this either recovered 0 screens or over-matched react-navigation-example (54→67/58→79 screens, a documented, reverted attempt). | **FIXED**, real cross-module walk, not a same-module regex widen: (1) `looksLikeRouteConfigFactory` recognises a module as a route-config producer from its own `routeConfig`/`*NavigationRoutes` debug function-name comment or self-export property write (an app-specific naming convention, confirmed across 4 independent modules in the sample bundle by hand) — gates three new resolution primitives, each *only* inside such a module (confirmed necessary: ungated, each regressed react-navigation-example through ordinary register-name reuse in an unrelated module, caught by this task's own hard bar before the gate was added): a `Reflect.apply(require, thisArg, [depmapIndex])` call spelling (`reflectTarget`, Service NSW's own require-call shape) alongside the existing bare-call form; a copy into/out of `src/split`'s own closure-captured `_eNNNN_M` environment slots (`envAliasTarget`/`envAliasTarget2`, needed because the require/dependencyMap parameters are routed through one); and a two-pass scan (a syntax-order problem no amount of gating fixes on its own — a nested function's *read* of an env slot appears in the text before the enclosing function's own *write* of it, so a single left-to-right pass can never see the write in time; pass 1 only populates alias/index/origin maps, pass 2 does the real, order-sensitive collection). (2) A third route-registry shape recognised inside a route-config-factory module: a registry accumulated via `<reg>.<RouteName> = <descriptor>;` (never a pre-shaped literal, so distinct from the two shapes already handled) whose value is a descriptor object resolved via its own `.component =`/`.screen =` assignment — gated on the factory signal plus excluding the factory's own self-export property name from ever being read as a route name (an observed false positive: `r4.VenueSignInNavigationRoutes = r0;`, the module's own export write, was briefly mistaken for a per-route registry key). (3) `detectRouteConfigConsumer` recognises the *navigator* half of the shape (a module with no `create<X>Navigator` call at all, only a `.entries(` walk over a required module's `routeConfig`/`*NavigationRoutes` property — `Object` is itself register-bound in the real bundle, `<reg>.entries(...)`, never the literal text `Object.entries(`) and feeds `detectNavigatorKind`'s existing kind-`""` fallback; `nameCustomModules` borrows such a consumer's route-set-for-naming-purposes from its own `deps`' already-resolved hits (real cross-module dataflow: the names it's named from live in a different module's text entirely). Committed fixture (`tests/gate/split/segregate.test.ts`, "cross-module route-config walk") hand-builds the exact shape (factory module with both resolution paths + a two-pass-dependent env-slot ordering, consumer module) since the real bundle can't be committed — screens resolve to `src/screens/<Name>.js`, the consumer navigator is named from the borrowed route-set prefix. Hard bar unchanged: react-navigation-example 4 navigators/54 screens WITH deps, 6/58 WITHOUT (`tests/gate/split/segregate.test.ts`, all pinned `assert.equal`s pass). Service NSW (never committed, numbers only): 0 → **36 screens**, 26 navigators (up from 26 generic-named ones; real names now include `CommonUIErrorScreen`, `DisasterHubScreen`, `Auth0LoginErrorScreen`, `ChangePinScreen`, `AnyFineDetailsScreen`, `CertificateOfRegistrationScreen`, real routes previously invisible). Full gate (`npm test`) 1354 pass / 0 fail / 17 skip. Remaining, real gaps (not attempted, out of this task's budget, no false positives from leaving them unhandled): a route's depmap index compiled as two statements (`r3 = 3; r3 = r20[r3];`, a literal assigned to a temp then used as a variable bracket index) rather than one (`r20[3]`) is not traced — `idxTarget` only matches a literal digit inside the brackets of the same statement — so some of Service NSW's own routes are still missed (never wrongly resolved, per the "missed positives, not false positives" standard this file already holds `traceModuleOrigins` to); and the root navigator (module-granularity "RootStack", merging several domains' route configs) has no common route-name prefix by design, so it keeps the generic `Navigator.js` name rather than a domain name — an already-documented gap (§3.1's "container role" fallback, not implemented). | M6 segregation lane (unassigned) | fixed | real-app |
| 2026-09-02 | M5 rung batch 3 (`default-params`), first real trace-oracle run of `51-default-params` at v94 | `src/passes/index.ts` (`pruneRegisterDecls`) | `default-params`'s writer can move every remaining use of a register out of a function's own `body` and into a `func.params[].init` (F15) — e.g. `defaultUsesFunction(a1, r0 = (r1 = 2, a1 * r1))`, where `r1` never appears in `body` again. `pruneRegisterDecls` only ever scanned `body` for liveness, so it dropped `let r1;` as dead. In non-strict emitted code (no `"use strict"`), the bare `r1 = 2` inside the default then created an implicit global `r1` — and the same thing happened to the caller's own unrelated `r1` (holding `print`, in `_fn0`'s own now-also-pruned decl), so calling `defaultUsesFunction` silently clobbered the caller's `print` reference: `r1 is not a function` at the next call site. Found by the trace oracle (`tests/gate/decompile/equivalence.test.ts`), not by inspection. | **FIXED**: `pruneRegisterDecls(body, params)` gained a second parameter — a register counts as live if it is read/written in `body` or in some `param.init` (checked via `identUses` on the `init` wrapped as a one-statement list, the same reads/writes counter, not a second implementation). `astPassHook` now passes `fn.params` through. Regression test: `51-default-params` is asserted PASS under the trace oracle in `tests/gate/decompile/equivalence.test.ts` (reproduced DIVERGENT before this fix, PASS after); `tests/gate/passes/ast.test.ts`'s existing `pruneRegisterDecls` unit tests keep passing unchanged (`params: []` default is a no-op). A second, related fix in the same commit: a multi-statement default body that produces and re-reads a genuine local register (not a pure-literal constant, e.g. `withSideEffectDefault`'s counter) is wrapped in a zero-argument IIFE with its own `let` (`collapseToIife`) rather than left as a bare comma expression — a parameter default's own scope is not the function's `body` scope (ES gives a non-simple parameter list a separate parameter environment), so a bare register assignment inside one has no legal binding to resolve against no matter what `body` declares. | M5 pass-ladder lane | fixed | passes |
| 2026-09-01 | CONSOLIDATION 4 (stage-A checker mutation testing) | `src/passes/for-header/check.ts` | `check` validated the promoted (do-while->while) case's `form.init.cfgBlock` against the block in front of the loop, but never validated `form.step` at all — neither `cfgBlock` nor `from`, in either the promoted or the already-`while` branch. Reproduced by hand on `countingLoop()` (04-for-loop-basic's shape): real rewrite gives `step: {cfgBlock:1, from:0}`; mutating to `step: {cfgBlock:99, from:0}` (a nonexistent block) or `step: {cfgBlock:1, from:999}` (right block, wrong instruction) both returned `{ok:true}` from the real `check`. A writer bug that folds in the wrong step instruction would slip through undetected — the emitted `for` head would print the wrong step expression. | **FIXED (commit `834aa77`)**: `check` now calls `match.ts`'s own `match(before, ctx)` — the pure function `check` always runs with the same `(before, ctx)` `match` itself was called with, so for any genuine site it reproduces the original `ForSite` exactly — and compares the re-derived `init`/`step` `{cfgBlock, from}` against `after.form.init`/`.step`. A wrong or missing step (or init) now diverges from the recompute and is rejected. Regression test: `tests/gate/passes/checker-mutation-stagea.test.ts`'s pinned case flipped from `test.todo` to an active assertion ("for-header/check rejects a rewrite whose step points at a block/offset the matcher never derived"). Full gate (`bash tools/deb/run.sh -- npm test`): 1326 pass / 0 fail, confirming the stricter checker still accepts every legitimate rewrite. | M5 pass-ladder lane | fixed | passes |
| 2026-09-01 | CONSOLIDATION 4 (stage-A checker mutation testing) | `src/passes/loop-cond/check.ts` | `check` validated the block sequence, that the annotated `form.cond` block is really an `if` inside the loop at the right (`head`/`tail`) position, and (for head-form) that the test block has no straight-line instructions — but never validated `form.kind` (`while` vs `do-while`) or `form.negate` at all. Reproduced by hand on `countingLoop()`: real rewrite gives `{kind:"do-while", cond:1, at:"tail", negate:false}`; mutating `kind` to `"while"` (same `cond`/`at`) or flipping `negate` both returned `{ok:true}` from the real `check`, though `match.ts`'s own comment says the two shapes (`head`->`while`, `tail`(-labeled)->`do-while`) are deterministic from which matcher fired — a writer bug that mislabels the kind or polarity would print the wrong loop keyword or invert the guard and slip through undetected. | **FIXED (commit `834aa77`)**: `check` now calls `match.ts`'s own `match(before, ctx)` the same way — reproducing the original `LoopSite` exactly for any genuine site — and compares the re-derived `kind`/`negate` against `after.form.kind`/`.negate`. A flipped while/do-while classification or a flipped test polarity now diverges from the recompute and is rejected. Regression tests: `tests/gate/passes/checker-mutation-stagea.test.ts`'s two pinned cases flipped from `test.todo` to active assertions ("loop-cond/check rejects a rewrite that flips form.kind", "... flips form.negate"). Full gate (`bash tools/deb/run.sh -- npm test`): 1326 pass / 0 fail. | M5 pass-ladder lane | fixed | passes |
| 2026-09-01 | CONSOLIDATION 4 (mutation-test the pass checkers) | `src/passes/expr-rebuild/check.ts` | `check` recomputed the R1a/R1b/R1c site's *classification* (rule, deadness, purity via `classifySite`) from `before` alone, and independently verified an exact read/write **count** delta on the folded register (`registerUseDelta`) — but never re-derived, and so never compared, the actual **value** the writer substituted at the read site. A mutated writer that folds in a wrong constant (or any other expression with the same read/write-count shape) instead of the register's real value passed the checker unchanged: same statement-count shape, same one-write/one-read delta, wrong semantics. Reproduced by hand: `r1 = 5; log(r1);` → correct rewrite `log(5);`, mutated rewrite `log(6);` — `check(before, mutated, ctx)` returned `{ ok: true }`. | **FIXED**: `check` now re-derives the site's `{ rule, i, j, reg, value }` the same way it already re-derives classification, then calls `rewrite.ts`'s own pure `rewrite({ root: before, data: site })` — the real writer's own code path — and compares the result structurally, byte-for-byte, against the actual `after` (same pattern `global-access`/`call-shape`'s checkers already use: recompute the expected output via the pass's own pure builder, never approximate it). Regression tests: `tests/gate/passes/checker-mutation.test.ts`'s pinned case flipped from `test.todo` to an active assertion that `check` rejects the wrong-constant mutation, plus two new active cases (wrong operator on the same operands, wrong operand register) — all three now correctly rejected with reason `"the rewrite did not fold in the expected value"`. Full `expr-rebuild` unit/gate suite (73 tests) and the whole `npm test` gate (1309 pass / 0 fail) stay green, confirming the stricter checker still accepts every legitimate rewrite. | M5 pass-ladder lane | fixed | passes |
| 2026-09-01 | QUEUE 6 (generator `.obf` `E_UNBOUND_IDENT`) | `src/passes/ast.ts` (`countUses`), `src/emit/ast.ts`/`src/emit/function.ts` | `decompile()` with every pass on threw `E_UNBOUND_IDENT: emitted identifier "r3" is not declared in any enclosing scope` on `23-generator-basic/v94.obf` and `26-infinite-generator-take/v94.obf` only (`--passes=none` was fine). Bisected with `--no-pass <name>` over every registered rung: `fn-naming` is the pass whose rewrite happens to fire inside the generator body function's own statement list (renaming its nested closures), which is what first flips `astPassHook`'s `r.applied.length > 0` gate and runs the framework's `pruneRegisterDecls` (F10) on that site — any other rung that fired there first would have triggered the same bug. | **FIXED**: the actual defect was in the framework, not `fn-naming`. `emit/function.ts`'s generator/async lowering returns a resume-dispatcher closure (`return function (__sent, __isReturn, __isThrow) {...}`) that is a `k:"func"` AST node but, unlike every other one, is not a separate Hermes `CreateClosure` — it is the same frame's own state machine, sharing the enclosing function's registers directly. `src/passes/ast.ts`'s `countUses` (behind `identUses`/`registerUses`, which `pruneRegisterDecls` calls) assumed every `k:"func"` boundary is a genuine separate register frame and never followed a register name across one — wrong for this one synthetic exception, so a register read only inside the resume closure counted as 0 uses and `pruneRegisterDecls` dropped its whole `let r0, r1, …` declaration out from under a live read. Fix: `Expr`'s `func` variant grew an optional `sameFrame?: true`, set only at that one construction site, and `countUses` treats a `sameFrame` closure as transparent — not a frame boundary — for every name, registers included; general rule, not a generator special case. Regression test: `tests/gate/passes/fn-naming-generator-obf.test.ts` (both fixtures: no `E_UNBOUND_IDENT` with all passes on, and PASS under the oracle at v94; the full `.obf`-at-every-version sweep already runs in `tests/sweep/decompile/sweep.test.ts`'s hardened tier). Same root cause plausibly explains the Service NSW `_fn14374 > … > _fn14377` `r15` `E_UNBOUND_IDENT` abort (BUGS row dated 2026-09-01) if that function is also an `isOpcodeGeneratorBody` (generator or async, which Hermes lowers through the same opcodes) — not attempted here, out of this task's scope. | passes lane | fixed | passes |
| 2026-09-01 | deps --confirm sweep verification (QUEUE "deps cluster") | `src/deps/confirm.ts` (`resolveCandidateVersion`/`nearestVersionByDate`) | "nearest npm release by date" can resolve a fast-moving package (`react-native-gesture-handler`, `react-native-screens`, `react-native-safe-area-context`, `react-native-pager-view` — all publish nightlies continuously) to an unbundleable nightly build, so `tests/sweep/deps/confirm-react-navigation.test.ts`'s own `>= 5 confirmed` floor sometimes isn't met on a live network run (2/5 confirmed one run, 5/5 another, same code) — precision stayed 100% every time observed, only recall is network-luck-dependent | **FIXED** (`01858d6`): `nearestVersionByDateDetailed` filters to stable (non-prerelease, no `-` suffix) npm releases first and only falls back to the nearest prerelease/nightly when a package has shipped no stable release at all, flagging that fallback (`usedPrerelease` / `ConfirmResult.usedPrereleaseVersion`) so `--confirm`'s report can surface it; `nearestVersionByDate` keeps its existing `string`-or-`null` signature. Regression tests (deterministic, no network) in `tests/gate/deps/confirm.test.ts`: "prefers the nearest STABLE release over a closer nightly/prerelease" and "falls back to the nearest prerelease, flagged, when a package has shipped no stable release at all". The original row's verdict (`tests/sweep/deps/confirm-react-navigation.test.ts`'s live-network `>=5/5` floor across repeated runs) is not re-verified here — that remains inherently network-luck-dependent for the exact count, but the root-cause selection logic it was tracking is now fixed and unit-tested. | deps lane | fixed | deps |
| 2026-09-01 | QUEUE 7 — `--split` correctness (real bundles throw `ReferenceError`) | `src/split/index.ts` | A module file could reference a nested function `_fnNNNN` declared in no split file: `emitModule`'s per-function closure-env analysis sometimes finds no creation site for a function (an "orphan"), so in the unsplit `decompile()` path it lands harmlessly at the shared top-level scope, but in `--split` it simply never appears in the one module file that calls it by name — a runtime `ReferenceError` (react-navigation's `module_8.js` calling undeclared `_fn1953`, 2921/2987 functions passes-off/on). | **FIXED**: `splitProject` now scans each module's printed factory text for `_fnN` references not declared anywhere in it, and pulls that function's already-decompiled body (`decompileAllBodies`'s `bodies` map, which holds every function `emitModule` reaches) in as an extra declaration nested *inside* the factory body (a real JS closure, not a file-top-level sibling — matters for the E2E harness's closure-tree comparison), transitively. A function referenced from two different modules is declared in the first (D17i has no shared-orphan-closure rule; not invented here, see the module's own diagnostic). Regression tests: `tests/gate/split/closure-closure.test.ts` (rn-template, passes on/off — a standing guard; rn-template never exhibited this specific cause, see the residual row below), `tests/sweep/split/closure-integrity.test.ts` (react-navigation-example-0.85.3, 0 dangling references after the fix vs 2188 before). `tools/e2e/roundtrip-corpus.ts`'s `tree:unmatched-closure` bucket for react-navigation fell from 2921/2987 to 1358/1493 (passes off/on) and % IDENTICAL rose 25.30%→28.72% / 29.52%→32.88% (`docs/e2e/roundtrip-baseline.json` bumped in the same commit); the residual bucket is a harder, still-open placement problem, tracked as a new row above. | M6 split lane | fixed | real-app |
| 2026-08-31 | npm-test-gate-speed task (gate wall-time investigation) | `src/passes/**` | `01-if-else-chain.min` at v84/v94 only decompiled to code with wrong runtime behaviour: the negative-number branch of a basic if/else chain was mishandled by the dead-store scan in `src/passes/expr-rebuild/match.ts`, which stepped past a `break`-to-outer-label arm and deleted the value the `break` carried to `return`. | **FIXED** (consolidation item 3, commit `4f8bc1a`): a four-valued `StepVerdict` makes a pass-through statement's `clear` terminal. Regression tests: `tests/gate/passes/expr-rebuild.test.ts` "not-dead (item 3)" and the fixture itself, now an ordinary gate PASS (`KNOWN_WRONG_OUTPUT` deleted). | fixed | fixed | passes |
| 2026-08-30 | review M4-M8 | `src/deps/dscan.ts` | `readLiterals(mod.literalValueBuffer, …)` was called without a version, so at v>=97 a tag-6 run desynchronised. | fixed in code as part of the deps landing (`1b679a3`, D17/D17a/D17b) — the version is passed at every `readLiterals` call site today; no gate fixture is a v>=97 Metro bundle, so the regression check remains the sweep-tier react-navigation-example inventory test until a gate fixture exists. | deps lane | fixed | deps |
| 2026-08-31 | adversarial fixture generation: 28-this-binding-extracted, 29-var-hoisting-redeclaration | `tests/fixtures/adversarial/README.md`'s committed `expected.txt` generation recipe | This repo's `"type": "module"` `package.json` force-parsed each fixture `source.js` as an ES module when generated with `require('./source.js')`, giving wrong `expected.txt` for both (28: `this` stayed `undefined` instead of substituting `globalThis`; 29: a top-level function/var collision threw `SyntaxError`). Neither is a real Hermes-vs-Node divergence — forcing CommonJS reproduces the fixtures' actual correct behaviour, matching both the Hermes VM and the real decompiler's output. | **fixed (CONSOLIDATION 24, commit `f723b9e`)**: 28/29 (and 36/41) regenerated with the README's explicit `--input-type=commonjs` recipe; regression test `tests/gate/harness/adversarial-expected.test.ts` re-derives every adversarial `expected.txt` in script mode on every gate run and rejects the ESM force-parse signature. | fixture lane | fixed | harness |
| 2026-08-31 | `src/harness/ladder.ts`'s D14 Hermes-VM cross-check (adversarial/36-optional-chaining-sideeffect, all versions) | harness comparison method | The cross-check compared the candidate's stdout-only print projection against the VM's raw combined stdout+stderr text, so any fixture whose top-level program legitimately throws uncaught (36: `null.method?.()`) always registered DIVERGENT even when both sides agreed on everything but the engine's own native error-message wording. | **fixed (CONSOLIDATION 25, commit `bc58f88`)**: both sides now project the same way — print lines + a normalised `uncaught <Name>` record, name only, never the engine-specific message. Regression test `tests/gate/harness/ladder-uncaught.test.ts`; fixture 36 through the real decompiler is PASS at v94/v96/v99. | M5/harness lane | fixed | harness |
| 2026-08-31 | CONSOLIDATION 26 triage of adversarial/21-class-private-fields (v99.hbc) | `src/emit/lower.ts` (`PutOwnGetterSetterByVal`/`DefineOwnGetterSetterByVal`, emit stage) | Static Hermes (v98/v99) lowers a class getter/setter pair as two instructions, each with the other half `undefined`; the emit unconditionally wrote a full `{get, set}` `defineProperty` on each, so the second call clobbered the getter with `undefined`. Confirmed real bug via minimal reproducer `tests/fixtures/constructs/58-class-accessor-pair-split` (v98/v99). | **FIXED (consolidation item 3, commit `142454e`)**: `src/emit/lower.ts` now omits the `get:`/`set:` descriptor key whose operand register statically holds a literal `undefined`; v99 trace-EQUIVALENT. Regression test: `58-class-accessor-pair-split@98/@99` removed from `KNOWN_WRONG_OUTPUT`, runs as an ordinary gate fixture. | fixed | fixed | emit-shape |
| 2026-08-31 | adversarial/06-closure-loop-var-vs-let (v94.hbc) | decompiler closure capture | For-loop `let` bindings appeared to be treated like `var`, so closures captured the final loop value instead of per-iteration values. | **d14-legit**: Hermes shares one binding across `let`-loop iterations too (matches the known `18-closure-loop-let` divergence, `reference-policy.ts`); the decompiled output matches the Hermes VM at v94/v96/v99. Only Node's spec-correct `expected.txt` differs — a documented D14 caveat, not a bug. `tests/gate/harness/reference-policy.test.ts` lists it in the known-divergence set. | M5 | d14-legit | semantics |
| 2026-08-31 | adversarial/02-proxy-trap-counting (v94.hbc) | decompiler Proxy handler | `'x' in proxy` was reported to not invoke the `has` trap (count=0 instead of 1). | **fixed**: no longer reproduces as of CONSOLIDATION 26 — `runTier` is PASS at v94/v96/v99 and the decompiled v94 candidate's output is byte-identical to the v94 VM's own run (`has traps: 1`). Fixed as a side effect of an intervening commit, not bisected; regression test `tests/gate/decompile/regressions.test.ts` ("an unused 'in' still invokes the Proxy has-trap") and adversarial fixture 02 remain as the regression tests. | M5 | fixed | semantics |
| 2026-08-31 | adversarial/28-this-binding-extracted (v94.hbc) | decompiler this binding | Method extracted and called without context appeared to show undefined-value instead of expected error/undefined-this behaviour. | **duplicate**: same root cause as the "adversarial expected.txt ESM" row above (CONSOLIDATION 24) — the committed `expected.txt` was wrong, not the decompiler. Per README's status table, decompiled output matches the Hermes VM *and* Node's real sloppy/CJS execution at v94/v96/v99 (standard spec-conformant sloppy `this` substitution). | M5 | duplicate | semantics |
| 2026-08-31 | adversarial/29-var-hoisting-redeclaration (v94.hbc) | decompiler hoisting | Var redeclaration after function with same name appeared to diverge from expected output. | **duplicate**: same root cause as the "adversarial expected.txt ESM" row above (CONSOLIDATION 24). Per README's status table, decompiled output matches the Hermes VM and Node's real CJS execution at v94/v96/v99 — no actual redeclaration conflict under sloppy/CJS semantics. | M5 | duplicate | semantics |
| 2026-08-31 | adversarial/30-tdz-shadowing (v94.hbc) | decompiler TDZ | Shadowing with TDZ (accessing `inner` before declaration in a scope where `inner` exists in outer scope) appeared to diverge from expected — thought to need a ReferenceError. | **d14-legit**: Hermes genuinely does not raise the TDZ ReferenceError for this shadowing shape (confirmed even under plain CJS Node, independent of 28/29's module-type issue) — decompiled output matches the VM at v94/v96/v99. Only Node's spec-correct `expected.txt` differs. `tests/gate/harness/reference-policy.test.ts` lists it in the known-divergence set. | M5 | d14-legit | semantics |
| 2026-08-31 | adversarial/36-optional-chaining-sideeffect (v94.hbc) | decompiler optional chaining | Optional chaining with side effects appeared to throw during decompiled execution differently from expected. | **duplicate**: false positive of the ladder cross-check artefact (row above, fixed under CONSOLIDATION 25, commit `bc58f88`). Fixture 36 is PASS-vs-VM at v94/v96/v99. | M5 | duplicate | harness |
| 2026-08-31 | gate-speedup agent | decompiler (v84/v94) | `01-if-else-chain.min` decompiled to WRONG output (`-5 -> undefined` instead of `-5 -> negative`). | **duplicate** of the "npm-test-gate-speed" if-else-chain row above; same fix (consolidation item 3, `expr-rebuild` `StepVerdict`, commit `4f8bc1a`); `KNOWN_WRONG_OUTPUT` deleted. | fixed | duplicate | passes |
| 2026-08-31 | gate-speedup agent (equivalence run) | `src/passes/label-clean` | `decompile()` never returned for `37-destructuring-array` (all versions) and `48-optional-chaining-nullish` (v84/v94) with label-clean enabled. Pass was disabled in `registry.ts` on 2026-08-31 as an emergency mitigation (`--no-pass` confirmed the workaround). | **fixed (commit `e239662`, "M5 rung 7 label-clean: fix the infinite loop on destructuring/optional-chaining and re-enable the pass")**: `labelClean` is back in `REGISTRY` (`src/passes/registry.ts`); regression tests `tests/gate/passes/label-clean.test.ts` ("37-destructuring-array and 48-optional-chaining-nullish decompile without hanging" and the full-pipeline trace-oracle test) cover the previously-hanging fixtures. | (was: pass disabled) | fixed | passes |
| 2026-09-01 | E2E tier 1 — bucket `diff:StartGenerator/CreateEnvironment`, 856 fns Service NSW (v96) | `src/emit` (generators via `__hbc_makeGenerator`, D9) | Generator functions are emitted through the runtime helper rather than as `function*`, so their recompiled form has no `StartGenerator`/`CreateGenerator` at all and is never IDENTICAL by construction. | **wontfix (by design, D9)**: expected until a `function*` re-raising rung exists (spec 07 batch 4); recorded so the E2E number is read correctly (~2% of NSW's functions). No test needed for the harness per the original row. | M5 ladder (generator rungs) | wontfix | emit-shape |
| 2026-09-01 | QUEUE 5 (`deb` job server) proof run: `tools/deb/run.sh -- npm test` on a byte-identical fresh checkout on `deb` (Linux) | `src/deps/confirm.ts:160-161`, `src/deps/guess.ts:155-156` (`res.ok`/`res.json()` on the global `fetch` `Response`) — likely toolchain (TypeScript global-type resolution), not the two call sites themselves | `npm run typecheck` fails only on a truly clean Linux checkout (`git worktree add` + fresh `npm ci`, same lockfile-pinned `typescript@5.9.3`/`@types/node@22.20.1`/`undici-types@6.21.0`, byte-identical `tsconfig.json` and `.d.ts` files verified by `sha256sum`) with `TS2339: Property 'ok'/'json' does not exist on type 'Response'`; the identical command against the identical commit is green on macOS, both against a pre-existing `node_modules` walked up from a parent checkout and against a fresh `npm ci` run directly in the worktree. A `tsc --showConfig` diff and a probe file (`typeof globalThis extends {onmessage:any}`, the exact conditional `@types/node`'s `web-globals/fetch.d.ts` uses to decide whether to defer to `undici-types`' `Response` or an assumed-present DOM one) both confirm the two checkouts agree lib-for-lib and resolve the same "no DOM" branch — so the divergence is not explained by anything found so far; root cause unknown. | fixed (deb-ci infra, not a type bug): the job server cached node_modules as `nm-cache/<hash>/<pkg>` and symlinked it; tsc resolves through the symlink and walks up for an ancestor literally named `node_modules/undici-types`, which did not exist → `@types/node`'s fetch `Response` degraded on Linux only. Fixed by caching as `nm-cache/<hash>/node_modules` (dea83c0); `npm test` on deb exit 0 in 41 s. | overseer | fixed | toolchain |
| 2026-09-02 | Fred's own review ("`StackNavigator.N.js` are bad names") + the row above's own "already-documented gap" note (§3.1's "container role" fallback, not implemented) | `src/split/segregate.ts` (`nameCandidateFor`, new `roleNameForRoutes`/`domainToken`) | The route-set-prefix naming above (`commonRoutePrefix`) only fires when every one of a navigator's routes shares a literal prefix — a root/tab container merging several unrelated domains (Service NSW's own top-level `RootStack`, react-navigation's typical root switch) has no such prefix by design, so it kept the generic `<Type>Navigator.js` name (still ordinal-suffix-colliding across an app's several stack/tab navigators) even after the route set itself was fully resolved. | **FIXED**: `roleNameForRoutes` (gated to navigators with >= 4 resolved routes, so a plain two-screen stack like `Home`/`Profile` keeps its call-shape name — the "several domains" shape this targets is a real aggregator, not a small navigator that merely lacks a shared prefix) names a navigator from whichever single domain token (`domainToken`: the leading all-caps abbreviation or camelCase word of each route name) covers at least half its routes (`LicenceScan`/`LicenceRenew`/`LicenceHistory`/`Profile` -> `LicenceNavigator`, a real majority-domain navigator with one outlier), falling back to a deterministic role name (`MainTabNavigator` when the call-shape kind names a tab factory, `RootNavigator` otherwise) when no domain dominates a genuinely diverse route set. Three new committed fixtures (`tests/gate/split/segregate.test.ts`): diverse-domain stack -> `RootNavigator`, diverse-domain `createBottomTabNavigator` -> `MainTabNavigator`, dominant-domain-plus-outlier -> `LicenceNavigator`; all three existing 2-route `StackNavigator` pinned assertions unchanged. Hard bar unchanged: react-navigation-example 4 navigators/54 screens WITH deps, 6/58 WITHOUT (`tests/gate/split/segregate.test.ts`, all pinned `assert.equal`s pass) — its own navigators' route sets never resolve on this fixture (each keeps its pre-existing call-shape name), so this change is a pure addition there, not a rename. Service NSW (never committed, numbers only, no `--deps-report`, same fast path as the row above): of 26 navigators, 3 now get a route/role-derived name (`VenueSignInNavigator` from a resolved route-set prefix, two `RootNavigator`s from the new role fallback) vs 23 still on the generic call-shape fallback (`StackNavigator`/`StaticNavigator`/`BottomTabNavigator`/`MaterialTopTabNavigator`, with ordinal suffixes) — real screens count unchanged at 36 (this task is naming-only, no new resolution paths). Remaining gap, not attempted (out of this task's budget): most of NSW's 23 still-generic navigators have no resolved route set at all yet (the underlying screen-resolution gaps this row's parent already tracks — the two-statement depmap index, and route-config shapes not yet recognised by `looksLikeRouteConfigFactory`/`detectRouteConfigConsumer`), not a naming-rule miss; `roleNameForRoutes`/`commonRoutePrefix` only ever act on a route set that's already been resolved. | M6 segregation lane (unassigned) | fixed | real-app |
| 2026-09-02 | M5 rung 16 (`destructure`) implementation, spec 16 §2.6's version-differences table | `docs/specs/passes/16-destructure.md` §2.6 (spec text, not code) | The spec's own v94 column claims `greet`'s `= {}` object-pattern parameter default is "already rewritten (`function _fn1(r2 = {})`)" by `default-params` before this rung ever sees it. Measured directly (`npx tsx src/cli.ts tests/fixtures/constructs/38-destructuring-object/v94.hbc --no-pass var-naming --no-pass fn-naming --no-pass default-params --function=1 --emit-ast` vs. the same without the last flag): `default-params` does **not** fire on this shape at v94 either — the `L0: { r2 = arguments[0]; … }` prologue survives untouched, identically to what the spec correctly documents for v99. Not a code bug (the destructure rung's own object rule keys on the observed source register regardless of how it came to hold the value — spec §8 Q2's own escape hatch — so implementation is unaffected either way; `greet`/`makeUser` both destructure correctly at v94 *and* v99 in the real pipeline), just a wrong empirical claim in the merged spec. | doc corrected in `docs/lowering/destructuring.md` §7 in the same commit as this row (spec 16 §2.6 itself left as merged text; the lowering doc is the authoritative measurement record) | M5 pass-ladder lane (unassigned) | duplicate | passes |
| 2026-09-02 | "resolve more route entries" brief (fifth revisit of the segregation-without-deps/Service NSW route-config walk row above) | `src/split/segregate.ts` (`traceModuleOrigins`: new `numLitByReg`, `idxTarget`'s `idxRegRef` alternative) | The third revisit's own tracked gap: a route's depmap index compiled as TWO statements (`r8 = 1; r3 = r20[r8];`, a numeric literal assigned to a plain register then used as a *variable* bracket index) rather than one (`r20[1]`, already handled) was not traced — `idxTarget` only matched a literal digit inside the same statement's brackets — so some of Service NSW's own routes stayed unresolved. | **FIXED**: a new `numLitByReg` map records every `<reg> = <digit literal>;` seen (tracked unconditionally — it only ever feeds the pre-existing `idxBase === "depmap"` gate below it, so recording it costs nothing extra to guard); `idxTarget`'s bracket alternative now accepts either a literal digit (`idxNum`, unchanged) or a register (`idxRegRef`, new), looking the latter up in `numLitByReg`. Committed fixture (`tests/gate/split/segregate.test.ts`, "resolves a route whose depmap index is built as two statements") hand-builds the exact shape since the real bundle can't be committed. Hard bar unchanged: react-navigation-example 4 navigators/54 screens WITH deps, 6/58 WITHOUT (`tests/gate/split/segregate.test.ts`, all pinned `assert.equal`s pass) — it never uses this call spelling. Service NSW (never committed, numbers only, no `--deps-report`): screens 36 → **176** (~5x — most of NSW's route-config factory modules used this exact spelling for at least one route); real names now include `PayFinesScreen`, `RegistrationsScreen`, `CertificateOfRegistrationScreen`, `ChangePinScreen`, `Auth0LoginErrorScreen`. Navigators recognised 26 → 18 and route/role-named navigators 3 → 1 (`RootNavigator`) as an expected side effect, not a regression: several modules previously fell back to a generic navigator name only because the route pointing to them (from a sibling navigator's now-resolved route config) hadn't resolved yet, and `nameCandidateFor`'s existing (unchanged) precedence already prefers a resolved screen-route hit over a module's own navigator-ness — a nested navigator used as another navigator's route target now correctly files as that route's screen instead (matches react-navigation's own model: a Stack/Tab navigator embedded as a screen component IS a screen from its parent's perspective), not information lost. Investigated, not shipped: most of NSW's remaining 18 "navigators" only re-export a bare `create<X>Navigator` factory result (no route registry of their own at all) — `detectNavigatorKind`'s call-shape regex matches a factory-result property access the same way it matches a real call site, a pre-existing (not introduced here) shape-detection imprecision in navigator *detection*, out of this task's scope (route *resolution*); produces no false-positive screen/route, only a less-specific navigator name, so no BUGS row opened for it (not confirmed as a bug rather than a working-as-designed ambiguity — flag for a future navigator-detection task, not filed here). | M6 segregation lane (unassigned) | fixed | real-app |
| 2026-09-02 | navigator-detection tightening brief (following the row above's own "flag for a future navigator-detection task") | `src/split/segregate.ts` (`detectNavigator`, new `moduleOwnsOrConsumesRouteConfig`/`looksLikeBareFactoryReexportShape`) | The row above's own finding: `detectNavigatorKind`'s call-shape regex (`\.create([A-Za-z]+?)Navigator\b`) matches a bare property READ off a required module (`r5 = r0.createStackNavigator;`) exactly as readily as a module that goes on to build a real route registry -- a module whose entire job is require + read/call the factory + export the result, with no `{name, component}` registry of its own and no `.entries(` consumer walk, is not a navigator. | **PARTLY FIXED, gated narrowly**: `moduleOwnsOrConsumesRouteConfig` (own `keyAssignments` non-empty, via the existing `traceModuleOrigins`, or `detectRouteConfigConsumer`) is the real ownership/consumption test; `detectNavigator` now refuses a call-shape match that fails it. Investigated (hand-inspection, both this repo's own fixtures and Service NSW's own `--split` output) and confirmed the exact same ambiguity exists inside react-navigation-example-0.85.3's own pinned hard-bar fixture: all 4 of its currently-counted "navigators" (`module_1086`/`1122`/`1611`/`1641`) are actually `@react-navigation/*` package barrel/index files (many nested `_fnNNNN` lazy-getter re-exports, `Link`/`LinkingContext`/other unrelated factories alongside the Navigator one) misclassified `custom`/`src` by an out-of-scope `classify.ts` package-boundary gap, not app navigators -- 3 of the 4 (`1122`/`1611`/`1641`) are themselves the exact bare-re-export shape this row targets, and the ownership test alone correctly excludes them (confirmed: applying `moduleOwnsOrConsumesRouteConfig` with no further gate drops react-navigation-example from 4 navigators to 1, `4->1`/`6->1`), which the task brief's own HARD BAR forbids (`assert.equal` pins must hold; "5 prior agents held this line"). Shipped a narrower, hard-bar-safe gate instead (`looksLikeBareFactoryReexportShape`: at most one `function` declaration in the module's own text -- a real app-level bare-reexport wrapper is a single flat factory function in every fixture measured, while a package barrel index is many nested getters): only excludes a call-shape match when it is BOTH flat AND owns/consumes nothing. Service NSW (never committed, numbers only, no `--deps-report`): navigators 18 -> **17** (one genuinely flat bare re-export dropped), route/role-named unchanged at 1 -- most of NSW's other 16 "navigators" turned out, on inspection, to actually own a `.name =`/`.component =` registry (12/17 modules match that pattern directly in their own text, just with incidental nested closures -- e.g. arrow-function screen-option callbacks -- that the flatness gate conservatively treats as "maybe a barrel, leave it"), so they were correctly real navigators mis-flagged as suspicious by the fifth revisit's guess, not remaining bare-reexport false positives. react-navigation-example UNCHANGED: 4 navigators/54 screens WITH deps, 6/58 WITHOUT (`tests/gate/split/segregate.test.ts`, all pinned `assert.equal`s pass) -- hard bar held. New committed fixture (`tests/gate/split/segregate.test.ts`, "a bare create<X>Navigator re-export ... is not counted as a navigator") hand-builds the flat require+call+export shape with an externally-sourced route config (module 15), confirming it is excluded with and without a deps report. **Remaining, real gap** (not attempted, blocked by the hard bar, `docs/PUSHBACK.md` P-10): the full ownership-based gate (no flatness restriction) is the *more correct* fix -- it keeps react-navigation-example's one genuine navigator (`module_1086`, `DrawerNavigator`, real `App`/`Examples`/`Home`/`NotFound` routes) and correctly drops its 3 barrel false positives, which is exactly this brief's own stated goal ("some things currently counted as navigators are bare factory re-exports that shouldn't be") -- but doing so moves the fixture's pinned hard-bar numbers from 4/54,6/58 to 2/54,2/58 (screens unaffected, only the navigator-only, no-screen-of-its-own barrel modules drop), which the task brief explicitly forbids without Fred's review (golden/pinned-fixture regeneration needs approval, reviewed as a batch, per this repo's CLAUDE.md). Applying the fuller fix to Service NSW's own remaining 5 no-registry navigators (`StaticNavigator`/`StaticNavigator.2`/`StackNavigator`/`StackNavigator.2`/`MaterialTopTabNavigator`/`BottomTabNavigator`/`BottomTabNavigator.2`, 7 of the 17, still generic-named with `nameComp=false` in hand inspection) needs the same broader gate. | M6 segregation lane (unassigned) | fixed | real-app |
| 2026-09-02 | P-10 (`docs/PUSHBACK.md`), "package barrel/index modules misfiled into `src/`" brief | `src/split/segregate.ts` (`segregateSplitTree`'s bucketing loop) | The row above's own real, tracked gap: this fixture's 3 barrel false-positive "navigators" (`module_1122`/`1611`/`1641`) are misfiled to `src/` not because of `detectNavigator`'s call-shape regex but one stage upstream — `segregateSplitTree` bucketed every module by `classify.ts`'s heuristic `classification` alone, and a package barrel/index's re-exported names (`createStaticNavigation`, `createMaterialTopTabNavigator`, ...) shape-match the app's own PascalCase Screen/Navigator vocabulary token closely enough to fool classify.ts's app-vocabulary signal into calling the module CUSTOM, even when `runDeps`'s own `moduleOwnership` (hash-matched against the signature DB, confirmed-tier only) already resolves that exact module id to a real package. | **FIXED, at the segregation layer**: a confirmed per-module `moduleOwnership` entry now takes precedence over classify.ts's `classification` in `segregateSplitTree`'s bucketing loop — never invented (only acts when match.ts already confirmed this exact module against the signature DB), never overriding a `library`-classified module's own bucket (only `custom`/`unknown`/no-data), so no real app-vs-library verdict is weakened, only a stronger, independently-derived signal takes precedence over a weaker heuristic one where they disagree. Measured on react-navigation-example-0.85.3: navigators 4→**3**, screens 54→**50** (WITH deps only; WITHOUT deps has no `moduleOwnership` to read and is unchanged at 6/58) — module 1122 (`@react-navigation/native`, confirmed) moves to `node_modules/@react-navigation/native/` and drops out of naming's `src`-bucket candidate set entirely, matching this row's own diagnosis exactly. Narrower than the sixth revisit's "4→2" guess: hand-checking the fixture's own `deps-truth.json` (test-only ground truth, never read by production code) found module 1641 is genuinely app code (real per-route logic, not a barrel shape — the sixth revisit's hand inspection over-called it) and module 1611 IS a confirmed `@react-navigation/material-top-tabs` barrel by that same ground truth but has no `moduleOwnership` entry to act on since `material-top-tabs` isn't in the signature DB at all (new open BUGS row above tracks that follow-up). Whole-bundle effect measured with `tools/e2e/oss-benchmark.mjs`: package-level classification recall improved 6.1%→**9.1%** (more real dependencies now get at least one correctly-bucketed module: `@react-navigation/native`, `@react-navigation/stack`, `react-native`, `react-native-screens`, `react-native-reanimated`, `react`, `@react-native-async-storage/async-storage`, `react-native-gesture-handler` all now represented, vs 6 packages before); the same benchmark's "precision" metric measured **lower** (52.2%→41.2%), traced by hand to a benchmark ground-truth limitation specific to this one monorepo-built fixture, not a real accuracy regression — react-navigation-example builds `@react-navigation/*` from workspace source (`/packages/native/src/...`), never a literal `node_modules/@react-navigation/...` path, so the benchmark's `loadTruth` (`tools/e2e/oss-benchmark.mjs`) can never credit ANY `@react-navigation/*` module attribution, correct or not, and 607 of the 1033 confirmed-owned modules after this fix are exactly those two packages (up from 289 before — already miscounted the same way pre-fix, not newly introduced); the packages the benchmark CAN credit (react-native, react-native-screens, reanimated, react, async-storage, gesture-handler) account for the recall gain and show the fix is real. Not fixed here (documented, not attempted): the benchmark's `loadTruth` could credit workspace-linked packages too (matching non-`node_modules` non-app source path segments against `confirmedDeps` package names the same way `appSourcePrefix` already special-cases app code) — flagged for a future e2e-benchmark task, no BUGS row opened since the benchmark tool itself, not the pipeline, is what's imprecise here. Pinned `assert.equal`s in `tests/gate/split/segregate.test.ts` updated to 3/50 (WITH deps) with the full before/after evidence inline as a code comment; `docs/specs/08-segregation.md` §6's "Seventh revisit" has the same evidence. | M6 segregation lane (unassigned) | fixed | real-app |
| 2026-09-02 | generalization-sweep brief (real, non-fixture apps: Brex, Uniswap, Rainbow, Bluesky, com.brex.mobile/com.uniswap.mobile local corpus) | `src/split/segregate.ts` (`traceModuleOrigins`'s `routeObjRegs`) | Screen-naming over-fit to the two fixtures it was built/tested against (Service NSW, react-navigation-example): `detectScreenHits`'s pre-jsx-recover `{ RouteName: Component }` registry-literal shape (`routeObjRegs`) was the one route-registry shape in `traceModuleOrigins` never gated on any navigator evidence in its own module — every other shape already requires a `create<X>Navigator` call or the NSW `routeConfig`/`*NavigationRoutes` naming convention. On apps with no react-navigation usage at all, an unrelated PascalCase-keyed data registry shape-matches it exactly: Brex's css-tree dependency (`{AtrulePrelude: null, AttributeSelector: null, AnPlusB: null, ...}`, one required module per AST node type, in a plain node-type registry) produced 71 fake "screens" (`AtrulePreludeScreen`, `AttributeSelectorScreen`, ...); Uniswap produced 45, some via straight register reuse pulling in an unrelated lowercase-keyed assignment on the same reused register (`allowedPrivateKeyLengthsScreen`, `__closureScreen`). Rainbow/Bluesky (numbers not independently reproduced here, proprietary corpus) reported the same shape (`FONT_WEIGHT_MAPPINGSScreen`, `SLOPE_FACTORScreen`) — and the identical pattern turned out to be bundled inside react-navigation-example-0.85.3's own dependency tree too (module 582, an unrelated font-weight-constants module), invisible until this fix stopped accepting it. | **FIXED**, two structural gates, no token denylist: (1) the registry-literal shape is only trusted when its own module shows navigator evidence (`scanJsxScreenProps`/`scanRouteConfigFactory`, same gate every other shape in the function already uses) **or** is a direct (one-hop only, not transitive) dependency of a module that does (`consumedByNavigator`, computed once in `nameCustomModules` from the full module graph) — the one-hop case is load-bearing, not just a safety margin: react-navigation-example's own top-level `SCREENS` registry module never calls a navigator factory itself, it's required directly by the `createDrawerNavigator`-calling module that consumes it. (2) the assigned key at the consumption site must itself look like a route name (`/^[A-Z]/`), closing the register-reuse gap fix (1) alone didn't catch. Committed fixture (`tests/gate/split/segregate.test.ts`, new test asserting a non-navigator PascalCase-keyed data module produces zero screens) plus the existing route fixtures kept green. Measured on real (proprietary, local-corpus, numbers only — never committed) apps: Brex 71→0 screens, Uniswap 45→0, Service NSW **176→176 unchanged** (17→17 navigators), react-navigation-example-0.85.3 WITH deps 3/50 unchanged (§6), WITHOUT deps 6 navigators unchanged / screens re-pinned 58→52 (the 6 dropped were themselves this exact bug, see test's inline comment and `docs/specs/08-segregation.md` §3's eighth revisit). Full detail: `docs/specs/08-segregation.md` §3, eighth revisit. | M6 segregation lane (unassigned) | fixed | real-app |
| 2026-09-02 | P-11b (reg-split default-on attempt, `docs/PUSHBACK.md` P-11) | `src/passes/jsx-recover/match.ts` (and/or the object-literal-merge step it depends on) vs `src/passes/reg-split` | Flipping `reg-split` to `optIn: false` (default-on) makes `jsx-recover` (`--jsx`) stop recovering JSX on `tests/fixtures/constructs/59-jsx-runtime-calls` at both v94 and v99. Without reg-split, the fixture recovers cleanly (`<_e0_2 style={r6}>hello</_e0_2>`, etc. — `tests/gate/passes/jsx-recover.test.ts`'s existing assertions). With reg-split in the default set, reg-split renames the object-literal-build register into per-store copies (`r3`, `r3_2`, `r3_3`, ...) and the elements that depended on that register being reused across the object-literal-then-property-writes-then-call shape stay as plain calls/property assignments instead of JSX — confirmed directly by running `decompile(bytes, { passes: { optIn: ["jsx-recover"], skip: ["reg-split"] }, emit: { jsx: true } })` (recovers) vs the same with reg-split left in (does not recover), both v94 and v99. Not a soundness problem (reg-split's own checker and the 16 `tests/gate/passes/reg-split.test.ts` tests are unaffected) — this is a downstream readability-pass pattern-match failure caused by reg-split's renaming, discovered while trying to finish P-11b; `optIn` was left `true` (reverted from the default-on flip) rather than loosening `jsx-recover`'s test to accept the regression. | resolved by the D23 stage-boundary reorder (2026-09-03, `src/passes/registry.ts`, `docs/DECISIONS.md` D23): `jsx-recover` moved to the end of the structure-recovery block (still before the renaming block: `fn-naming`, `reg-split`, `var-naming`), so `reg-split` no longer runs before it. Verified: both existing `jsx-recover.test.ts` JSX-recovery assertions on `59-jsx-runtime-calls` v94/v99 pass with `reg-split` in the default set; `reg-split`'s 16 rung tests and its spec's five §10 target fixtures stay 0-DIVERGENT; full `npm test` 1753/1753 pass. `reg-split.optIn` flipped to `false` (default-on), closing `docs/PUSHBACK.md` P-11's follow-up. | M5 pass-ladder lane (unassigned) | fixed | passes |
| 2026-09-02 | construct-fuzz smoke campaign (seed-base 424242, work range, `tools/fuzz/construct-fuzz.mjs`), differential-fuzz oracle on `tests/fixtures/constructs/59-jsx-runtime-calls/source.js`'s `screen`/`html`/`cleanup` functions | `src/passes` naming heuristic (var/param naming) + `src/harness/fuzz.ts`'s differential function fuzz oracle | `59-jsx-runtime-calls` is an existing gate-passing fixture; its own `source.js` top-level code never calls `screen`/`html`/`cleanup` with argument types that hit their internal `.map`/`.reduce`/`.push` TypeError paths, so the gate's trace oracle never sees this. The construct-fuzzer's differential-fuzz oracle calls these functions with a wider argument space and three distinct argument combos trip a `TypeError` whose message embeds the accessed variable's *name* -- decompiled candidate names diverge from source names (`r13`/`arr`/`r3` vs `items`/`log`/`strings`). Every other aspect of the trace matches exactly -- purely a naming-recovery gap surfaced only by comparing thrown-error message *text*. | reclassified, not fixed: `tests/fixtures/build.sh` compiles every construct fixture without `-g` (confirmed by reading it), so no parameter/local debug-name table exists anywhere in `59-jsx-runtime-calls`'s `.hbc` for any version -- the original names `items`/`log`/`strings` are absent from the decompiler's input by construction, not merely hard to infer. This is a documented recoverability ceiling (same class as the register-name fallback `docs/lowering/arguments-object.md` documents, and the synthesised-name passes `fn-naming`/`var-naming`/`reg-split` already exist for), not a `src/passes` bug -- the naming heuristics are already doing the only thing possible without debug info. The distinct harness-soundness question this triage also raised (is exact-text comparison of thrown TypeError messages a sound oracle at all, given it embeds a name no non-`-g` decompiler could ever recover) is filed separately as its own open row (2026-09-02, `harness` cluster) rather than folded into this ceiling. | M5 pass-ladder lane (naming heuristics) / fuzz-lane follow-up (unassigned) | wontfix | passes | ceiling confirmed by reading `tests/fixtures/build.sh` (no `-g` on any construct fixture compile) -- ordinary gate PASS for `59-jsx-runtime-calls` at both versions is unaffected; the harness-oracle-soundness half of this finding continues as its own open row above. |
| 2026-09-03 | fix-wave item, docs/QUEUE.md "FIX WAVE" finding (appgen triple `d4e1aacf818f482d`, "tabs" router, "lazyRequire" depStyle: navigator DETECTED, screens 0/4) | `src/split/segregate.ts` (`traceModuleOrigins`) | A screen loaded through a source-level thin-loader IIFE (`function loadFoo() { return require('./Foo').default; } const Foo = loadFoo();`, called immediately, not `React.lazy`) was invisible to §3.2: Hermes inlines the trivial IIFE at compile time (no separate function survives in the decompiled text at all), so the require + interop-`.default` hop + closure-capture-slot (`_eNNNN_M`) write land at the END of the navigator module's own top-level statements — textually AFTER the nested `<Nav.Screen component={Foo} />`-building closure that reads the slot back out. `traceModuleOrigins`'s single left-to-right scan never saw the write in time to resolve the read. | **FIXED**: a new self-contained regex (`ENV_SLOT_ORIGIN_RE`) resolves a `_eNNNN_M` slot's origin directly from its own bounded `<reg> = require(<dep>); [<reg> = <reg>["default"] or .default;] <slot> = <reg>;` statement run, independent of text order and of this scan's ordinary (order-dependent) register tracking — then a new adjacent-statement alternative in `TRACE_STMT_RE` (`<reg> = _eNNNN_M; <obj>.name / .component = <reg>;`, no statement in between) feeds it into the existing `jsxScreenPending` accumulation, gated on `scanJsxScreenProps` (§3.1's own navigator-connection evidence — no widening of the false-positive gate). Two safer approaches were tried and reverted first: (1) widening `scanRouteConfigFactory`'s existing warm-up double-scan to `scanJsxScreenProps` reran the WHOLE scan's ordinary `<reg>` tracking too, not just env slots, and regressed react-navigation-example-0.85.3's pinned navigator count (3→2 WITH deps, 6→5 WITHOUT — a stale origin leaked from `DrawerNavigator.js`'s own register reuse into an unrelated `Chat` route, mis-targeting `MaterialTopTabNavigator.js`); (2) forwarding the env-slot origin into `moduleOriginByReg` for the ordinary register it briefly flows through (no adjacency requirement) fixed that regression but introduced a NEW one — a register reused later for an untracked object literal (e.g. `r7 = {["if"]: null, screens: null};`, matching none of this scan's tracked shapes) kept the stale forwarded origin, producing 2 false screens from shared library barrel modules (`react-native` itself named `SupportedScreen`, a UI-library barrel named `ArticleScreen`, WITHOUT-deps run only). The shipped fix requires the env-slot read and the `.name=`/`.component=` use to be the *immediately next* statement, removing the staleness window by construction — no persistent `moduleOriginByReg` entry for an ordinary register is ever created via an env slot. Verified three ways: (a) appgen triple `d4e1aacf818f482d` (`tools/appgen/compare.mjs`): screens 0/4 → **4/4**, navigator still detected; (b) `tests/sweep/deps/corpus.test.ts` + `tests/sweep/e2e/corpus-regression.test.ts` green, no new false-positive screens; (c) `tests/gate/split/segregate.test.ts`'s react-navigation-example-0.85.3 pins (navigators 3/50 WITH deps, 6/52 WITHOUT deps) hold exactly, unchanged, both directions — the false positives from both reverted approaches are gone and no genuine screen count changed. New regression fixture (rung-owned, hand-derived from the appgen triple's module 440 shape, no npm deps): "resolves a screen loaded through an inlined lazy-require IIFE" (positive case) + "an env-slot read does NOT leak into an unrelated later route assignment through register reuse" (negative case, modelled on the reverted approach's own false positive). Full gate: 1926/1926 pass (was 1924/1924 before the 2 new tests), 0 fail. | M6 segregation lane (unassigned) | fixed | real-app |
| 2026-09-04 | docs/PUSHBACK.md P-14 (2026-09-02/03 seed 777007 -> `tests/fixtures/adversarial/43-fuzz-async-guard-shared-range`, root-caused as a toolchain build mismatch, blocked on inverting `tests/gate/harness/ladder-d14-override.test.ts`'s adversarial/43 assertions) | `src/harness/ladder.ts` (D14 Hermes-VM reference run) + `src/harness/reference-policy.ts` (`VM_LIMITATIONS`) + `tests/gate/harness/ladder-d14-override.test.ts` | v99's D14 VM oracle compiled a fixture with `tools/hermesc/v99/hermesc` but ran it under `tools/hermes-vm/v99/bin/hermes`, a different Hermes commit that disagrees on a builtin-closure index (`GetBuiltinClosure b58`, the async driver's `_makeAsyncIterator`), crashing the VM on valid bytecode -- a toolchain artifact, not a decompiler bug, per P-14's diagnosis. | **FIXED at the framework level, not by curating fixture 43 into `VM_LIMITATIONS`.** `runOracleLadder` now takes an opt-in `matchedCompilerReference` flag: when set, and the reference VM is source-built with its own sibling `hermesc` next to it (`tools/hermes-vm/v99/bin/hermesc`, `.../v94/bin/hermesc`), the D14 reference run recompiles the fixture's own `source.js` with that matched `hermesc` instead of using `opts.hbcBytes` -- the candidate stays derived from the real `tools/hermesc`-compiled bytecode (never masked), only the "what should this program truly do" oracle side gets the matched compiler. Opt-in rather than automatic: `opts.sourceJsPath` is only guaranteed to be the exact program that produced `hbcBytes` at real call sites (`tiers.ts`'s gate/sweep runner, every `tools/fuzz/*.mjs` script -- all four opted in), not at every test harness call site (`tests/gate/harness/ladder-uncaught.test.ts` deliberately passes a *different* program as `sourceJsPath` on purpose, and correctly keeps the flag off/default). Verified directly against every v99 fixture `VM_LIMITATIONS` used to route around this mechanism: `27-async-await-basic`, `28-async-await-error`, `29-promise-chaining`, `31-microtask-ordering`, `54-try-catch-finally-shared-range`, plus `43-fuzz-async-guard-shared-range` itself (never added to that table, per P-14's block) -- all six now PASS through the real `hermes-vm` engine with the matched compiler, so their `VM_LIMITATIONS` entries are removed (43 needed none, since it was never added). `07-for-of-iterable` stays in `VM_LIMITATIONS`: re-verified with the matched compiler too, it still throws on this fixture's custom `Symbol.iterator` range -- a genuinely different, still-open VM incompleteness unrelated to `_makeAsyncIterator` (07 touches no `async`/`await` at all), so it is correctly NOT reclassified. `ladder-d14-override.test.ts`'s soundness test, which used fixture 43 as its "genuine candidate-vs-VM disagreement" specimen (a false premise once 43 was root-caused to a toolchain artifact), is reframed to drive the override's decision logic directly with synthetic `(candidate, vm, node)` trace triples -- a fake executable standing in for "the Hermes VM," no real hermesc/Hermes-VM binary needed at all -- asserting `vm != candidate` always stays DIVERGENT and `vm == candidate != node` is PASS (the override's own legit case). Full `npm test` green; full gate tier (identity decompiler, 528 checks) still 0 DIVERGENT; `tests/gate/decompile/equivalence.test.ts` (real decompiler, v94+v99, 216 checks) still 0 DIVERGENT. `docs/PUSHBACK.md` P-14 closed. | fuzz-lane follow-up (unassigned) / P-14 toolchain framework fix task (2026-09-04, Claude Sonnet 5) | fixed | harness |
| 2026-09-01 | overseer end-to-end on Service NSW (au.gov.nsw.service, v96, 12.7 MB, 43,384 fns, 4,510 modules; local corpus) | `src/emit/scope-check.ts` + `src/emit/index.ts` | Whole-file decompile (`--lenient-env`) FAILS with zero output: `E_UNBOUND_IDENT: emitted identifier "r15" is not declared in any enclosing scope (module > _fn0 > _fn14374 > _fn14375 > _fn14376 > _fn14377)`. The module-level scope check runs after per-function isolation, so one bad nested function aborts the whole 12 MB output. Same family as the generator `.obf` E_UNBOUND_IDENT row. `--split` of the same bundle succeeds (4,510 files, 13 s) because it runs no passes. | **HALF FIXED (2026-09-04, commit 2810099)**: the isolation half is done — `src/emit/scope-check.ts`'s new `collectUnbound` reports every unbound identifier with the function-statement path it sits under, and `emitModule` stubs exactly those functions (`W_UNBOUND_ISOLATED`, counted in `stubbedFunctions`) before re-running the throwing `checkBindings`, so one bad nested function can no longer cost the module its output. Measured after the fix on this bundle: whole-file `--lenient-env --passes=none` completes in 12 s and writes 78 MB with ZERO functions isolated for `E_UNBOUND_IDENT`, so the `r15` site does not reproduce with passes off; it was a passes-on failure, still unreproduced end to end (the passes-on run on this 12.7 MB bundle takes >10 min and was not waited out). Still open for: confirm `r15` under the full pass pipeline, and fix whatever leaves it unbound. **NOW FULLY FIXED (2026-09-04)**: re-run on deb (own clone, node 22, `nice -n 10`) with passes ON — the run 33a9e5f's agent killed at 20 minutes — completes in 24 min 0 s (1,435 s) and writes 65 MB with ZERO `W_UNBOUND_ISOLATED`, zero unbound identifiers and zero stubbed functions; `--passes=none` completes in 58 s and writes 78 MB, same zeroes. `r15` no longer reproduces at either setting, before or after the orphan-placement change (this bundle has no orphan that needs hosting) — the fix that mattered was 2810099's scope-check-granularity isolation. Only finding left is cost, not correctness: 24 minutes of passes for one 12.7 MB bundle. Standing check (was the verdict while open): prove fixed: whole-file decompile of the Service NSW bundle with `--lenient-env` and every pass on reports zero `W_UNBOUND_ISOLATED` diagnostics; QUEUE 7 (Service NSW whole-file abort) | (unassigned) | fixed | real-app |
| 2026-09-05 | campaign-2 rediff, family `iterable-wording`, 21 finds / 4 signatures, v96 only, example `v96-seed2000104.js` | `src/runtime/helpers.ts`'s `__hbc_b_arraySpread` and `__hbc_iterBegin` | Root-caused, but the original claim was wrong: `__hbc_b_arraySpread` threw a bare `new TypeError("is not iterable")` with no value description; `__hbc_iterBegin` built a V8-style `"<description> is not iterable (cannot read property Symbol(Symbol.iterator))"` text that this row's earlier draft called "the wording Hermes/V8 actually use" -- that was checked against V8/Node only, never against a real Hermes VM. Measured directly against three real interpreters (`tools/hermesc/v84/hermes`, `tools/hermesc/v96/hermes`, `tools/hermes-vm/v99/bin/hermes`, all three agreeing exactly, and `tools/hermes-vm/v94/bin/hermes` too): Hermes's own text carries no value description at all -- `"Cannot convert null value to object"` / `"Cannot convert undefined value to object"` for null/undefined, `"iterator method is not callable"` for every other non-iterable value, identical whether the source was IteratorBegin (destructuring/for-of) or CallBuiltin arraySpread (spread). `src/harness/compare.ts`'s message comparison is raw (only the unrelated missing-global family is normalised), so this is a real decompiled-output-vs-VM divergence, not a harness artifact. | Fixed 2026-09-05: both helpers now throw via one shared `__hbc_notIterable(src)` reproducing the measured Hermes text exactly. Regression fixture `tests/fixtures/adversarial/47-spread-non-iterable-message` (call-argument spread + destructuring over undefined/null/number/object, in try/catch, printing `e.constructor.name + ": " + e.message`) -- placed in `adversarial/`, not `constructs/`: this construct's whole point is that the original source.js, under Node, throws different text than the real Hermes VM (that's the bug), which `constructs/`'s blanket "identity decompiler" gate test cannot tolerate without a `KNOWN_DIVERGENT_FIXTURES` registration in `src/harness/reference-policy.ts` -- tried and reverted, since its own ratchet test in `tests/gate/harness/reference-policy.test.ts` would then need updating too and both files are out of scope this task (shared tree). New dedicated gate test `tests/gate/runtime/spread-non-iterable-message.test.ts` drives the real decompiler directly and asserts PASS at every version with a VM on this machine; DIVERGENT before the fix at v94/v96/v99, PASS after -- verified directly, `git stash` on just `src/runtime/helpers.ts` reproduces the pre-fix DIVERGENT. Plus 3 unit tests in `tests/gate/runtime/helpers.test.ts` (`__hbc_notIterable`, `__hbc_iterBegin`, `__hbc_b_arraySpread`). Plain array-literal spread (`[...x]`) is deliberately not exercised by the fixture: `src/passes/spread-rest` rewrites a whole-array `__hbc_b_arraySpread` call back to native `[...x]` syntax for readability, which then runs under Node's own (different, pre-existing, out-of-scope) spread semantics instead of the helper -- a separate, already-documented Node-vs-Hermes tension in that pass, not this bug. Corpus effect: only 1 of the family's 21 finds (`v96-seed2000104.js`) was available locally (deb has the rest, not committed); reclassified directly with a standalone script mirroring `tools/fuzz/reclassify-finds.mjs`'s `reclassifyOne`, DIVERGENT -> **PASS**. The other 20 v96 finds need a v96 Hermes VM to reclassify with full confidence (this Mac's `tools/hermesc/v96/hermes` interpreter was used for the direct-wording measurement above and is enough to confirm the fix, but `chooseReference`'s automated `findHermesVm` path -- and therefore `reclassify-finds.mjs`'s own D14 cross-check -- is unverified for v96 on this machine); left for the separate deb-side v96-VM task the coordinator started. | fuzz-lane follow-up (unassigned) | fixed | passes |
| 2026-09-05 | campaign-2 rediff (`docs/reports/2026-09-05-campaign2-rediff.md`), family `TDZ/ReferenceError-value`, 158 finds / 139 signatures, v96 only, example `v96-seed2000000.js` | unconfirmed — `src/emit` TDZ/hoisting or `src/cfg` env-graph (not yet investigated to file/line depth) | Under the current (post P-16 / fix-wave-3) harness, in full-ladder mode, the decompiled candidate disagrees with the real Hermes VM's own execution of the same v96 bytecode at the *first* trace record: VM prints `undefined` where the candidate throws `caught: ReferenceError true`, or the reverse shape elsewhere in the same family. Largest single family in this task's 4,000-program v96 sample by a wide margin (158 of 617 raw divergences). Not a P-16/F3 harness artifact — reproduces after the fix. | Resolved 2026-09-05 as D14-legit (`docs/reports/2026-09-05-campaign2-v96-vm-rediff.md`): the original re-diff ran on deb where `tools/hermesc/v96/` had only `hermesc`, so `chooseReference()` fell back to a Node-semantics reference while the cells were still labelled `full-ladder` -- every member of this family was a Node-vs-Hermes engine difference, not a decompiler divergence. With a real v96 interpreter (built from `644c8be`, verified byte-for-byte) all 400 saved v96 finds reclassify to 397 PASS / 3 INCONCLUSIVE / 0 DIVERGENT, and 4,000 fresh v96 seeds give 0 divergent. Harness now records the reference engine per cell and never emits `full-ladder` without a VM (`23b4996`). | fuzz-lane follow-up (unassigned) | d14-legit | semantics |
| 2026-09-05 | campaign-2 rediff, family `unexpected-ReferenceError-missing-global`, 61 finds / 61 signatures, v96 only, example `v96-seed2000012.js` | unconfirmed — candidate throws a `ReferenceError: <name> is not defined` at a point where the real Hermes VM's own trace of the same bytecode does not throw at all | Distinct from the already-fixed F3 row (`docs/BUGS.md` fix-wave-3, which was wording-only on an error both sides already threw); here the *presence* of the throw itself differs from the VM's real execution, in full-ladder mode. | Resolved 2026-09-05 as D14-legit (`docs/reports/2026-09-05-campaign2-v96-vm-rediff.md`): the original re-diff ran on deb where `tools/hermesc/v96/` had only `hermesc`, so `chooseReference()` fell back to a Node-semantics reference while the cells were still labelled `full-ladder` -- every member of this family was a Node-vs-Hermes engine difference, not a decompiler divergence. With a real v96 interpreter (built from `644c8be`, verified byte-for-byte) all 400 saved v96 finds reclassify to 397 PASS / 3 INCONCLUSIVE / 0 DIVERGENT, and 4,000 fresh v96 seeds give 0 divergent. Harness now records the reference engine per cell and never emits `full-ladder` without a VM (`23b4996`). | fuzz-lane follow-up (unassigned) | d14-legit | semantics |
| 2026-09-05 | campaign-2 rediff, family `other-cannot-read-props`, 38 finds / 38 signatures, v96 only, example `v96-seed2000034.js` | unconfirmed | Candidate throws `TypeError: Cannot read properties of undefined (reading '<prop>')` (commonly `'map'`) where the VM's own trace does not, across several distinct call sites (`screen#N`, others) — value/argument handling divergence, not caught by the already-fixed F1 spread-rest row (that row's fix already landed and this reproduces on top of it). | Resolved 2026-09-05 as D14-legit (`docs/reports/2026-09-05-campaign2-v96-vm-rediff.md`): the original re-diff ran on deb where `tools/hermesc/v96/` had only `hermesc`, so `chooseReference()` fell back to a Node-semantics reference while the cells were still labelled `full-ladder` -- every member of this family was a Node-vs-Hermes engine difference, not a decompiler divergence. With a real v96 interpreter (built from `644c8be`, verified byte-for-byte) all 400 saved v96 finds reclassify to 397 PASS / 3 INCONCLUSIVE / 0 DIVERGENT, and 4,000 fresh v96 seeds give 0 divergent. Harness now records the reference engine per cell and never emits `full-ladder` without a VM (`23b4996`). | fuzz-lane follow-up (unassigned) | d14-legit | semantics |
| 2026-09-05 | campaign-2 rediff, family `cleanup-push-undefined`, 20 finds / 2 signatures, v96 only, example `v96-seed2000129.js` | unconfirmed | `cleanup#N()` calls throw `TypeError: Cannot read properties of undefined (reading 'push')` where the VM's own trace does not, across several call arities of the same generated function. Only 2 distinct signatures for 20 finds, so likely one root cause with high fan-out. | Resolved 2026-09-05 as D14-legit (`docs/reports/2026-09-05-campaign2-v96-vm-rediff.md`): the original re-diff ran on deb where `tools/hermesc/v96/` had only `hermesc`, so `chooseReference()` fell back to a Node-semantics reference while the cells were still labelled `full-ladder` -- every member of this family was a Node-vs-Hermes engine difference, not a decompiler divergence. With a real v96 interpreter (built from `644c8be`, verified byte-for-byte) all 400 saved v96 finds reclassify to 397 PASS / 3 INCONCLUSIVE / 0 DIVERGENT, and 4,000 fresh v96 seeds give 0 divergent. Harness now records the reference engine per cell and never emits `full-ladder` without a VM (`23b4996`). | fuzz-lane follow-up (unassigned) | d14-legit | semantics |
| 2026-09-05 | campaign-2 rediff, family `rest-args-length`, 16 finds / 10 signatures, v96 only, example `v96-seed2000061.js` | unconfirmed — rest-parameter / `arguments.length` interaction, `src/emit`'s arguments-object lowering (`docs/lowering/arguments-object.md`) | Divergence in traces that print `rest=[...] arguments.length=N` shapes; candidate and VM agree on some calls in the same trace and disagree on others in the same family, so the bug is call-shape-specific, not blanket-wrong. | Resolved 2026-09-05 as D14-legit (`docs/reports/2026-09-05-campaign2-v96-vm-rediff.md`): the original re-diff ran on deb where `tools/hermesc/v96/` had only `hermesc`, so `chooseReference()` fell back to a Node-semantics reference while the cells were still labelled `full-ladder` -- every member of this family was a Node-vs-Hermes engine difference, not a decompiler divergence. With a real v96 interpreter (built from `644c8be`, verified byte-for-byte) all 400 saved v96 finds reclassify to 397 PASS / 3 INCONCLUSIVE / 0 DIVERGENT, and 4,000 fresh v96 seeds give 0 divergent. Harness now records the reference engine per cell and never emits `full-ladder` without a VM (`23b4996`). | fuzz-lane follow-up (unassigned) | d14-legit | semantics |
| 2026-09-05 | campaign-2 rediff, family `globals-dump-mismatch`, 6 finds / 5 signatures, v96 only, example `v96-seed2000247.js` | unconfirmed | The harness's end-of-run `globals {...}` dump (module-level bindings) differs between candidate and VM on a small family of programs. | Resolved 2026-09-05 as D14-legit (`docs/reports/2026-09-05-campaign2-v96-vm-rediff.md`): the original re-diff ran on deb where `tools/hermesc/v96/` had only `hermesc`, so `chooseReference()` fell back to a Node-semantics reference while the cells were still labelled `full-ladder` -- every member of this family was a Node-vs-Hermes engine difference, not a decompiler divergence. With a real v96 interpreter (built from `644c8be`, verified byte-for-byte) all 400 saved v96 finds reclassify to 397 PASS / 3 INCONCLUSIVE / 0 DIVERGENT, and 4,000 fresh v96 seeds give 0 divergent. Harness now records the reference engine per cell and never emits `full-ladder` without a VM (`23b4996`). | fuzz-lane follow-up (unassigned) | d14-legit | harness |
| 2026-09-05 | campaign-2 rediff, family `map-set-range-v99`, 21 finds / 9 signatures, v99 only, example `v99-seed2000298.js` | unconfirmed — `Map`/`Set` construct-fuzz grammar family, v99-specific | Divergence in a `Map`/`Set`-dedup/custom-range construct's trace, v99 only (not reproduced at 84/94/96 in this sample). | Resolved 2026-09-05 as D14-legit (`docs/reports/2026-09-05-campaign2-v94-v99-vm-reclassify.md`, real v99 VM on the Mac): all 29 v99 members are error-wording-only -- both sides print the same `map:`/`set (dedup):` lines and then fail at the identical `Function.prototype.apply`-on-non-callable point; Hermes says `Can't apply() to non-callable`, Node says `CreateListFromArrayLike called on non-object`. The decompiler reproduces the VM's crash correctly; only the two engines' native TypeError text differs. No value or branch divergence. | fuzz-lane follow-up (unassigned) | d14-legit | semantics |
| 2026-09-02 | M5 rung 18 (`optional-chain`) implementation, confirming the rung at v99 | `src/passes/optional-chain/match.ts` (`matchBaseGuard`) | v1's C-rule matcher required every run to open with a base guard (`rRes = undefined; if (B0 == N) break L;`, spec 18 par.4). At v94 this always held; at v99 the compiler occasionally elides a chain's own base guard once it has separately proven that register non-nullish (an object-literal base, or an earlier sibling chain over the same register), observed on `48-optional-chaining-nullish`'s own `user?.profile?.name` and `user?.profile?.contacts?.email`, both of which start directly with `r9 = r6.profile`, no base guard at all (`docs/lowering/optional-chaining.md` par.7). | **FIXED 2026-09-04**: `src/passes/optional-chain/match.ts`'s `parseChainAt` no longer requires a run to open with a base guard -- `ChainLink` gained a `guarded` field, and a single `matchChainGuard` helper (replacing the old `matchBaseGuard`/`matchLinkGuard` pair) both discovers `rRes`/`label` from whichever statement is the run's first real guard and confirms every later guard against the already-known register/`rRes`/`label`; when no guard precedes a link's read, that link is read directly (its base discovered from the read itself) and marked `guarded: false`. `rewrite.ts` prints a `guarded: false` link as plain `member`/`call` (never `optmember`/`optcall`) and `check.ts` compares the written node's kind against the recomputed `guarded` flag per link, so a mutation that upgrades an unguarded link to `?.` (or downgrades a guarded one to plain `.`) is rejected exactly as before. Soundness: an unguarded link is byte-identical to the original statement (same read, same throw-on-nullish behaviour), so no new proof obligation is needed for it -- the spec's own note ("the first link may be unguarded in source, `a.b?.c`") already anticipated this. Verified directly against `48`'s own real v99 shape (`user?.profile?.contacts?.email` reconstructed byte-for-byte from `tools/hermesc/v99/hermesc -dump-bytecode` output) in a new unit test asserting the base link is plain `.profile` and the rest are `?.contacts?.email`; all 5 existing positive C-rule cases plus the negative/mutation cases are unaffected. **Separate, newly-identified blocker (does not regress this fix, was already present before it)**: on the actual compiled `48-optional-chaining-nullish` v99 binary, no chain recovers yet at all, elided-base or not -- confirmed 0 chains recovered at v99 both before and after this change. Root cause: this binary's register allocator reuses the null-sentinel register (`r2`) for unrelated values later in the same function, which trips `isNullSentinel`'s whole-function "only write in the function is literal null" precondition for every guard testing against `r2`. Tracked as a new Open row above (`isNullSentinel`, position-blind precondition), not a regression of this fix. v94 is unaffected throughout (13 chain guards + 5 nullish guards, 0 residual, `tests/gate/passes/optional-chain.test.ts`). | M5 pass-ladder lane (unassigned) | fixed | passes |
| 2026-09-04 | M5 rung 18 (`optional-chain`) BUGS-row follow-up, confirming the base-guard-elision fix against the real `48-optional-chaining-nullish` v99 binary | `src/passes/optional-chain/match.ts` (`isNullSentinel`/`nullWriteCount`) | Precondition 1 (`not-null-guard`, spec 18 par.4) requires a guard's null-sentinel register have exactly one write in the *whole function*, literal `null`. `48-optional-chaining-nullish`'s real v99 binary reuses that same register (`r2`) for two unrelated later values in the same function: a later chain's own v99 spilled-compare destination (`r2 = r6 == r2`, i.e. `rC` happens to be `r2` itself) and an unrelated `0 ?? 'fallback'`-shaped literal near the end of the function (`r2 = false`). Both are `otherWrites` by `nullWriteCount`'s whole-function scan, so `isNullSentinel(r2)` returns false everywhere in the function and every guard testing `== r2` refuses, including ones with their own base guard fully intact -- so 0 of `48`'s chains recover at v99 currently, unrelated to base-guard elision (that generalisation is fixed and unit-tested in isolation; this row is what blocks it from reaching the real fixture). Confirmed by direct probe: extracting this fixture's own real v99 shape and re-running the matcher with `fnBody` scoped to just that shape (no downstream register reuse in scope) matches and rewrites correctly, reproducing `docs/lowering/optional-chaining.md` par.7's evidence. | **FIXED 2026-09-05**: `isNullSentinel` (whole-function "only write is literal null") replaced by `isNullSentinelAt`, a reaching-definitions check over the statement-list AST the pass already has -- the *reaching* write to the sentinel register at each specific guard's read must be literal `null`, walking `list[0..idx-1]` plus every enclosing list's statements before the one containing `list` (outward to `fnBody`), in flow order (same `prefixWrites`/`prefixWritesIn` pattern `global-access/match.ts`'s par.4 condition 6 established for a structurally identical whole-function-proof bug, reimplemented locally per D12a's import boundary), plus a loop-reentry guard mirroring `global-access`'s par.4 condition 5 for soundness completeness (not exercised by any fixture). No reaching write found at all falls back to the old whole-function rule -- ambiguous, refuse exactly as before, never a new acceptance. `check.ts` needed no changes: it already re-derives every precondition, including this one, by re-running the real matcher on `before`. Neither of `48`'s two clobbers can ever be the reaching write for an *earlier* guard's read, so every chain whose own guard shape is otherwise intact now recovers. Measured on the real v99 binary: 0 `?.`/`??` occurrences before this fix -> 9 `?.` + 3 `??` occurrences after (verified RED at the parent commit, GREEN after), matching v94's shape for every site except one -- `user?.profile?.name` stays unrewritten, for a third, unrelated, separately-tracked reason (a dead `r1 = undefined` store interleaved between that one guard's own compare and its `if`, not a null-sentinel or base-guard-elision issue -- see the new Open row below). Tests: 2 new unit tests in `tests/gate/passes/optional-chain.test.ts` (a positive case where the sentinel register is reused *later* in the function and the chain still rewrites; a negative case where a clobber sits *between* the null write and the guard and the pass refuses) plus the v99 fixture-level test strengthened from a length-only placeholder to asserting actual `?.`/`??` recovery counts and a residual-guard ceiling. `docs/test-count-baseline.json` bumped +2 (own additions). v94 unaffected throughout (13 chain guards + 5 nullish guards, 0 residual). | M5 pass-ladder lane (unassigned) | fixed | passes |
| 2026-08-30 | T14 adversarial pass-ladder testing | `src/structure/structure.ts` (`ramsey`'s `maxDepth` recursion guard, default 1500, `src/structure/index.ts`) | On a cold process a flat chain of basic blocks with no actual nesting overflows Node's real V8 call stack with an uncaught `RangeError` well before the documented 1500-guard fires (measured ~1030 OK / ~1075 crash on one platform, ~1000 on another — the threshold is platform-dependent). The portable claim is the failure *mode*: below the 1500 guard a flat chain dies with a raw `RangeError` instead of a clean `E_TOO_COMPLEX` refusal. | **FIXED** (2026-09-04). Root cause: `ramsey` spent one `doTree` recursion (and one unit of `depth`) per *block*, not per level of nesting, so a straight line of blocks recursed as deep as it was long — 4-5 native frames each — and V8 ran out of stack hundreds of blocks before `maxDepth` (1500) could fire. Fix, both halves in `src/structure/structure.ts`: (a) the flat-chain case is now iterative — `doTree` accumulates a `jump`/`fallthrough` chain in a loop inside ONE frame (`doBranch` split into `resolveBranch`, which returns the next block id instead of building its subtree), so `maxDepth` now measures nesting, as documented; genuine nesting (branch arms, switch arms, try bodies, merge kids, loop bodies) still recurses and is still guarded. Output is byte-identical because `seq` flattens nested `seq` nodes, so the accumulated list builds exactly the tree the nested form built, and the leaf side effects keep their order. (b) a stack-overflow conversion at `ramsey`'s entry point ONLY: a `RangeError` whose message contains "Maximum call stack size exceeded" is rethrown as `Hbc2jsError` `E_TOO_COMPLEX` with functionIndex/section — sound there because the stack is fully unwound and no half-built tree survives. (b) is load-bearing, not belt-and-braces: on this host 3000 levels of *genuine* nesting also died with a raw `RangeError` before reaching the 1500 guard. The portable regression test the row asked for is now possible and shipped: `tests/gate/structure/recursion-guard.test.ts`, 4 tests, none of which measures the host stack — a 5000-block flat chain structures (and structures under `maxDepth` 4, proving depth is spent on nesting only), and a genuinely nested ladder refuses with `E_TOO_COMPLEX` whichever guard fires first on the host. RED before the fix in a detached worktree at the parent commit: 3 of the 4 failed, two with `RangeError: Maximum call stack size exceeded`. Design note in docs/DECISIONS.md D24. | structurer lane | fixed | passes |
| 2026-09-05 | spec 21 (for-in/for-of) follow-up, agent/forin registering the rungs and re-verifying against the real Hermes VM | src/passes/for-of/match.ts (P4.2/P5/P6, the abrupt- and normal-close detection) | `06-for-of-array`'s first loop (the one with a source `break`) does not convert: the real bytecode's exception shape is not the `Catch rX; IteratorClose state,1; Throw rX` handler docs/lowering/for-of.md §2/§7 measured for this exact example, but a nested `try` around the break path (its own `IteratorClose`-then-break needs its own exception safety) whose catch is `break <mergeLabel>`, with the shared `Catch;IteratorClose;Throw` sitting at the merge point as a *sibling* statement, not as either try's own `handler` field -- the ir.ts `AugmentedCfg` doc's "a handler shared by several regions... becomes a merge point" note, which the spec's for-of section did not carry over. `07-for-of-iterable` shows a related but distinct gap: only 1-2 of its 3 loops convert depending on version (v84/v98/v99 get 2, v94/v96 get 1), not yet root-caused. Both fall back to the safe, unconverted `while (true)` form -- confirmed non-divergent by the equivalence oracle at every version for both fixtures -- so this is a readability/coverage gap, not a correctness bug. | Fixed 2026-09-05 (agent/forin, commit 02f987f). The diagnosis in this row was right about the merge point and wrong about which versions: only v84/v94/v96 use it. Three distinct per-version shapes were closed at once, each measured by running the stage-A pipeline (loop-cond, for-header, for-of) over the real fixtures and dumping the tree, not from the spec's transcription. (1) Merge-point cleanup, v84/v94/v96: `src/passes/for-of/match.ts`'s `locateTry` now descends one `labeled` level, `resolveHandlerBlock` reads the cleanup out of the wrapper's following sibling when a `try`'s own handler is `break <mergeLabel>`, the new optional `IterForm.mergeLabel` carries it to the emitter, and a nested `try` inside the body must resolve to that same cleanup block or the whole site is refused as a user try. `src/emit/function.ts` drops the wrapper, both `break`-handlers and the sibling `throw` block. (2) v96/v98/v99 schedule the body's own constant loads after the setup block's `IteratorBegin` (`IteratorBegin r4,r6 ; LoadConstUInt8 r5,30`), so P4's "last instruction" test failed; it now takes the last matching `IteratorBegin` and requires only that nothing after it names `state` or `src`, and the emitter drops that single instruction through a new per-block skip set rather than a trailing trim. (3) v99 spells a normal close `Mov r0, state ; IteratorClose r0, 0 ; Jmp`, so P6 resolves the close operand through the block's own `Mov`s (shared with `check.ts` as `closeStateOf`), allows only Mov/Jmp filler in a block it is about to delete, and requires every scratch register those Movs write to be dead after it. The `07-for-of-iterable` under-recovery was a fourth, unrelated cause: `registerLiveAfter` in `src/passes/tree.ts` answered "live" for any revisited (block, index) pair, so every register was spuriously live whenever any loop was reachable downstream -- which is every loop's own exit block. The query is existential, so a revisit contributes no new read; it now answers false. Together with asking P8 of `form.binding` in `match` exactly as `check` already asked it (PUSHBACK P-20), that ended the `for-of binding is live after the loop` abandonment at v94/v96. Result: `06-for-of-array` and `07-for-of-iterable` both report 3 `for (... of ...)` heads at all five versions, no residual `__hbc_iterBegin`/`__hbc_iterNext`/`__hbc_iterClose` in 06, gate tier 555 PASS / 0 DIVERGENT, and `tests/gate/passes/for-of.test.ts`'s skip is lifted (10/10) with its assertions unchanged. | Stage-A loop-ladder lane (unassigned) | fixed | passes |
| 2026-09-05 | QUEUE #3 `require(N)` points-to pass landing measurement (spec 17 §14.4) | `src/artifact/points-to.ts` (export-side resolution) | The pass proved a receiver AND a property name at far more call sites than it could turn into edges: 152 of 1,086 such sites on rn-template and 5,183 of 11,972 on Service NSW resolved to no function. Cause 1 of 3, babel's `exports.default = void 0;` prologue (a proven closure and an unproven value under one name), is now RESOLVED where last-write-wins is provable. The two other causes stay open in their own row. | Fixed in `src/artifact/points-to.ts` (branch agent/pt-exports): a store of a value PROVEN to be `undefined` is set aside instead of poisoning the name, and forgiven only when `provenLastWrite` proves over the factory's CFG that the single closure store to that name dominates-and-post-dominates every void-0 store, no exception region covers the span, and the exports object is neither read nor escapes inside it. Regression fixture `tests/fixtures/constructs/68-babel-default-export-prologue` (module 0 resolves, module 1's conditional second write still refuses) at all five committed versions, asserted in `tests/gate/artifact/points-to.test.ts`. Measured: Service NSW 5,183 -> 4,827 unresolved of 11,972 (+356 edges, 6,789 -> 7,145), no existing edge moved. rn-template is UNCHANGED at 152 of 1,086 because its residue contains no `default` at all (62 `oneOf`, 19 `shape`, 15 `polyfillGlobal`, 11 `oneOfType`, then hooks) - the PROVE column's rn-template clause was written on an assumption this task disproved; see docs/PUSHBACK.md P-27. | Stage-2 xref lane (unassigned) | fixed | metrics |
| 2026-09-05 | mcp-status task (Sonnet 5, lean worker), spotted while reviewing `src/mcp/resources.ts` `annotatedCalls` | `src/mcp/resources.ts` (`annotatedCalls`) | `annotatedCalls` emitted `status: f.record.status` for its `finding` field. Per `src/project/findings.ts` (`toFindingRecord`) a finding record's own `status` is always "open" and never mutates -- the live status lives on `ResolvedFinding.status` (`f.status`, from `StatusStore`/the DB transition chain). A finding transitioned to e.g. "confirmed"/"refuted" was still LABELLED "open" in this join's rows, even though the `status` FILTER on the very same call (`this.project.findings(query, ...)`) already used the live status correctly -- so a row was filtered correctly but reported the wrong label. | Fixed 2026-09-05: `annotatedCalls` now emits `status: f.status` instead of `f.record.status`. Regression test added to `tests/mcp/resources.test.ts` ("annotated-calls reports the finding's LIVE status, not the frozen record's own") -- adds a fresh finding, confirms it via `setFindingStatus`, then asserts the annotated-calls row's `finding.status` is "confirmed" rather than the record's frozen "open"; RED before the fix (`'open'` !== `'confirmed'`), GREEN after. | MCP resources lane (Sonnet 5, lean worker) | fixed | metrics |
| 2026-09-05 | grep "SecretsService" docs/BUGS.md follow-up: `hbc2js secrets <verb> --artifact <dir>` against a DB-backed (`.hbcproj`-only, spec 16 §2.4) artifact | `src/cli.ts` (`runSecrets`) | The 2026-09-04 `SecretsService` fix above wired the MCP path (`McpResources.scanSecrets`) to pass its own already-open `ArtifactService` in, but the CLI's `runSecrets` still constructed `SecretsService({artifactDir})` with no `ArtifactService`, so every `hbc2js secrets` verb still took the legacy direct-disk `index/strings.json`/`index/string-uses.jsonl` read path and threw `ENOENT` against an artifact directory that only has a `project.hbcproj` and no `index/` directory at all. | Fixed 2026-09-05: `runSecrets` now opens an `ArtifactService` the same way `runProject` already does, and passes it into `SecretsService` when the open succeeds; when it fails (the narrow index-only fixtures with neither a `manifest.json` nor a `.hbcproj` that `ArtifactService` cannot open at all) `runSecrets` falls back to the pre-existing legacy `{artifactDir}` construction, unchanged. Test: `tests/secrets/cli-db-backed.test.ts` builds a DB-backed artifact with `writeSplitResult` + `buildIndexRows` + `initProjectDb` (same construction as `tests/mcp/leads-search.test.ts`), no `index/` directory at all, and runs `secrets scan --artifact <dir>` through the real CLI entry point — RED before this fix (`ENOENT ... index/strings.json`), GREEN after. Existing `tests/secrets/cli.test.ts` (manifest-backed JSONL artifact) stays green unchanged. | secrets lane / MCP follow-up (Sonnet 5, lean worker) | fixed | metrics |
| 2026-09-05 | campaign-2 rediff, family `arity/arguments-aliasing`, 20 finds / 3 signatures, v84,94,99 (v96 slice reclassified D14-legit by `docs/reports/2026-09-05-campaign2-v96-vm-rediff.md`; the other versions still unverified against a VM), example `v84-seed2000502.js` | unconfirmed — `arguments` object handling, `src/emit` or `src/cfg`, versionless (reproduces on every traced bytecode version, which usually points at the decompiler rather than a version-specific opcode quirk) | Candidate diverges from the real VM at record 0 (`"original"` vs `"changed-via-arguments"`) on a construct that reassigns a parameter via the `arguments` object at a specific declared-arity/called-with combination. Distinct from the already-fixed 2026-09-02 harness row (`docs/BUGS.md`, D14 VM-agrees override) — that row was a harness-comparator bug fixed by using the VM as ground truth; this reproduces *after* that fix, i.e. the VM itself disagrees with the candidate. | Fixed 2026-09-05 (branch agent/args-alias). The family's remaining v84 members were reduced by hand to the shape the fuzz programs share, and the divergence is real and versionless, but it is NOT sloppy-mode aliasing (D14 holds: Hermes 84-99 never aliases a parameter). `GetArgumentsPropByVal dst, idx, lazyReg` and `GetArgumentsLength dst, lazyReg` take the function's lazy-arguments register as their last operand: it holds `undefined` until a `ReifyArguments*` materialises the object into it and the materialised object afterwards, so after any write into `arguments` the lazy reads must go through the written object. `src/emit/lower.ts` ignored that operand and always emitted the host `arguments`, dropping every such write -- measured at v84 with `tools/hermesc/v84/hermes`, `function k(){ arguments[0]='K'; return arguments[0]; }` gives `K` on the real VM and gave the incoming argument in the candidate, at all of 84/94/96/98/99. Fix: new `__hbc_argsLive(lazy, raw)` selector helper, emitted only in a function that contains a `ReifyArguments*` (new `FunctionEmitter.argumentsReified`), so a function that never reifies keeps the plain `arguments` form and no existing output moves. Regression: construct fixture `tests/fixtures/constructs/69-arguments-reify-readback` (every touched index outside the declared parameter list, so Node's mapped sloppy `arguments` and Hermes's unmapped one agree and the fixture measures the reify/read-back path only) at all five versions, plus 10 tests in `tests/gate/decompile/regressions.test.ts` -- five against the fixture oracle and five against a real Hermes VM where one exists (v84 and v96 run here, v94/v99 skip) -- RED at all five versions before the fix, GREEN after, and the helper's own unit test in `tests/gate/runtime/helpers.test.ts`. The already-reclassified members are unaffected: the v96 slice stays D14-legit and the v94/v99 exemplars stay INCONCLUSIVE (their non-terminating `for (let i = -Infinity; i < arguments.length; i++)` loop runs before any reify), which is exactly why the family's real bug survived three re-diffs -- the fuzz programs put the read-back after a loop that never ends. | fuzz-lane follow-up (unassigned) | fixed | semantics |
| 2026-09-05 | UI QA pass on the live rig (BUG 2), Fred: "the app is not properly functional" | src/ui-server/routes.ts (`/api/search/source`) + src/mcp/leads.ts (`searchSource`) | `GET /api/search/source` took 83.5 s on a real 12 MB app and froze the WHOLE server while it ran: seven `/api/jobs` probes issued over 70 s all timed out and then completed in 0 ms the instant the search returned (head-of-line blocking on the single event loop). Typing in the string-search box hung the entire UI for over a minute per query. Two causes: the scan called `artifact.fn(fn)` per function (O(native rows) plus an overlay query each) and `artifact.source(fn)` per function (a full `readFileSync` + split of the owning MODULE per function, so one module's text was re-read once per function it owns), and it ran inline on the event loop. | Fixed 2026-09-05 (commit 5908aee). Algorithmic half: new `ArtifactService.listRanges`/`overlayNameOf`/`hasActiveNames` give the scan cheap rows, module text is read once per file per scan (bounded 64-file cache), and an overlay lookup happens only for a MATCHED row — 5,349 ms to 132 ms on the rn-template fixture (4,199 fns) for `q=function`, same 10,926 matches, and the fixture's whole-scan floor drops from 3.0 s to 148 ms. Scheduling half: `src/incremental.ts` drains a scan written as a generator, yielding the event loop every 8 ms; the route uses `searchSourceAsync`, and the synchronous MCP/CLI entry point drains the SAME generator, so semantics cannot drift. `?limit=` is now pushed down into the scan (stops at page+1 rows, 1 ms for `limit=5`) and the page says `partial: true` when it did. Acceptance: `tests/ui-server/search-perf.test.ts` — `/api/functions` behind an in-flight search took 712 ms before the fix (RED) and is under 200 ms after; plus the event-loop-yield, limit-pushdown and async-equals-sync tests. | ui-server lane (Claude Opus 5, agent/search-perf) | fixed | metrics |
| 2026-09-05 | UI QA pass on the live rig (BUG 3) | src/ui-server/routes.ts (`/api/template-injections`) | `GET /api/template-injections?limit=2` took 23.2 s for a 472-byte body on a real 12 MB app, blocking every other route for that window exactly as the `search/source` row above did. `limit` was applied only AFTER the whole-bundle scan. | Fixed 2026-09-05 (commit 5908aee) for the blocking half, which is the part that hurt: `scanTemplateInjectionsSteps` expresses the scan as steps and `ArtifactService.templateInjectionsAsync`/`McpResources.templateInjectionsAsync` drain it with `src/incremental.ts`, so the route yields the loop every 8 ms and concurrent calls share one in-flight scan; the scan stays cached after the first call. `limit` is deliberately NOT pushed into the scan: the rows are RANKED (substitutions-inside-quotes desc), so the top `limit` rows are only known once every function has been decoded — pushing it down would change the answer, not just its cost. Test: `tests/ui-server/search-perf.test.ts` ("GET /api/template-injections answers off the critical path and honours ?limit="). | ui-server lane (Claude Opus 5, agent/search-perf) | fixed | metrics |
| 2026-09-01 | E2E tier 1 — bucket `diff:TryGetById(string)`: top bucket with passes on, 284 fns rn-template, 1309 react-navigation, 1882 Service NSW (passes off) | `src/passes/global-access` + `expr-rebuild` (dead-store elimination) | A `r0 = globalThis;` dead store survives the rewrite (`hermesc -O` cannot drop the `TryGetById "globalThis"` since it may throw, so the function never round-trips although it is behaviourally identical). expr-rebuild's dead-store scan doesn't see the store as dead, or global-access leaves the residue after inlining. | Fixed 2026-09-05: a new stage-B rung, `globalthis-dead-store` (docs/LOWERING-CATALOGUE.md R11, `src/passes/globalthis-dead-store/`), registered `after: ["expr-rebuild", "global-access"]` and `before: ["fn-naming", "reg-split", "var-naming"]` (the `try-clean`/R8 ordering pattern, so a register its deletion exposes as dead never reaches a renaming rung first). Root cause was two-fold: (1) stage-B passes each run once, in registry order, with no re-visit, so `expr-rebuild`'s R1b dead-store rule sees the store as live (the guarded read is still there) and never gets another look once `global-access` folds that read away later in the pipeline; (2) `var-naming`'s deliberate "globalThis alias -> refuse" rule (spec 07 section 4.2 item 2) means nothing downstream ever deletes it either. A first attempt used a whole-function "does the register have any read left" test, which is unsound on its own: real bundle code (rn-template module_1 fn#155, `r0 = globalThis; r0 = console.warn(...); return r0;`) reuses the dead register for an unrelated later value, and a whole-function read count credits that later read to the wrong write, refusing the safe deletion. The shipped analysis (`isDeadLocally` in `src/passes/globalthis-dead-store/analysis.ts`) instead asks the same position-sensitive question `expr-rebuild`'s own R1b/D-a rule asks — is the store's value read before something redefines the register? — falling back to the whole-function count only when the local scan is inconclusive (reaches the end of a nested list without a definitive answer). Tests: `tests/gate/passes/globalthis-dead-store.test.ts` (18 cases: analyze/match/rewrite/check unit tests including the double-write shape that broke the first attempt, plus a red->green fixture test on 19-var-hoisting at all 5 versions asserting the dead print-guard's store is gone while the live DeclareGlobalVar-fed store survives — a rung-owned regex/count assertion, no whole-output comparison). E2E evidence (this row's own "prove fixed" criterion): on rn-template-0.72 with passes on, `diff:TryGetById(string)` drops from 280 to 16 functions and overall IDENTICAL rises from 44.36% (1830/4125) to 46.47% (1917/4125) — module_1 fn#155 (the row's own named example) now round-trips. The remaining 16 `diff:TryGetById(string)` functions are a different cause, untriaged, not this row's concern. No QUEUE item — owner: M5 ladder (global-access owner). | M5 ladder (global-access owner) | fixed | emit-shape |
| 2026-09-05 | spec-27 real-APK validation (same worker) | `hbc2js deps --out` native-ingest path | `deps --out` on an `.apk` reported nothing on stderr when `ingestNative` succeeded -- only its failure path wrote a line. A silent success looks identical to a build that never wired the L9 ingestion call in at all. | Fixed 2026-09-05 (commit 7908ce7). `runDepsCmd` in `src/cli.ts` now always writes one stderr line either way: "native ingestion -- N modules, M tables written, K seams" on success, unchanged "native ingestion skipped -- <why>" on failure. Test: `tests/gate/cli/deps.test.ts`'s existing spec-27 L9 single-run test now asserts the success line. | native lane / toolchain | fixed | toolchain |
| 2026-09-05 | spec-27 real-APK validation (Sonnet lean worker), /Users/fred/nsw-hunt/NATIVE-INGEST-TEST.md | `src/native/seams.ts` (`anchorFns`) + `tests/fixtures/constructs/66-native-module-seams` | JS<->native seam join linked 0/9 native modules on the real NSW bundle. `anchorFns` required `NativeModules`/`TurboModuleRegistry`/`requireNativeComponent` as JS GLOBALS (`index/globals.jsonl`) -- true ONLY in the hand-written fixture; real Metro bundles bind them as `require()`-locals so the anchor never fired. Evidence existed (`NativeModules`+`Crypto` co-occur fn:8871, `index/string-uses.jsonl`, matches manual NATIVE-SEAM.md) but the join never read it. Fixture-overfit -- passed synthetic acceptance, failed every real app. | Fixed 2026-09-05 (commits 39611a3, 3940a70). Fixture 66 rebuilt Metro-shaped (`__d`/`__r` mini-registry, host required into a captured local, both an inline member-chain shape and a module-top-capture-then-nested-closure shape); RED on the unfixed join (4/7 seams.test.ts tests failed). `anchorFns` in `src/native/seams.ts` now anchors a function on an exact `property-get`/`global-name` string-use of the channel host name in that function OR any lexical ancestor (`index/functions.jsonl` `parent`, walked to full depth), in addition to the existing global-read anchor; `candidatesByFn` excludes the anchor's own name from candidate extraction. Real-APK check on the NSW bundle (base.apk): before the fix 0/97 named native modules linked (0 linked, 0 js-only, 97 native-only, replicated by re-running the OLD anchor logic against the same artifact); after the fix, 115 linked seams across 44 distinct module names (475 js-only, 59 native-only of 649 total rows), and ALL 11 first-party modules under `au.gov.nsw.service.react.modules.*` link, including Crypto (exceeds the >=8/9 target). Tests: `tests/gate/native/seams.test.ts` (all 8, incl. a new test asserting both the inline and module-top-capture shapes independently). No bundle-derived identifiers were committed; only the fixture and aggregate counts. Also fixed independently by the deb loop at 77da169 (property-get anchor only, no ancestor walk); merged keeping this superset and this fixture; the deb goldens for 66 (eb06d04) target the other hbc build and are regenerated in the next golden batch. | native lane / spec-27 owner | fixed | real-app |
