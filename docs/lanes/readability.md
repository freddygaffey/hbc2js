# Lane pack: readability (spec 28, the LLM naming / rewrite / file-op layer)

This file is a lane worker's whole orientation. Read it first, then only the files a task names. It is OWNED BY THE WORKER: the last act of every task is to rewrite it (advance the queue, add gotchas, delete stale lines). The ledgers stay where they are: `docs/BUGS.md` (open bugs, owners), `docs/PUSHBACK.md` (design deviations), `docs/AGENT-LOG.md` (what each task did). Never duplicate them here; point at them.

## What this lane is
An LLM proposes names, function rewrites and file-tree ops for decompiled Hermes bytecode. Every proposal is `suggested` until a human (UI) or an opt-in evaluator promotes it; every accepted change passes the equivalence oracle or is discarded; every change is a reversible, provenance-stamped record. Spec: `docs/specs/28-llm-readability.md` (sections 0a, 1b, 1d, 7, 9.x are the contracts; 10 is the landing history). User docs: `docs/READABILITY.md`, `docs/RENAME.md` (the Design-D overlay the names live in).

## Files and what owns what
- `src/readability/types.ts`: every shared type and constant; `cacheKey`, `parseReadabilityResult` (never throws; strips one markdown fence), `READABILITY_MCP_TOOLS` / `READABILITY_CLI_VERBS` / `READABILITY_UI_ACTIONS` (machine-checked against spec section 9.7: rename a verb only together with the spec text), `resolveClaudeCliConfig` / `resolveHaikuConfig`.
- `src/readability/backends.ts`: the one backend-id map. `claude-cli` is the DEFAULT (Fred: run on the plan, never the API); `haiku` = API, opt-in; `replay` (committed recordings); `heuristic`; `fake`. Env `HBC2JS_LLM_BACKEND`, CLI `--backend`.
- `src/workers/backends/claude-cli.ts`: spawns `claude -p` with `--output-format json --tools "" --no-session-persistence --system-prompt <skill body>`; prompt on stdin (limit 10 MB); usage mapped to tokens; `is_error`/non-zero/timeout = `TransientBackendError`; missing binary = `ReadabilityConfigError`. The only file allowed to open a socket or spawn a model; nothing under `tests/` imports it except its stub-driven test.
- `src/readability/skills.ts` + `skills/*.md`: skill files with front matter `id`, `kind`, `version`; body sections Inputs / Rules / Output contract / Abstain. Bumping `version` re-keys the cache and REQUIRES re-keying `tests/fixtures/llm-readability/*.recording.json` with `tools/readability/rekey-recording.ts` in the same commit.
- `src/readability/cache.ts`: content-hash cache, one JSON per key under `<projectDir>/cache/llm-readability` (gitignored, derived).
- `src/readability/name-pass.ts`: `runNamePass`, the per-target loop for NAMES: targets are per-function (`{fn, regs, context}`, one model call per function, default) or per-register (`--per-register`); reply `names[]` matched by bindingId; write to the overlay as `suggested`; equiv backstop = apply-then-revert render byte-identity (whole-module `NameService.render()`, exactly twice per run, O(1)).
- `src/readability/scope.ts`: `computeSrcScope` = the segregation classifier's `src`-bucket functions (`--only src`).
- `src/readability/rewrite.ts`: `gateRewrite` / `runRewritePass`: splice a candidate function into the faithful render, run `runFunctionEquiv` (`src/harness/hbc-equiv.ts`: module-hbc leg under the Hermes VM plus a spec-09 fuzz leg when trace coverage is thin); PASS only; DIVERGENT and INCONCLUSIVE both reject; the faithful original always survives.
- `src/readability/transactions.ts` + `file-ops.ts` + `src/projdb/readability-shards.ts`: the spec-18 extension: `recordTransaction` (DB-first, then shard `analysis/readability/<id>.json`, then hash-chained log), `revertTransaction` (restores every `prior` path to its recorded sha256; revert is itself a transaction), `traceFile` (file -> origins -> `{fn,reg}`; zero-origin outputs are refused at write time). `runFileOp` for make / rename / move / combine / split with the tree-level equiv gate. Op enum includes `rewrite`.
- `src/readability/surfaces.ts`: the seven tools as plain functions over `ReadabilityContext` (`suggestNames`, `rewriteFunction`, `classifyModule`, `fileOp`, `promoteChange`, `revertChange`, `listSuggestions`). Names live in the overlay sidecar (`overlayPathFor`), addressed by `suggestionId`; rewrites/file ops in `readability_tx`, addressed by `txId`; the review tools see both. `promoteChange` refuses a `worker:` who. `loadAnalysis` is async, pool-backed, cached per (ctx, path, size, mtime). `evaluate?` is honoured via `maybeEvaluate`.
- `src/readability/evaluate.ts`: evaluator host: `evaluationModeFor` (`ui` surface ALWAYS `human-ui`; mcp/cli default `none`), plugins `NONE_PLUGIN`, `createInlineCallerPlugin`, `createAgentEvaluatorPlugin`; adversarial re-check (`skills/hbc-adversarial.md`, job kind `adversarial-recheck`) demotes `misleading` names to `low` + flagged. A report never promotes.
- `src/mcp/tools.ts`: `registerReadabilityTools` + JSON-schema validation. Spec-17's `promote` / `revert` are different tools; ours are `promote_change` / `revert_change`.
- `src/ui-server/readability-routes.ts` + `server.ts` (`buildReadabilityCtx`, `--llm-backend`): HTTP surface; the three write actions enqueue jobs (202 + jobId) and the pane polls. `src/workers/runner.ts` + `readability-worker.ts`: `readability-suggest-names` jobs run in a worker thread that opens NO project-DB connection (spec 18 single writer, DECISIONS D34); `readability-rewrite-function` / `combine-files` still run in-process (BUGS open row; needs a compute/commit split in surfaces.ts).
- `ui/src/panes/WorkersPane.tsx` (Readability section), `ui/src/workers/readability-{wire,hooks}.ts`, `ui/e2e/readability.spec.ts`. UI art direction is Fred's: extend, never restyle.
- `src/parallel/analysis-pool.ts`: `analyseModuleParallel` (stage-A pool; byte-identical by construction; default OFF below 20,000 functions, `HBC2JS_ANALYSIS_POOL=1|0`). Measured: it does not speed up setup (NSW 34 s either way). Do not reach for it to fix a slow path.
- `tools/readability/record.ts` (`--backend`, `--only src`, `--sample N --seed S`, `--resume`, `--per-register`) and `rekey-recording.ts`.
- `src/cli.ts`: verbs `name llm-fill`, `readability rewrite|file-op|review`, `hbcproj ...`, `ui-server --llm-backend`.

## Test commands (run the ones your task touches; the orchestrator runs the full gate)
`npm run typecheck`; `node --test "tests/gate/llm-readability/*.test.ts"` (3 skips are expected: held-out coverage/quality need a recording; NSW leg needs `HBC2JS_NSW_HBC`); `node --test "tests/workers/**/*.test.ts" "tests/ui-server/**/*.test.ts" "tests/projdb/**/*.test.ts"`; `node --test "tests/mcp/**/*.test.ts"`; `node --test "tests/gate/cli/*.test.ts" "tests/gate/name-overlay/*.test.ts" "tests/gate/parallel/*.test.ts"`; `node --test "tests/gate/docs/*.test.ts"` (24: BUGS/PUSHBACK/baseline/spec-text checks); `(cd ui && npx tsc --noEmit && npx vitest run)`; `(cd ui && npm run e2e)` only when 1-min load is under 12 (the rig pins `HBC2JS_LLM_BACKEND=heuristic`). Gate test count baseline: `docs/test-count-baseline.json`, measured with the regex `(?<![.\w])test\(` over `tests/gate/**/*.test.ts`; bump it in the commit that adds gate tests.

## Gotchas learned the hard way (each cost a relaunch once)
- `NameService.render({fn})` and the old `service.render` path emit the WHOLE module. Never call them per function or per target; use `decompileFunction` (scoped) for prompt sources. A run that did this on NSW took 38 minutes and 16 GB before its first model call.
- `decompileFunction` still re-parses and re-analyses the whole bundle per call (~1.2 s/function on the held-out app). Open BUGS row; fix = pass one parsed module/analysis through. Until then, always bound real runs with `--only src --limit N` or `--sample N`.
- Real model output arrives wrapped in ```json fences, sometimes with prose after it; `stripCodeFence` handles that. Do not assume clean JSON.
- `claude -p` refuses stdin over 10 MB; fn 0 of a bundle renders to the whole program. Skip sources over 48 KB.
- A readability job on the ui-server main thread stalls every request (ECONNRESET on `/api/modules`); that is why suggest-names runs in `readability-worker.ts`. Do not add synchronous bundle-sized work to `runReadabilityJob`.
- Playwright's role matcher is a substring match: two buttons named "Suggest name(s)" collide; use `exact: true`.
- Tailwind arbitrary px sizes fail the spec-20 token lint; use the type ramp (`text-xs` etc.).
- BUGS cells must not contain a literal `|`; Open rows need a known cluster and a `prove fixed:` verdict naming an owner or `QUEUE N`.
- Skill version bump without re-keying the synthetic recording breaks `tests/workers/backends.test.ts`.
- Spec 18 has one writer: a worker thread must not open the project DB.
- `context()`/`source()` (and so `get_context`/`get_source`, checkpoint 1a) throw "no range recorded" for every fn on a `tests/fixtures/constructs/**` fixture -- those are single-script, no `__d()` modules, so `splitProject`/`init` write zero `ix_ranges` rows (same note `tests/mcp/resources.test.ts` already carries). `suggest_names`/`promote_change` never call those two resources, so a construct fixture is still fine for exercising THEM (the mcp-server round-trip test and the agent-driver stub test both do exactly this on purpose) -- use a real bundle (`rn-template-0.72`, fn 188/190 are known-good) whenever a test needs `get_context`/`get_source` to actually return something.
- `suggest_names`' `loadAnalysis` re-parses+re-analyses the WHOLE `.hbc` file behind `ctx.hbcPath` from scratch on every call (the `decompileFunction`/`loadAnalysis` cost row above) -- one call against `rn-template-0.72` (4,199 functions) ran past a 90 s timeout with no result. `hbc2js mcp-server`'s `--hbc` and `hbc2js readability agent`'s `--hbc` do not have to name the same bytecode the project directory was `init`-ed from; a test that needs BOTH a real-bundle project (for `get_context`) AND a fast `suggest_names` call should point `--hbc` at a tiny construct fixture and accept that the two are semantically unrelated for that run.
- `hbc2js init` on `react-navigation-example-0.85.3` is FIXED (2026-09-13, agent/init-fix; BUGS row moved to Resolved) -- see queue item 2 for the root cause (`_fn<idx>__c<copy>` closure-copy names parsing to `NaN`). Both `.hbc`/`.debug.hbc` init cleanly now; use this bundle freely for `init`/`.hbcproj` work.
- `hbc2js readability agent` with no `--hbc` gives a real answer but an unprovable one: the end-of-run equiv leg reports `INCONCLUSIVE, "no bundle supplied: nothing to prove tree equivalence against"` rather than skipping or erroring. As of queue item 3, `--hbc` is REQUIRED (fails fast, before spawning anything) precisely so this can't happen silently anymore.
- `openProjectDbReadonly(x)` takes the PROJECT DIRECTORY, not the `project.hbcproj` file path (`dbPath(artifactDir)` is a separate helper for the file path itself) -- passing the file path throws `SQLITE_CANTOPEN` ("unable to open database file"), silently swallowed by a `try { } catch { db = undefined }` guard that treats "can't open" as "no db to check against" rather than a caller bug. Cost a stub-transcript test hang (the buggy path let a MISMATCHED `--hbc` sail through validation and spawn a real MCP-server child that then had nothing useful to do) before the fix.
- `project.hbcproj`'s `meta` table (and `MODULES.json`) record the bundle's `bundle_sha256`, never its filesystem path -- do not assume a project can tell you where its own source `.hbc` lives on disk. See PUSHBACK P-66.
- `claude -p --output-format json`'s final result JSON (confirmed by reading the actual `claude` binary's embedded zod schema strings, not just observed output) has `is_error`, `num_turns`, `stop_reason`, `terminal_reason`, `permission_denials` (an array -- denied tool GRANTS, never a count of successful calls), `usage`, `modelUsage`, `total_cost_usd`; there is no field anywhere in it that counts successful tool calls. `--output-format stream-json --verbose` would give a per-event transcript that could be counted, but that is input/output-format surgery, not a one-line addition -- the skill's own `DONE` JSON output contract (`investigated`/`wrote`/`refusals` arrays) is the load-tolerant substitute `agent-driver.ts` uses instead (`countToolCalls`). When a run errors out before producing a `DONE` reply at all (e.g. `max_turns` hit mid-turn), `resultText` falls back to the raw un-parsed stdout JSON blob, which correctly parses to `toolCalls:0`.
- `--tools ""` genuinely removes every built-in tool (Bash, Read, Edit, ...) while leaving MCP tools alone (gated separately by `--allowedTools`) -- confirmed via `claude -p --help`'s own text ("Use \"\" to disable all tools ... from the built-in set") and by a real smoke where `permission_denials` dropped to empty. It does not, by itself, make the model call an MCP tool faster -- see the new BUGS row below.
- On a real held-out module (queue item 2's smoke, module 5 / fn#7, ~13 KB with a nested closure), 8 turns were not enough for `claude -p` (haiku) to make even one successful MCP tool call: it spent its turns proposing `Bash` commands that shelled out to a hand-rolled query script (`mcp_query`, or a heredoc'd python/bash wrapper) instead of using the the MCP tools/call method mechanism the `--mcp-config` wiring actually exposes -- both attempts were correctly denied (no `Bash` tool is granted), then the run hit `max_turns` with `is_error: true` and 0 names written. Not diagnosed further (one real call is this task's whole budget): worth checking with a bigger `--max-turns` or a skill-prompt tweak that states more forcefully "call the readability MCP tools directly, do not shell out" before spending more real-model budget on this lane.
- Both `suggest_names` and `heuristic`'s naming reply are PLAIN TEXT by default (`FakeBackend`'s `defaultReply`, `HeuristicBackend`'s `deriveName`), not the `{names:[...]}` JSON `parseReadabilityResult` requires -- a `suggest_names` call against either backend with no custom `replies` legitimately ABSTAINS (zero suggestions written), every time. Do not write a test that asserts a nonzero write count against `--llm-backend fake` or `heuristic`; assert the invariant that survives ("anything written is `tier:suggested`"), not the count.
- `set_name` (spec-17, `ProjectService`/DB-backed, `d_names`) and `suggest_names`/`list_suggestions` (spec-28, file-based `OverlayStore` sidecar) are TWO SEPARATE naming systems that both answer to "name" -- a `set_name` write never shows up in `list_suggestions`, and vice versa. Do not expect them to share state.
- The real `claude` binary must be directly executable (shebang + `chmod +x`) to stand in for `claudeBin` in `agent-driver.ts`/`claude-cli.ts` -- `spawn(claudeBin, args)` never goes through a shell.

## The two naming systems, and provenance filtering (open design gap)
`ReadabilityContext.who` decides who a `suggest_names`/`rewrite_function`/`file_op` write is attributed to (never the tool-call arguments -- an MCP caller cannot lie about its own identity), but nothing today lets a CALLER (the checkpoint 1b driver, in particular) pass a distinguishing run id into that `who` and then filter `list_suggestions` by it. `agent-driver.ts`'s end-of-run check works around this with a before/after snapshot diff instead of a `who`-filtered query (P-59's `NameRecord`/`ReadabilityTransaction` both carry a `who`, `ListSuggestionsFilter` does not expose one) -- correct for a single concurrent run against one project, wrong if two agent runs against the same project ever overlap. Filed here rather than as a PUSHBACK row because it was a load-tolerant implementation choice within checkpoint 1b's own scope, not a deviation from anything the spec or this pack settled.

## Held-out measurement (spec section 7)
App: `tests/fixtures/bundles/react-navigation-example-0.85.3` (symlinked into agent worktrees, never committed as bytes). `--only src` = 345/1,782 modules, 4,655 functions, 31,298 registers. Recording command: `node tools/readability/record.ts <hbc> tests/fixtures/llm-readability/react-navigation-example-0.85.3.recording.json --backend claude-cli --only src --resume`. Real cost per call on the plan: ~5k tokens in, ~55 s. The labelled sample: `tests/fixtures/llm-readability/react-navigation-example-0.85.3.labels.json`.

## Queue (in order; one item = about an hour; checkpoint with a commit after each; update this section last)
1. **DONE (2026-09-13, agent/agentic-hook, both checkpoints).** `hbc2js mcp-server <project> [--hbc <bundle>] [--llm-backend <id>]` (`src/mcp/server.ts`, wired in `src/cli.ts`): hand-rolled stdio JSON-RPC 2.0 (initialize/tools/list/tools/call/resources/list/resources/read) over the spec-17 read/write tools plus the seven readability tools when the project has a readable tree. `hbc2js readability agent <project> --module M | --fn N | --file <path> [--model] [--max-turns] [--budget-tokens]` (`src/readability/agent-driver.ts` + `skills/hbc-agent.md`): spawns `claude -p` against a temp `--mcp-config` pointing at the server above, then runs its own end-of-run check (before/after `list_suggestions` diff + tree equiv). Never promotes. Tests: `tests/mcp/server.test.ts` (real child-process round trip), `tests/readability/agent-driver.test.ts` (stub-driven, `tests/support/stub-claude.mjs` extended with an `--mcp-config` mode, shared with `tests/workers/claude-cli-backend.test.ts` unchanged). One real hand smoke ran (`--model haiku --max-turns 8`, real network call): 18 s wall, 123,396 tokens in / 1,220 out, 0 names written, 0 refusals, exit 0 -- correctly abstained because it ran against a construct fixture with no `ix_ranges` (see Gotchas). Could NOT run against the pack's own held-out app: `hbc2js init` on `react-navigation-example-0.85.3` fails with a new bug (BUGS Open, 2026-09-13, real-app cluster, `UNIQUE constraint failed: ix_ranges.fn`) -- **next worker on this lane: either fix that `init` bug first, or accept `rn-template-0.72` as the smoke target and re-run the hand smoke there with a real writable target (a function whose evidence is strong enough for a name to actually land) to get a nonzero "names written" number**, which this run did not produce. Also open from this task: the "two naming systems" / provenance-filtering gap documented above (not a blocker, just unfinished).
2. **DONE (2026-09-13, agent/init-fix).** Root cause of `UNIQUE constraint failed: ix_ranges.fn` (BUGS row, now Resolved): NOT two modules sharing a range -- `src/split/index.ts`'s factory-mark loop parsed a printed function's `onFunctionRange` name into a bytecode fn id with `Number(mark.name.replace(/^_fn/, ""))`, which assumes the name is always exactly `_fn<idx>`. A duplicated closure copy (`src/emit/index.ts`'s `copyNameOf`, used when the same Hermes function is emitted at more than one closure-creation site) names itself `_fn<idx>__c<copy>` for `copy > 0`; stripping only the `_fn` prefix left `"<idx>__c<copy>"`, and `Number()` on that is `NaN`. A `NaN` bound as `node:sqlite`'s `INTEGER PRIMARY KEY` (`ix_ranges.fn`) does not error -- it silently AUTO-ASSIGNS the next free rowid, and the crash only surfaces later, when a real row happens to need that same id. Found on react-navigation-example-0.85.3: fn#13435 (module_1682.js) is duplicated into 3 copies; the `NaN` from `_fn13435__c1` auto-assigned rowid 10111, which then collided with the real fn#10111's row. Fix: extract the leading digit run (`/^_fn(\d+)/`) instead of a fixed-prefix strip, so a copy-suffixed name still resolves to its ORIGINAL fn id (last copy printed wins the one row `ix_ranges` has room for); a name that still fails to parse is dropped with a diagnostic instead of writing a fabricated key. `hbc2js init` now exits 0 on both `react-navigation-example.hbc` and `.debug.hbc`, and `hbc2js hbcproj export` + `hbcproj verify --full` are clean afterward on both. Tests: `tests/projdb/init-real-bundle.test.ts` (new) -- a unit test on the exact `_fn13435__c1` duplicate shape (no NaN keys in `functionRanges`, fn#13435 still gets a recorded range, no dropped-range diagnostics) plus an end-to-end regression test (`init` on both `.hbc` files, asserting `rangeRows` has no NaN/duplicate fn and `ix_ranges`'s row count matches). Full `tests/gate/split/*.test.ts` + `tests/projdb/rebuild-verify.test.ts` + `tests/gate/cli/hbcproj-hooks.test.ts` (50 tests) still green; `npx tsc --noEmit` clean. Redid item 1's hand smoke on the now-fixed held-out project: `hbc2js readability agent /tmp/rn-init-test --module 5 --max-turns 8` (module 5 = fn#7, a real ~13 KB gesture-handler-shaped body with a nested `_fn1856`; no `--hbc` passed, so the equiv leg had nothing to prove against -- see Gotchas). Real numbers: wall 35.6 s (34.4 s API), 167,658 tokens in / 1,844 tokens out (haiku), 0 names written, terminal_reason `max_turns` (hit the 8-turn cap, `is_error: true`, exit code 1), 2 tool-permission denials (both `Bash`, the model trying to shell out to a hand-rolled `mcp_query`/python script instead of calling an MCP tool directly -- it never appears to have made a single real the MCP tools/call method), equiv verdict `INCONCLUSIVE` ("no bundle supplied: nothing to prove tree equivalence against", because `--hbc` was omitted). This is the ONE real model call this task's budget allowed; not re-run. See the new Gotchas below.
3. **DONE (2026-09-13, agent/driver-fix).** (a) `src/readability/agent-driver.ts` spawns `claude -p` with `--tools ""` (no built-in tool set at all) alongside `--allowedTools "mcp__hbc2js__*"` -- confirmed by a real smoke that `permission_denials` is now empty (the item-2 Bash-shelling-out detour is gone). (b) `--hbc` is now REQUIRED (was optional): `project.hbcproj`'s `meta` table records `bundle_sha256` but never a path, and `MODULES.json` doesn't either -- there was nothing to "derive" a path FROM, so the driver's new `resolveBundleHbc` fails fast (before spawning anything) when `--hbc` is missing, and verifies a supplied one's sha256 against the recorded hash, refusing a mismatch. PUSHBACK P-66 filed for the gap between the queue item's premise and what the two artifacts actually store. (c) `skills/hbc-agent.md` opens with "You have no shell and no files... Your ONLY tools are the `mcp__hbc2js__*` tools" and one worked `get_context` call; deliberately does NOT reference a `help` tool (agent/discoverability's sibling task, not yet landed in this worktree) so that merge stays a one-line insert. (d) `AgentRunResult.toolCalls` (parsed from the skill's own `DONE` JSON contract: `investigated.length + wrote.length + refusals.length` -- the only place a call count CAN come from; `claude -p --output-format json`'s own result has no successful-tool-call field, only `permission_denials` for refused grants) and `exitCode` now escalates to `1` when `toolCalls` is `0`, independent of the child's own exit code; the CLI wrapper's exit code now actually reflects it (previously hardcoded `return 0`). Tests: `tests/readability/agent-driver.test.ts` grew 2 -> 6 (missing/mismatched `--hbc` fail fast before spawning, `--tools ""` present in argv, a zero-tool-call transcript yields `toolCalls:0` + non-zero `exitCode`); `tests/support/stub-claude.mjs` gained `STUB_CLAUDE_ECHO_MCP_ARGV` and `STUB_CLAUDE_ZERO_TOOL_CALLS`. Real smoke (`--module 5 --hbc <bundle> --max-turns 12`, real haiku call): 44.7s wall, 369,004 tokens in / 4,029 out, `toolCalls:0`, 0 names, 0 refusals, `exitCode:1` (now correctly reported as a failure -- pre-fix this always returned 0), equiv INCONCLUSIVE (no Hermes VM for v98, unrelated). `permission_denials` empty this run, but the model STILL made zero tool calls before `max_turns` -- its last turn's `stop_reason` was `"tool_use"`, as if it ran out of budget right as it was about to call one. New BUGS Open row (harness cluster, 2026-09-13) rather than diagnosed further (one real call was the whole budget). **Next worker: either retry the smoke with more turns / a leaner system prompt to see if it clears that BUGS row, or move on to item 4 (both are independently useful; item 4 is not blocked by this one).**
4. **Reuse one parsed module across calls**: `decompileFunction` (or a sibling) takes a caller-supplied parsed module + analysis so a run pays parse+analysis once (BUGS open row, cluster metrics). Measure: full `--only src` collection on the held-out app should drop from ~97 CPU-minutes to about the 12 s setup plus per-function emit. This ALSO fixes the checkpoint-1 `suggest_names`-on-a-large-bundle timeout noted in Gotchas above -- worth doing before the next real hand smoke against a bundle bigger than a construct fixture.
5. **Rewrite and combine jobs off-thread** (PUSHBACK P-65, BUGS open row): split compute from commit in `surfaces.ts` so the worker returns a transaction the main thread commits; then move `readability-rewrite-function` / `combine-files` into `readability-worker.ts`.
6. **Held-out recording** (orchestrator runs it on the plan, bounded with `--resume`) -> coverage and quality legs go green or the numbers say what to fix next.
7. Sweep-tier runs on the real held-out app for landing 1 (fidelity) and landing 3 (file-op pass), behind `requireSweep`.
8. Design calls for Fred (do not implement without him): tree multi-select for "combine these files"; a queued/running indicator for async actions; whether the 55 s stall test joins the default gate (P-64).

## Last update
2026-09-13 (agent/driver-fix): queue item 3 done -- `--tools ""` (no built-in tool to shell out to), `--hbc` now required (fails fast; "derive from the project" wasn't implementable, PUSHBACK P-66), the skill's opening instruction + worked example rewritten (no `help` reference -- not landed yet), and `toolCalls`/escalated `exitCode` so a zero-tool-call run is reported as a failure. 4 new stub-transcript tests (2 -> 6). Real smoke on the held-out app (module 5, `--hbc` supplied, `--max-turns 12`): `permission_denials` now empty (item 2's Bash-detour is gone) but still `toolCalls:0` -- new BUGS Open row (harness cluster). Next worker: queue item 4 (reuse one parsed module across calls) is unblocked and independent; alternatively, chase the new BUGS row (bigger `--max-turns`, or a leaner/more directive system prompt) before spending more real-call budget on it -- either is a reasonable next step, item 4 does not depend on it.
