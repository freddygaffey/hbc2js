# 2026-09-03 — P2.2 step 2: schema + serialization (lean Sonnet)

76k tokens, 36 tool calls, ~7 min, green first try, under budget. Commit e6ce155.

- src/project/schema.ts (envelope, 6 record types, v1 TAGS taxonomy, major-version refusal, builtFor merge precondition helper), io.ts (byte-deterministic (target,rid)-sorted JSONL + whole-dir load/save), evidence-resolver.ts (interface only; step 4 wires ArtifactService).
- P1 re-pointed at real code, same assertions; new P1g = load->save byte-identity on the sample store (round-trip proof). P2 stays on mock per its own header (step 4).
- Gate 1818/0.
