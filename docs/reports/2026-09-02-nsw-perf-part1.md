# 2026-09-02 — Service NSW superlinear term, part 1 — Sonnet, lean
Tokens 190k · tool calls 80 · green.

deb profile (whole-file, --lenient-env): passes-on 946 s vs passes-off 19 s. Hot frames: `expr-rebuild/match.ts` `listIndex` (whole-list index rebuilt on every applied site because `spliceList` changes list identity; 59 s self) and `passes/ast.ts` `expressionOnlyCheck` (whole-list effect sequence per site; ~90 s). Fixed: `nextRelevant` bounded scan over memoised `stmtInterest`; `expressionOnlyCheck` and `check.ts` register-count delta strip the shared prefix/suffix first. New `tools/perf/summarize-cpuprofile.mjs`; synthetic 5,000-site benchmark in `pipeline-speed.test.ts`. Remaining: `check.ts` `bu = registerUses(before)` fresh O(list) walk per site when the memo is cold (~4 s CPU on the synthetic) — part 2. NSW re-measure pending. Side result: the passes-on whole-file run completed with no E_UNBOUND_IDENT → the sameFrame fix removed the NSW abort (re-measure job confirms with app.js).
