# 2026-09-03 — P2.2 step 6: orphan detection (lean Sonnet)

112k tokens (over the 90k hard cap — the overrun pattern persists even with tightened briefs; splitting further next time), 62 tool calls, ~9 min, green first try. Commit 3c236cc.

- orphans.ts live-computed flag-never-drop (independent of evidence-resolver per E4); service/CLI wired (real orphans verb + stat count); step-5 interim builtFor throw replaced by spec's orphan semantics (E1).
- Proof: tag under artifact A -> open under B: excluded from reads, 1 orphan row w/ ctx; reopen under A: same rid active again. Zero drop, zero mutation.
- Gate 1869/0. Remaining: step 7 merge, step 8 check-store/measure + STATUS.
