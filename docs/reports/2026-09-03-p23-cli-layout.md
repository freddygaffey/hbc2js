# 2026-09-03 — P2.3 CLI + layout integration (lean Sonnet)

146k tokens (over 100k cap — systemic overrun continues), 126 tool calls, ~13 min. Commit de0be98.

- Layout: artifactDir = artifact ROOT everywhere; nested index/ shape only (the only shape the real writer emits) — one code path, no fallback; materializer updated to match. REAL-artifact proof: rn-template scan = 5265 strings, 2 tier-C findings; measure through the corrected path reproduces the finisher's numbers (recall 1.0, FP 0.38/1k).
- T7 fixed (real ProjectService signature + real status API); 10/10 secrets tests; glob wired -> gate now 1886 tests.
- CLI: secrets scan/report/list/show/hosts/paths (--json everywhere, --help wired); hosts/paths honestly empty pending the spec-11 taxonomy extension.
- P2.3 now functionally COMPLETE except §3.4 xref context-gating (documented v1-optional; standing truth flag).
- Gate 1 fail = concurrent step-8 agent's STATUS.md WIP vs the 101-line cap gate (theirs to resolve).
