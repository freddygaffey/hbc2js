# 2026-09-04 — P2.5 version-diff spec (lean Fable, third-slot)

55k tokens, 11 calls, ~6 min. Commit 3d3b539, docs/specs/14-version-diff.md.

- Tiers: exact-hash (identity, unique-both-sides; ambiguity demotes) / strong-structural (>=0.90 + 0.15 margin + 2 anchor families) / candidate (never a lead, never rebound) / unmatched (honest added-removed). Orphan auto-rebind at exact ONLY; below = flagged proposals, zero-silent-drop.
- Decision-8: 0 wrong asserted matches on >=5 appgen pairs; removed-check recall 100% on >=10 seeded guard removals; verb caps + build <=15% of index build; held-out = real rn-template version bump.
- Coordination: impl step 7 adds flags to tools/appgen — sequence after appgen increment 2 lands.
- 5 open questions -> review gate (launched).
