# 2026-09-04 — spec 18 (project storage/integrity) review gate (lean Fable) — APPROVED AS AMENDED

35k tokens, 7 calls. Commit 4b52e74 (spec 18's first tracked version + review).

- DATA-LOSS BUG fixed: §8 recovery re-exported on ANY hash mismatch, clobbering un-adopted hand edits on restart. Now: lag (content matches recorded hash) -> DB wins + re-export; hand edit (content != hash) -> adopt/restore/conflict, never auto-overwritten.
- Hook/CI: log entries now record affected shard ids + post-write hashes + chain continuity (else the pre-commit "log covers changed shards" check was DB-free-unverifiable); forged-hash+forged-log caught by CI verify --full (tamper-evident, acceptable).
- Content-hash finding ids hash ONLY immutable defining fields (status transitions can't change the id) — the concurrency win confirmed.
- §12 explicitly supersedes spec 16's "DB is THE storage; JSON a view" (dangling contradiction), keeping its DDL as cache.db.
- c02c1b3 (DB annotation write path) confirmed = §6 step 2; export/porcelain build ON it, replace nothing.
- Missing decision-8 quadruple + impl plan SUPPLIED by reviewer: round-trip 100%, mutation-fuzz detection 100% (held-out seeds), 1000-finding two-writer concurrency; suite <=120s, verify --full <=60s.
- Impl step 0 cleared: `hbcproj export` — materialize analysis/ shards (content-hash ids, per-shard hash lock + state binding) from the .hbcproj DB.
