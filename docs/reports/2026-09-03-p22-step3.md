# 2026-09-03 — P2.2 step 3: tags/bookmarks/comments stores (lean Sonnet)

99k tokens, 47 tool calls, ~14 min, green first try. Commits 25dcb8b, 61dbd05.

- Followed the SPEC's step 3 (narrower than the brief — correctly): TagStore ((target,tag) slots), BookmarkStore, CommentStore (fn-level + site-level w/ rangeStale), shared assertProvenance; write verbs reject missing prov before minting.
- A-PROV + A-DISPLAY proven: no-prov rejected across types; tool-vs-human provenance distinguishable; decompile.ts has zero project imports and a fixture renders byte-identically before/after annotation writes.
- Findings/evidence = step 4; orphans = step 6 (untouched, per spec).
- Gate 1841/0; 36/36 new tests.
