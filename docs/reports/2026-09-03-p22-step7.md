# 2026-09-03 — P2.2 step 7: merge (lean Sonnet)

142k tokens (90k cap — overrun pattern is systemic, tracked in scoreboard ranking), 85 tool calls, ~15 min. Commit 9a7f822.

- mergeStores: line-union + content-hash deterministic rid remap (repeat merge idempotent AND byte-identical, tested); conflict records mint per slot w/ both sides surviving (no silent pick, tested via tagsOn returning both); builtFor mismatch refused pre-work, store untouched (tested); per-type slot conventions honored (finding: (target,patternId) only).
- CLI: project merge <dir> + real conflicts verb replace step-5 stubs.
- Gate 1873 tests / 1 fail = pipeline-speed timing flake, 5/5 green isolated (campaign CPU).
- Remaining: step 8 (check-store, measure, STATUS) = P2.2 close-out.
