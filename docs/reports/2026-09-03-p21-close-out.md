# 2026-09-03 — P2.1 close-out (lean Sonnet; cut by spend limit at its final commit, landed by orchestrator)

~110k tokens (est., cut before self-report), WIP preserved as 2069f3d; gate verified green by orchestrator (1805/0).

- Build-time MISS: FIXED, no renegotiation — semantic-walk ran per-function THREE times (calls/globals/string-uses); consolidated to one buildSemanticIndexes pass, byte-identical output (sorted-diff verified, zero lines differ). Now <=25% of decompile time.
- Index-size MISS: openly RENEGOTIATED per the reviewer's ruling — 30% -> 70% of rendered bytes with the measured completeness floor documented in spec §10 "P2.1 close-out size renegotiation"; completeness never traded.
- overlayHash wired + full §4.2 staleness matrix in stale.test.ts; new tests: host-globals, name-queries, rename-survival (A5/A7/A10). BUGS rows resolved.
- P2.1 IS NOW COMPLETE: format + indexes + query CLI + independent checker + measured record, all targets met or openly renegotiated.
