# 2026-09-03 — P2.3 finisher: T4 fix + measure.ts (lean Sonnet)

104k tokens, 65 tool calls, ~9 min, on budget. Commit a2cf275.

- T4 root cause: per-span addFinding on a (sid,patternId) slot -> same-pattern spans alternately superseded forever; fixed by grouping hits per patternId (one finding, multi-span evidence — spec §4.2 literal). T4/T5/T8 now pass; 9/10 standalone.
- ALL decision-8 targets MET: tier-A 100%, overall 100% (>=95), FP 0.38/1k tuning (<=5), 1.41/1k held-out react-navigation (<=8), timings trivially under bounds (honest caveat: timed on the seeded artifact per brief, not 4k-fn scale; and the 0.38 is raw, no reviewed allowlist applied — stated, not assumed).
- T7 fails OUTSIDE src/secrets: tests/secrets/refutation-sticky.test.ts uses ProjectService({artifactDir}) vs the real (artifactDir, artifact) signature. ALSO flagged: SecretsService reads flat strings.json; real artifacts nest index/strings.json — must be fixed in the CLI-wiring step before real-bundle use.
- Remaining P2.3: CLI verbs (secrets scan/report/list/hosts/paths), §3.4 xref context-gating (standing truth flag), T7 call-shape + layout fix.
- Gate red only on concurrent step-7 agent's untracked merge.ts WIP (verified: zero errors excluding it).
