# 2026-09-02 — batch-3 sugar specs (16/17/18) — Fable, lean
Tokens 113k · tool calls 47 · green (design only).

Wrote docs/specs/passes/16-destructure.md, 17-spread-rest.md, 18-optional-chain.md against shapes confirmed at v94+v99 (--emit-tree). destructure: one labeled block per element (each own iterNext + done-flag + tail break; staged-commit style — finer than P-9's per-element); array rest = inline index-append while; object rest = 3-arg copyDataProperties. spread-rest: helper forms (arraySpread chains, 2-arg copyDataProperties, copyRestArgs inline). optional-chain: labeled-block suffix runs, loose-Eq-null guard per link, Reflect.apply for ?.(); no cond reaches stage B (P-3 confirmed). Ownership: destructure owns pattern-rest (loop + 3-arg helper), spread-rest owns helper-call forms (2-arg + arraySpread + copyRestArgs); spread-rest after:[destructure]. All checkers recompute-and-diff; 18 tags guard-depth for D14. P-3+P-9 resolved. Found: default-params doesn't fire at v99 on 38/39 (BUGS flagged).
