# 2026-09-03 — P2.2 step 4: findings + evidence resolution (lean Sonnet)

146k tokens (over ~110k budget — flag in ranking), 67 tool calls, ~12 min, green first try. Commits a803482, 2579634.

- FindingStore (R3 patternId slot discriminator; status engine keyed by finding rid), ArtifactEvidenceResolver over ArtifactService (+ non-throwing hasFn/hasString/hasModule), dynamic-ref detection.
- A-STATUS proven: confirm requires dynamic ref; tool can NEVER self-confirm (human can); refuted sticky both directions; transitions append-only w/ provenance.
- P2 repointed per its header (real rule code, mock resolver per spec §6); 9-test real-artifact resolver integration on rn-template.
- Correctly kept step-4 scope (query methods on stores; ProjectService/CLI = step 5; orphans = 6; merge = 7).
- Gate 1859/0.
