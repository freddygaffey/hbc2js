# 2026-09-03 — P2.3 string/secrets indexer spec (lean Fable)

45k tokens, 11 tool calls, ~5 min, clean first try. Commit ac7d26f, docs/specs/12-string-secrets.md.

- Artifact-only consumer (strings/string-uses/ArtifactService) — zero xref re-derivation, bundle-free.
- Findings as spec-11 records (source:"tool", evidence spans + use-sites); tiers A/B/C candidate-language only; reachability appends evidence, never confirms; no network key-probing (non-goal w/ rationale); <=8-char secret quoting cap.
- Decision-8: 100% tier-A / >=95% recall on a seeded fixture (~50 secrets + 30 near-misses), FP <=5/1k tuning / <=8/1k held-out (react-navigation, never tuned on), cold <5s / warm <0.5s at 4k fns.
- T1-T8 specified verbatim for impl step 0 (tests/ owned by concurrent agent).
- Open questions 1-4 -> review gate.
