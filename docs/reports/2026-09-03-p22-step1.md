# 2026-09-03 — P2.2 step 1: RevisionStore<T> extraction (lean Sonnet)

77k tokens, 43 tool calls, ~12 min, green first try. Commit e02d309.

- src/project/revision-store.ts: append-only slot-keyed engine (get/history/set/revert/search/patchActive, injectable clock, seq round-trip for byte-exact save).
- name-overlay now a thin wrapper — public API unchanged, disk key-order preserved; PROOF: name-overlay tests untouched and passing incl. the byte-determinism round-trip; render.ts/cli.ts needed zero changes.
- P3 tag-supersession flipped on (todo removed per its header, no other edits); tests/project/** wired into npm-test glob (step-0 deferral closed).
- Gate 1817/0 (pipeline-speed CPU flake re-run idle green — campaign running).
- Next: step 2 src/project/schema.ts.
