# 2026-09-03 — P2.1 impl steps 0-1 (+partial 2) (lean Sonnet)

102k tokens, 71 tool calls, ~9 min. Committed on main (see log), not pushed.

- Step 0: A1 acceptance test materialised VERBATIM + sample-artifact fixture; 4/4 green standalone; wired into test globs.
- Step 1: src/artifact/{schema,build,write}.ts; --split now writes manifest.json + index/{functions.jsonl,modules.json}; --overwrite immutability flag (refusal + override verified) per §10-E4.
- Step 2 partial: functions/modules rows from env/closure graph; rn-template: 4199 rows == functionCount, 435 modules == MODULES.json, ownership 4125/4199 (rest legitimately factory-less). ranges.jsonl DEFERRED honestly — needs a real renderer range-recording hook; fabricating ranges would violate the truth rule.
- Note for consumers: manifest.index.builtFor.producer = sha256 of the producer config JSON (spec left encoding open; documented in build.ts).
- Gate at landing: 1756/1760, sole fail = pipeline-speed 12.2x vs 12x ceiling — concurrent var-naming WIP + CPU load; verdict deferred to var-naming's idle landing run.
