# 2026-09-03 — P2.3 step 0: patterns + seeded fixture + T-tests (lean Sonnet)

108k tokens, 69 tool calls, ~22 min. Commit ac65a50.

- src/secrets/patterns.ts: hbc2js-secrets/1, 19 patterns (7 tier-A / 3 B / 2 C secrets + 6 tag categories), every pattern vendor-doc/RFC-cited; gitleaks/trufflehog NEVER consulted (R2); honest "no vendor spec, xref-role-gated" citations for flag/debug patterns.
- Seeded fixture: 47 synthetic secrets (all obviously fake, self-check-verified per-regex) + 33 near-misses inside a 249-string hand-built spec-10 artifact; sid-1 zero-use case for T4.
- T1/T3 pass now; T2/T4/T5/T6/T7 fail cleanly pending classify/service/measure; T8 skips (oracle-escalatable). tsc typechecks tests/secrets even unglobbed (caught 1 issue).
- Gate: 1 fail = pipeline-speed CPU flake under campaign load; orchestrator re-ran isolated: 5/5 pass -> effectively fail 0.
- Handoff: classify(value)->Hit[], SecretsService({artifactDir}), measure --json [--held-out]; RevisionStore already landed so ruling-4 shim unneeded.
