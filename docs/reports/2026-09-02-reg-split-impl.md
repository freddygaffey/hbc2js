# 2026-09-02 — reg-split implementation (spec 19) — Sonnet, lean
Tokens 389k · tool calls 191 · green (OPT-IN; over budget: intricate 3-way matcher/checker soundness).

src/passes/reg-split/: web/live-range matcher (forward reaching-defs fixpoint over stage-B tree; loop back-edge, break/continue/labeled joins, try anyB exception rule, for-header ForPart timing), pure rN_j rename writer, independent 3-layer sound checker (byte-identical undo + occurrence bijection/nested-frame leak + independently-coded coarse reach relation R). 16 tests incl. checker-refusal mutations (rejects splitting a loop var / a try-def-vs-handler-use with `coarse-reach-crosses-split`). Splits reuse: 04-for-loop r0's 6 jobs → r0,r0_2…r0_7 each single-def, do-while accumulator stays one var (14→22 unique reg-vars). Full fixture sweep with pass ON: 0 crashes.
LANDED optIn:true (NOT default-on) — PUSHBACK P-11: default-on trips (1) pipeline-speed 12x ceiling (measured 13.6x — R-loop/R-catch coarsening still O(regs×tries)/fn) and (2) ~10 rungs' tests assert r\d+ regexes that now legitimately see rN_j. Follow-up to make it default-on (+ then var-naming compound to NAME the split ranges = the readability win): fix perf under 12x + update the ~10 rung test regexes to accept rN_j.
