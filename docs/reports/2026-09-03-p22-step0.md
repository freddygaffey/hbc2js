# 2026-09-03 — P2.2 step 0: acceptance harness + sample store (lean Sonnet)

75k tokens, 37 tool calls, ~7 min, green first try. Commit 98b3ceb.

- tests/project/: P1 format-schema (6 pass, checks the real sample-store fixture), P2 finding-evidence vs mock resolver (5 pass, incl. dynamic trace: refs), P3 tag-supersession (todo-marked; step 1 deletes the {todo} + import guard, no other edits).
- Sample store: spec 11's format examples as real files (worked finding, supersession chain, status transition).
- Deliberate deferral: npm-test glob lacks tests/project/** — goes into step 1's commit WITH the RevisionStore extraction (avoids package.json collision with the concurrent gate-cache agent).
- Gate 1805/0; tsc clean.
