# Agent run reports

One file per agent run: `<YYYY-MM-DD>-<slug>.md` — the agent's final report (condensed by the orchestrator when long), tokens used, tool calls, landed-green-first-try. `docs/AGENT-LOG.md` is the one-line index/history; `docs/QUEUE.md` is what runs next.
