# 2026-09-03 — P2.4 steps 0-1: fixture + licensing + red harness (lean Sonnet)

102k tokens, 67 calls, ~16 min, on budget, green first try. Commit 2348d79.

- vuln-app fixture: 10 SEED-tagged classes, lockfile pins w/ 3 real GHSAs, committed v96.hbc (decompiles cleanly), credential seed DEFUSED at rest (materialize.ts scheme reused; grep-verified in source AND compiled bytes). APK fixture = Lane M's job, flagged not silently gapped.
- T-L licensing re-verified hands-on (5/5 SPDX match, URL+date in tools/security/LICENSES.md). T1 pass; T2-T8 skip-with-reason pointing at their owning step (oracle-escalatable). tests/security in gate globs.
- Gate 1904/0.
- Lane O handoff: osv-gate.ts two-key classifier + measure-osv.ts; fixture deps not yet bundled into the hbc — step 2 must bundle or add signature strings (README flags it).
