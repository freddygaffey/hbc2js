# 2026-09-02 — Design D naming overlay (rename tool v1) — Opus
Tokens 170k · tool calls 95 · green. THE substrate for the LLM fuzzing loop + Phase 2.

Feasibility confirmed: fnIndex reaches emit as cfg.functionIndex ({fn,reg} stable+addressable); external name injects into var-naming's slot via renameRegistersInFrame before the astPass. src/name-overlay/: id.ts ({fn,reg}), store.ts (versioned append-only sidecar, setName/getName/revert/history/search, supersedes, collision flag), gate.ts (reuse gate via classifySite; --override → low+overridden; reserved/property/no-binding refused), render.ts (pure frame-local alpha-rename, deterministic suffix), service.ts (resident). CLI `name set|get|revert|search` + `render`. 15 acceptance tests, trace-oracle 0-DIVERGENT, behaviour-preserving by construction. v1 = register locals; env slots/fn names reserved (pending closure-naming/fn-naming). Names key on the binary, not text — an LLM cites {fn,reg}, never breaks when another name lands.
