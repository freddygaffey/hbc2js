# 2026-09-02 — unverified-code guards (CONSOLIDATION 27, 28) — Sonnet, lean
Tokens 119k · tool calls 98 · green first try.

Finding: both guards already refuse real input; they were untested. Item 27: layout classes A/D and pre-87 B return no opcode table → E_UNSUPPORTED_VERSION before decode; OpcodeDef.unverified → E_UNKNOWN_OPCODE (set on no table today); arm64 build already arch-gated. New `tests/gate/parse/unverified-paths.test.ts` (guards throw; 5 canonical versions still decode). Item 28: fetch-db.sh already quarantines any signature with an empty `subtractedBaselines` marker into `_rejected-unsubtracted/` — the "doc note only" claim was stale; new `tests/gate/tools/filter-unsubtracted.test.ts`. No new refusal logic invented (nothing to run behind a force flag). CONSOLIDATION 27/28 done.
